# Diario autónomo 2026-05-10

> Resumen 60s de lo que el coordinator procesó hoy.

---

## Pasada 1 — Cierre administrativo del lote 117c

**Trigger:** `trabaja` (Jorge — día nuevo, OK implícito de cierre del último sprint del lote 117c que quedó EN_REVISION_HUMANA).

**Procesado (sin cambios de código):**

- **SPRINT-117c6** promovido formalmente de EN_REVISION_HUMANA → COMPLETADO. Hash deployado `9b5aee2`. Entrada activa colapsada a stub "MOVIDO A HISTÓRICO". Entrada completa preservada en sección histórico.
- **SPRINT-117c4** también promovido formalmente (su entrada activa quedó EN_REVISION_HUMANA del 2026-05-09 aunque el histórico ya decía COMPLETADO con "si" implícito).
- **SPRINT-117c2** también promovido formalmente (mismo patrón).
- Header de `COLA_AUTONOMA.md` actualizado a fecha 2026-05-10 + nota explícita de cola autónoma agotada.

**Lote 117c — CERRADO al 100%:** 5 de 6 sub-sprints aprobados deployados (117c1 `759a76b`, 117c2 `9f71883`, 117c3 `9c262c9`, 117c4 `480532f`, 117c6 `9b5aee2`). El 6º (117c5) fue rechazado por Jorge en el OK selectivo del 2026-05-09 — chocaba con el sistema de permisos individuales `usuarios/{uid}.permisos.*`.

**Sin commits de código:** la pasada es puramente administrativa. Sólo edits a docs de sprint para sincronizar estado.

**Cola revisada — sin sprints nuevos:** Cowork no agregó nada durante la noche. `BLOQUEOS.md` sin OKs nuevos.

**Pendientes humanos en cola (NO procesables autónomos):**
- SPRINT-112 — Schema drift + matriz de permisos por rol. Requiere QA por rol con cada empleado presente.
- SPRINT-113 padre — UX flujo orden. 4 de 6 criterios COMPLETADOS por sub-sprints 113a/b/c. Pendiente: QA manual end-to-end con técnico/operaria reales.

**Decisión deliberada — NO se generó `CIERRE_LOTE_117c_2026-05-10.md`:** cada sub-sprint del lote ya tiene su entrada histórica completa con resultado, validación, plan de rollback, hash y trail en `EJECUCION_AUTONOMA.md`. La propuesta original (`docs/sprints/PROPUESTA_IA_2026-05-08.md`) sigue siendo el doc de referencia del rediseño completo. La sub-regla CLAUDE.md "documentación viva" indica NO crear docs por inventarlos. Si Jorge o Cowork lo piden explícitamente más adelante, se genera entonces.

**Bloqueos abiertos:** ninguno introducido. Los pendientes humanos no son bloqueos del coordinator — son trabajo humano fuera del scope autónomo.

**Tiempo estimado:** ~7 minutos (sólo lectura de cola + edits a docs).

---

## Cazadores anti-regresión (sin cambios)

- **P-001..P-007:** baseline 0 hits desde el cierre de SPRINT-103. Sin nuevas reintroducciones.

## Próximos pasos

- Cuando Cowork agregue sprints nuevos a `COLA_AUTONOMA.md`, dispará `trabaja` y el coordinator los procesa.
- Para SPRINT-112 (matriz QA por rol): requiere coordinar con cada empleado (Aury, Wilainy, Yohana, etc.) para validar las celdas de la matriz. Sprint humano-presencial.
- Para SPRINT-113 padre QA: requiere recorrer end-to-end un flujo real con técnico + operaria + admin. Sprint humano-presencial.

## Métricas
- Total commits del día: 0 (cierre administrativo puro).
- Total minutos del coordinator: ~7.
- Hotfixes follow-up disparados por bugs introducidos hoy: 0.

---

## Pasada 2 — Procesar SPRINT-119 a 123 (5/5 COMPLETADOS, sin bloqueos)

**Trigger:** `trabaja` (Jorge — segunda pasada del día, después de que Cowork agregó 5 sprints procesables autónomos a la cola en commit `e019ea0`).

**Procesado:**

- **SPRINT-119** — Postmortem-positivo del lote 117c (rediseño IA del sidebar). Hash `55f55e3`. Archivo nuevo `docs/postmortems/2026-05-10-rediseno-ia-aprendizajes.md` (formato libre, no es bug). Documenta el approach exitoso de dividir un rediseño grande en 6 sub-sprints chicos con QA visual humana entre cada deploy. 5/6 sub-sprints aprobados deployados sin rollback. El sexto (117c5) rechazado por Jorge en OK selectivo previo.
- **SPRINT-120** — Cazador P-008 health-check notis legacy. Hash `a61022e`. Script nuevo `scripts/invariantes/check-notis-legacy-data-shape.ts` + comando `npm run audit:notis-legacy` + entrada P-008 en `docs/PATRONES_REGRESION.md`. Es el primer cazador de DATOS en producción (los anteriores P-001..P-007 son de código). Requiere Admin SDK + Firebase, queda FUERA de pre-commit hook por diseño.
- **SPRINT-121** — Eliminar `Productos.tsx` (Catálogo legacy) del routing. Hash `03e24df`. Archivo eliminado (387 líneas), import quitado de `App.tsx`, ruta reemplazada por `<Navigate to="/admin/precios" replace />`. Bundle bajó de 2,651.94 kB a 2,643.32 kB.
- **SPRINT-122** — Primera lectura formal de `npm run metricas`. Hash `ee4cecc`. Doc `METRICAS_2026-05-10.md` generado por el script + interpretación cualitativa agregada por archivist en modo MÉTRICAS. Veredicto: **salud BUENA**, recurrence rate 0%, MTBF 1.0 d engañoso (racha mala 2026-05-07/08 superada). Ninguna acción urgente.
- **SPRINT-123** — Decidir destino de `COWORK_CONTEXTO.md`. Hash `ba5180a`. Cerrado como **no-op administrativo** — la decisión "versionar" ya estaba aplicada en commit `0181778` del 2026-05-08. La cola contenía un sprint obsoleto.

**Cazadores anti-regresión:** durante toda la pasada, pre-commit hook corrió en cada commit con 7/7 PASS, 0 hits. Total cazadores activos al cierre: **8** (P-001 a P-008, ahora con P-008 nuevo). Allowlist size: 17, sin cambios.

**Sprint que llegó durante la pasada (NO procesado):**
- **SPRINT-124** — Auditoría: cobertura de permisos granulares vs módulos del sidebar. Cowork lo agregó mientras yo procesaba la cola. Alta prioridad pero scope grande (~20 módulos × 5 roles × 3 capas). Queda PENDIENTE para próxima pasada `trabaja` — el usuario explícitamente solo listó 119-123 en el pedido inicial.

**Bloqueos:** ninguno introducido. `BLOQUEOS.md` sin OKs nuevos.

**Tiempo total estimado:** ~25 minutos.

## Métricas pasada 2
- Commits pusheados: 5 (`55f55e3`, `a61022e`, `03e24df`, `ee4cecc`, `ba5180a`).
- Minutos del coordinator: ~25.
- Hotfixes follow-up disparados por bugs introducidos: 0.
- Cazadores activos al cierre: 8 (+1 nuevo P-008).

---

## Pasada 3 — Procesar SPRINT-124 (1/1 COMPLETADO, doc-only)

**Trigger:** `trabaja` (Jorge — tercera pasada del día, scope explícito de SPRINT-124 que había quedado PENDIENTE de la pasada 2).

**Procesado:**

- **SPRINT-124** — Auditoría: cobertura de permisos granulares vs módulos del sidebar. Archivo nuevo `docs/MATRIZ_PERMISOS_VS_MODULOS.md` (251 líneas). Read-only. Mapea 43 filas del sidebar contra la fuente real de gating. Hallazgo central: el modal expone 29 de las 35 keys booleanas required de `PermisosSistema`; 6 keys quedan invisibles (`pagosRegistrar`, `ordenesEnviarAFacturacion`, `facturasCerrar`, `bancosGestionar`, `avancesGestionar`, `clientesReactivacionGestionar`); 18 módulos del sidebar son rol-only y NO controlables persona-por-persona.

**Veredicto entregado a Jorge:** la regla declarada "los permisos vienen del módulo de usuarios" se cumple **parcialmente, no totalmente** — 37% granular puro + 14% mixto + 42% rol-only + 7% granular-no-modal (limbo).

**Recomendación archivada en el doc:** Opción A (riesgo bajo, ~5 líneas en `GestionUsuarios.tsx`) para exponer las 3 keys granular-no-modal con módulo (Bancos, Avances, Reactivación de clientes). Si Jorge aprueba, abrir SPRINT-125. Decisión queda en sus manos.

**Bugs colaterales detectados (NO arreglados — fuera de scope):**
- Coord ve 4 links rotos en sidebar para Web / Empresas Aliadas / Formularios / Solicitudes (gate sidebar `esAdminOCoord`, gate ruta `RolRoute roles=['administrador']`). Bug visual menor.
- Comisiones tiene gating doble inconsistente (sidebar OR-permissive, ruta rol-restrictive).
- Usuarios & Permisos mismo patrón.

**Cazadores anti-regresión:** 7/7 PASS, 0 hits. Pre-commit hook ejecutado correctamente. Build pasa.

**Bloqueos:** ninguno introducido. Cola autónoma queda vacía al cierre.

**Tiempo total estimado:** ~25 minutos.

## Métricas pasada 3
- Commits pusheados: 1 (`e635230`).
- Minutos del coordinator: ~25.
- Hotfixes follow-up disparados por bugs introducidos: 0.
- Cazadores activos al cierre: 8 (sin cambios).

## Pasada 4 — Procesar SPRINT-112 fase documental (1/1 COMPLETADO, doc + script read-only)

**Trigger:** `trabaja` (Jorge — cuarta pasada del día, scope explícito SPRINT-112 fase documental). Jorge clarificó que el sprint tiene 2 componentes — (a) doc + script de auditoría procesable autónomo; (b) QA manual con usuarios reales por rol que requiere humano. Procesé sólo (a). El (b) queda en `BLOQUEOS.md` como SPRINT-112-QA.

**Procesado:**

- **SPRINT-112 fase documental** — Hash `6aae2e5`. 3 archivos creados/modificados:
  - `docs/MATRIZ_PERMISOS.md` (200 líneas) — matriz por flujo crítico × rol. 27 flujos × 6 roles = 162 celdas. Cada celda con cita exacta de gate UI + rule. Hallazgos: 16 granular puro, 6 granular-no-modal (cubre las mismas 6 keys huérfanas que detectó SPRINT-124), 5 rol-only. 3 inconsistencias UI ↔ rule detectadas (eliminar orden #14, ver eliminadas #15, secretaria + trabajo realizado #8).
  - `scripts/auditoria/schema-drift.ts` (274 líneas) — script Admin SDK read-only que samplea N=20 docs por colección y reporta drift entre TS interfaces y docs reales. Smoke test contra prod: 65 drift+ (campos en Firestore NO en TS, ej: `clientesContactados` en `campanas_marketing`), 157 drift- (campos en TS no en muestreo de 20 docs — probable muestreo bajo, no realmente drift).
  - `package.json` — comando `npm run audit:schema-drift` agregado.

**Doc complementario:** `docs/MATRIZ_PERMISOS.md` (este sprint, vista por flujo) y `docs/MATRIZ_PERMISOS_VS_MODULOS.md` (SPRINT-124, vista por módulo del sidebar) son hermanos — se referencian mutuamente. Las 6 keys granular-no-modal aparecen en ambos.

**Decisiones que NO tomé (registradas en `BLOQUEOS.md` como sub-sprints humano):**
- **SPRINT-112-QA** (BLOQUEADO): QA manual de las 162 celdas con usuarios reales. ~2h con Yohana/Aury/Wilainy/secretaria activa. Este es el componente humano del sprint padre.
- Los SPRINT-125/SPRINT-126 (que Cowork ya agregó como PENDIENTES en cola tras OK de Jorge "ambos") NO los procesé en esta pasada — Jorge no los mencionó en el prompt de invocación. Quedan para próxima pasada `trabaja`.

**Cazadores anti-regresión:** 7/7 PASS, 0 hits durante todo el sprint. Pre-commit hook ejecutado correctamente.

**Bloqueos abiertos al cierre:** 1 nuevo (SPRINT-112-QA). Total bloqueos del día: 1.

**Cola autónoma al cierre:** SPRINT-125 + SPRINT-126 PENDIENTES (agregados por Cowork durante esta pasada). NO vacía como pasadas anteriores.

**Tiempo total estimado:** ~30 minutos.

## Métricas pasada 4
- Commits pusheados: 1 (`6aae2e5`).
- Minutos del coordinator: ~30.
- Hotfixes follow-up disparados por bugs introducidos: 0.
- Cazadores activos al cierre: 8 (sin cambios).
- Sub-sprints humano nuevos en BLOQUEOS.md: 1 (SPRINT-112-QA).

---

## Resumen del día (4 pasadas, total)
- Sprints procesados: 1 (pasada 1, administrativos 117c) + 5 (pasada 2) + 1 (pasada 3) + 1 (pasada 4) = **8 sprints**.
- Commits de código/docs pusheados: 5 (pasada 2) + 1 (pasada 3) + 1 (pasada 4) = **7 commits**.
- Bloqueos abiertos al cierre: 1 nuevo (SPRINT-112-QA, humano puro).
- Cola autónoma al cierre del día: **SPRINT-125 y SPRINT-126 PENDIENTES** — Cowork los agregó durante pasada 4 con OK previo de Jorge "ambos". Próxima pasada los procesa.
- Pendientes humano-presenciales: SPRINT-100, SPRINT-112-QA (en BLOQUEOS.md), SPRINT-113 padre.
- Sin recurrencias de bugs históricos. Sistema estable.

---

## Pasada 5 — Procesar SPRINT-125 + SPRINT-126 (2/2 COMPLETADOS, sin bloqueos)

**Trigger:** `trabaja` (Jorge — 5ª pasada del día, los 2 sprints quedaron PENDIENTES tras pasada 4 con OK explícito previo registrado en COLA_AUTONOMA.md línea 6).

**Procesado:**

- **SPRINT-125** — Hash `8d1851e`. Exponer 3 keys huérfanas (`bancosGestionar`, `avancesGestionar`, `clientesReactivacionGestionar`) en el modal de Usuarios. Cambio mínimo: agregada sección "Operaciones" en `GestionUsuarios.tsx:991` con las 3 checkboxes. Las keys ya estaban definidas en `PermisosSistema`, con defaults por rol y usadas en sidebar/rules; solo faltaba la UI del modal. Matriz `docs/MATRIZ_PERMISOS_VS_MODULOS.md` actualizada: 19 granular + 0 granular-no-modal.
- **SPRINT-126** — Hash `af2ba02`. Bugs colaterales de SPRINT-124. 3 ajustes en `Sidebar.tsx`: (1) Web/Empresas Aliadas/Formularios/Solicitudes ahora gateados a admin-only en sidebar (alineado con RolRoute admin-only en App.tsx) — Wilainy coord deja de ver 4 links rotos; (2) Comisiones alineado a `esAdminOCoord` (antes `esAdminOCoord || p('configuracionVer')`); (3) Usuarios & Permisos alineado a `esAdminOCoord` (antes `p('personalModificar')`). Decisión: alinear sidebar a la ruta más restrictiva, no abrir permisos, no tocar rules. Matriz sección 4.3 marcada como RESUELTA.

**Cazadores 7/7 PASS** en ambos commits. Pre-commit hook ejecutado sin bypass.

**Bloqueos nuevos:** 0. Ninguno de los 2 sprints tocó `firestore.rules`, migraciones, ni endpoints `api/`.

**Tiempo estimado:** ~10 minutos.

---

## Pasada 6 — Procesar SPRINT-127 + SPRINT-128 (1 COMPLETADO + 1 BLOQUEADO)

**Trigger:** `trabaja` (Jorge — 6ª pasada del día, después de que Cowork agregó 2 sprints más de "deuda técnica conocida" a la cola en commit `30b88c9`).

**Procesado:**

- **SPRINT-127** — Hash `305a9e5`. Cleanup notificaciones legacy ruta **B1 conservadora**. Auditoría confirmó que TODOS los 11 callers de `crearNotificacion` ya pasan `userId` (no `destinatarioId`) — el renombrado masivo se hizo en commit anterior `3733237`. Agregadas assertions runtime + JSDoc en `notificaciones.service.ts` que emite `console.warn` si llega `destinatarioId` o falta `userId`. Typing legacy se mantiene porque scripts/queries lo necesitan. Query dual del service intacta como red de seguridad. Gotcha "bug pre-existente en notificaciones" tachado en CLAUDE.md. Ruta B2 (eliminar query dual) queda como sprint follow-up que requiere correr `auditoria-notis-legacy-todos.ts` con service-account.
- **SPRINT-128** — **BLOQUEADO** (movido a `BLOQUEOS.md`). Builder evaluó R1 vs R2 durante archivist PRE-CHANGE y descubrió que:
  1. **R1 es no-op**: `PERMISOS_DEFAULT_OPERARIA` (src/types/index.ts:1267) **NO incluye** `ordenesEliminar: true` override — hereda de `TODO_FALSE` → default real es `false`. El sprint pedía "cambiar `true → false`" pero ya era `false`.
  2. **R2 (alinear rule a `puede('ordenesEliminar')`) toca `firestore.rules`** → BLOQUEO obligatorio por sub-regla autonómica.
  3. **Hallazgo colateral pagado en flight**: `docs/MATRIZ_PERMISOS.md` líneas 61+92 decían erróneamente "default operaria `=true`". Corregido en mismo commit del bloqueo con nota explicativa SPRINT-128.

**Cazadores 7/7 PASS** en commit `305a9e5`. Pre-commit hook ejecutado sin bypass.

**Bloqueos nuevos abiertos al cierre:** 1 (SPRINT-128 R2, requiere OK de Jorge para alinear `firestore.rules:369` al granular `ordenesEliminar` + `deploy:rules`).

**Tiempo estimado:** ~18 minutos.

---

## Resumen del día (6 pasadas, total — actualizado tras pasada 7)
- Sprints procesados: 1 + 5 + 1 + 1 + 2 + 2 = **12 sprints**.
- Commits de código/docs pusheados: 5 + 1 + 1 + 2 + 1 = **10 commits** (+ 1 pendiente del cierre administrativo de esta pasada con los docs).
- Bloqueos abiertos al cierre: 2 (SPRINT-112-QA humano puro + SPRINT-128 R2 esperando OK).
- Cola autónoma al cierre del día: **VACÍA** (sin sprints procesables autónomos pendientes).
- Pendientes humano-presenciales: SPRINT-100, SPRINT-112-QA, SPRINT-113 padre.
- Sin recurrencias de bugs históricos. Sistema estable.
- **Lección de la pasada 6:** un sprint puede tener spec correcto pero la realidad puede haber cambiado bajo sus pies (R1 era no-op por estado actual del código). El flujo aguantó: archivist PRE-CHANGE detectó el desfase, se hizo la corrección documental, y el sprint subió a BLOQUEOS con el fix conceptual correcto. Sin pedirle nada a Jorge.

---

## Pasada 7 — Procesar `procesa bloqueos`: SPRINT-128 R2 COMPLETADO (1/1)

**Trigger:** `procesa bloqueos` (Jorge — pasada 7 del día, después de que Cowork registró el `OK: jorge 2026-05-10 vía Cowork` en `BLOQUEOS.md` SPRINT-128 con quote: "puedes corregir las reglas tu por favor"). SPRINT-112-QA sigue sin OK humano (es trabajo humano puro, no procesable autónomo) — queda en bloqueos.

**Procesado:**

- **SPRINT-128 R2** — Hash en `git log --grep SPRINT-128 -1`. Rule `firestore.rules:369` cambiada de `allow delete: if esAdminOCoord();` a `allow delete: if isAuth() && userData().permisos.ordenesEliminar == true;` (usando el helper `userData()` ya existente en línea 62 — decisión más limpia que inlinear el `get()` completo del spec literal). Acompañada de 9 líneas de comentario explicativo (rationale del cambio, defaults por rol, defensa segura si `permisos` missing, trazabilidad SPRINT-128 R2). Restricción explícita honrada: solo línea 369 + comentarios — ningún otro cambio en `firestore.rules`.

**Flujo ejecutado:**
1. **archivist PRE-CHANGE (auto-rol):** categoría especial P-005 detectada; postmortem `2026-05-07-iniciar-chequeo-rules-sin-deploy.md` leído antes de cualquier edit. Lock pre-cambio: `94c7639...` deployedAt `2026-05-07T22:31:41.677Z`.
2. **Builder:** edit directo a `firestore.rules` línea 369.
3. **`npm run deploy:rules`:** `released rules firestore.rules to cloud.firestore`. Lock actualizado a `29247a9ac037fdc9a7398db716a15c31521a905e7438e8b857d95b12440561c6` deployedAt `2026-05-10T23:03:57.139Z`. Warnings inocuos pre-existentes en el archivo (`esSecretaria/esOperaria/esTecnicoDe` unused) — no introducidos por este sprint.
4. **regression_guardian (auto-rol):** PASS por análisis semántico contra P-001..P-007.
5. **reviewer (auto-rol con foco rules — sub-regla obligatoria):** APPROVED. Análisis de 3 paths (doc existe / no existe / campo missing) confirmando fail closed seguro.
6. **Documentación sincronizada:** `MATRIZ_PERMISOS.md` #14 marcado RESUELTO + cita de la nueva rule. `COLA_AUTONOMA.md` entrada SPRINT-128 colapsada a stub COMPLETADO. `BLOQUEOS.md` bloque reemplazado por stub + entrada al final de Histórico de desbloqueos con spec original en `<details>`.
7. **Commit + push:** pre-commit hook PASS sin bypass. Cazadores 7/7 PASS (P-001..P-007). P-008 no se corre en pre-commit (es audit on-demand de datos Firestore).

**NO se generó cazador nuevo P-XXX.** Este sprint fue cierre de deuda técnica conocida (auditada por matriz), no hotfix de bug reportado por usuarios. La sub-regla "cada bug en producción → cazador" no aplica. Si en el futuro una operaria con `ordenesEliminar=true` activado tira `permission-denied`, ahí sí se abre postmortem + cazador.

**Bloqueos nuevos:** 0. SPRINT-112-QA sigue abierto (humano puro). SPRINT-128 cerrado y movido a Histórico de desbloqueos.

**Tiempo estimado:** ~22 minutos.

## Métricas pasada 7
- Commits pusheados: 1 (commit del sprint — ver `git log --grep SPRINT-128`).
- Minutos del coordinator: ~22.
- Hotfixes follow-up disparados por bugs introducidos: 0.
- Cazadores activos al cierre: 8 (P-001..P-008, sin cambios; P-008 sigue siendo audit on-demand).
- Lock `firestore.rules.deployed.lock`: sincronizado a `29247a9...` (cambio del día).
- Sub-sprints humano nuevos en BLOQUEOS.md: 0.

---

## Resumen del día (7 pasadas, total — final)
- Sprints procesados: 12 (de pasadas 1-6) + 1 (pasada 7) = **13 sprints**.
- Commits de código/docs pusheados: ~11 (10 hasta pasada 6 + 1 del cierre pasada 7).
- Bloqueos abiertos al cierre: 1 (SPRINT-112-QA humano puro).
- Cola autónoma al cierre del día: **VACÍA** (sin sprints procesables autónomos pendientes).
- Pendientes humano-presenciales: SPRINT-100, SPRINT-112-QA, SPRINT-113 padre.
- Sin recurrencias de bugs históricos. Sistema estable.
- **Lección de la pasada 7:** el ciclo `BLOQUEOS → OK humano → procesa bloqueos → COMPLETADO` funcionó end-to-end por primera vez con un cambio a `firestore.rules`. P-005 + el helper `userData()` + reviewer auto con foco rules + sub-regla "rules sin deploy bloquea pre-commit" formaron una red de defensa que evitó repetir el bug de SPRINT-103/106. Lección estructural: las sub-reglas de CLAUDE.md acumuladas pagan dividendos cuando se respeta el orden del flujo sin saltarse pasos.

---

## Pasada 8 — Procesar `trabaja`: SPRINT-129 COMPLETADO (1/1) — auditoría asignaciones técnico↔operaria

**Trigger:** `trabaja` (Jorge — pasada 8 del día, después de que Cowork registró SPRINT-129 en cola tras reportar el bug puntual de Aury Mon sin operaria en producción).

**Procesado:**

- **SPRINT-129** — Auditoría sistémica read-only de asignaciones técnico↔operaria + huérfanos. Archivos nuevos:
  - `scripts/auditoria/asignaciones-tecnico-operaria.ts` (610 líneas) — Admin SDK + `service-account.json` + read-only enforced. Audita 6 tipos de inconsistencia: `TECNICO_SIN_OPERARIA`, `HUERFANO_TECNICO`, `OPERARIA_HUERFANA`, `ORDEN_SIN_OPERARIA_DESINCRONIZADA`, `ORDEN_OPERARIA_DESACTUALIZADA`, `RESPONSABLE_HUERFANO`. Sample N=500 órdenes activas más recientes. Privacidad: solo primer nombre + ID parcial 12 chars. Output stdout + reescribe `docs/sprints/AUDITORIA_ASIGNACIONES_<YYYY-MM-DD>.md` con reporte completo (resumen ejecutivo, tabla por tipo, sección "Cómo arreglar manualmente" UI, sección "Si querés fix masivo" con propuesta condicional de SPRINT-130 `--apply`).
  - `docs/sprints/AUDITORIA_ASIGNACIONES_2026-05-10.md` (placeholder ~80 líneas) — explica cómo correrlo, qué hacer después, decisión arquitectural pendiente (snapshot vs reactivo en `useOrdenCreateForm.ts`/`OrdenEditForm.tsx`).

**Flujo ejecutado:**
1. **archivist PRE-CHANGE (auto-rol coordinator):** categoría "datos en prod" + scripts standalone. Historial revisado: SPRINT-112 (`6aae2e5`) y SPRINT-117 (`ac54662`) como precedentes de patrón. Convención de IDs verificada por grep antes de codear: `personal.operariaId` = doc id (NO uid), confirmado en `PersonalPage.tsx:1204-1207` y `nomina.service.ts:172`.
2. **Builder:** archivos creados desde cero. Cero archivos de la app modificados.
3. **Tester:** `npm run build` PASS, `npx eslint` PASS para archivo nuevo (0 errors), `npm run check:regression` 7/7 PASS. P-008 activo via `npm run audit:notis-legacy` (no corre en pre-commit por diseño — requiere Admin SDK).
4. **Read-only enforcement check (criterio explícito):** grep negativo, sin `.set/.update/.delete` sobre Firestore.
5. **regression_guardian:** NO INVOCADO — no toca rules, services ni context. Solo scripts/ standalone.
6. **reviewer (auto-rol):** APPROVED. 8 criterios validados (read-only, no toca hooks de creación/edición, convención reusada, privacidad, fallback de índice, decisión arquitectural documentada, limitaciones documentadas, no se corre contra prod desde coordinator).
7. **Commit + push:** pre-commit hook PASS sin bypass.
8. **Devops:** no aplica — solo `scripts/` + `docs/sprints/`, no afecta bundle Vite ni rules.

**Decisión arquitectural pendiente documentada (no resuelta):** la causa raíz del bug puntual es derivación **snapshot** en `useOrdenCreateForm.ts:588-590` y `OrdenEditForm.tsx:72-77`. Cambiar a comportamiento **reactivo** elimina la clase entera de bug pero requiere decidir si las órdenes históricas deben actualizarse retroactivamente cuando el técnico cambia de operaria. Scope de sprint propio con input de Jorge — NO es parte de SPRINT-129.

**Próximo paso para Jorge:** correr `npx tsx scripts/auditoria/asignaciones-tecnico-operaria.ts` en su Mac. El reporte se reescribe con datos reales. Si <5 órdenes desinc → fix manual UI. Si >20 → pedir a Cowork SPRINT-130 con scope acotado (script `--apply` por orden IDs, queda en BLOQUEOS.md esperando OK).

**NO se generó cazador nuevo P-XXX.** Este sprint no es hotfix de bug — es auditoría preventiva sobre datos en prod. La sub-regla "cada bug en producción → cazador" no aplica directo. Si el script encuentra patrón nuevo NO catalogado, ahí sí se evalúa P-XXX en sprint follow-up. El bug puntual (snapshot vs reactivo) podría cazarse como check de patrón en `useOrden*.ts` futuro, pero requiere decisión arquitectural primero.

**Bloqueos nuevos:** 0. SPRINT-112-QA sigue abierto.

**Tiempo estimado:** ~18 minutos.

## Métricas pasada 8
- Commits pusheados: 1 (commit del sprint — ver `git log --grep SPRINT-129`).
- Minutos del coordinator: ~18.
- Hotfixes follow-up disparados por bugs introducidos: 0.
- Cazadores activos al cierre: 8 (P-001..P-008, sin cambios).
- Sub-sprints humano nuevos en BLOQUEOS.md: 0.

---

## Resumen del día (8 pasadas, total — final actualizado)
- Sprints procesados: 13 (pasadas 1-7) + 1 (pasada 8) = **14 sprints**.
- Commits de código/docs pusheados: ~12.
- Bloqueos abiertos al cierre: 1 (SPRINT-112-QA humano puro).
- Cola autónoma al cierre del día: **VACÍA** (sin sprints procesables autónomos pendientes).
- Pendientes humano-presenciales: SPRINT-100, SPRINT-112-QA, SPRINT-113 padre.
- Pendientes Jorge-acción (no humano-presencial pero requiere su decisión): correr script SPRINT-129 + leer reporte para decidir si pide SPRINT-130 fix masivo; decisión arquitectural snapshot-vs-reactivo en derivación de operaria.
- Sin recurrencias de bugs históricos. Sistema estable.
- **Lección de la pasada 8:** primer sprint de auditoría puramente preventiva (no fix de bug, no hotfix, no deuda priorizada). El patrón "Cowork ve bug puntual en prod → audita el vector amplio antes de proponer fix" replica con éxito lo aprendido en SPRINT-117/118 (emails personal/usuarios + tecnicoId migration). El script queda commiteado para re-correr más adelante — no es one-shot. Es la primera instancia donde la output del sprint NO es una mutación al sistema, sino una herramienta para decisión humana acotada.

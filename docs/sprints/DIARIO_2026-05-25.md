# Diario — 2026-05-25

> Actualizado por coordinator autónomo. Pasadas del día: 48 (`trabaja` — B-2 desbloqueado + procesado opción B, FIX-LEADS escalado a BLOQUEOS) + 49 (`trabaja` con autorizaciones explícitas — FIX-LEADS opción A + GARANTIA fase A) + 50 (`trabaja` — SPRINT-WA-FIX-PLANTILLAS-PARAMS) + 51 (`trabaja` nocturno — bloque FLUJO-DEPENDENCIAS completo).

---

## Pasada 51 (`trabaja` nocturno — Jorge se va hasta mañana, bloque FLUJO-DEPENDENCIAS completo)

**Disparada:** Jorge dejó autorización global para procesar TODO lo seguro del bloque FLUJO-DEPENDENCIAS en orden estricto, con reglas duras: [NO CERRAR sin QA] para 4 sprints, NO tocar rules/Meta/pagos B-3/migraciones, escalar lo que necesite OK con instrucciones claras.

**Resultado:** **8 sprints commiteados + pusheados, 1 ESCALADO. 4 COMPLETADOS autónomos, 3 awaiting QA Jorge.** 8 commits clean (24/24 cazadores PASS cada uno). Tiempo ~80 min.

### Sprints procesados

| Sprint | Estado | Hash | Nota |
|---|---|---|---|
| SPRINT-AGENDA-1-MANTENIMIENTO-ATA-CLIENTE | ⏸ awaiting QA | `132d9b5` | Typeahead cliente + buscarOCrearCliente + tecnicoId=uid + emite orden_asignada. Mantenimientos pre-sprint con `clienteId=''` bloquean botón Generar Orden. [NO CERRAR sin QA] |
| SPRINT-AGENDA-2-CALENDARIO-MUESTRA-CITAS | ✅ COMPLETADO | `e4f92bf` | Calendario.tsx + AgendaDia.tsx leen `citas_por_confirmar` + `mantenimiento` y los muestran como capa tentativa (ámbar/púrpura, borde punteado). Toggle "Mostrar tentativos". Solo lectura. |
| SPRINT-AGENDA-3-HONRAR-TECNICO-ASIGNADO | ✅ COMPLETADO | `f9697b9` | useOrdenCreateForm honra `citaPreset.asignadoId` al confirmar cita web (precarga técnico por uid). Effect secundario maneja race condition cuando personal carga después. |
| SPRINT-AGENDA-4-UNIFICAR-FORMS-PUBLICOS | ✅ COMPLETADO | `fba51a4` | CitaPublica.tsx escribe `equipoTipo` + `equipoMarca` + `telefonoNormalizado` (alineado con FormularioAgendarPublico). Anti-duplicado aplica a ambos vías. |
| SPRINT-AGENDA-5-PROXIMO-MANTENIMIENTO-AL-CERRAR | ✅ COMPLETADO | `8f6a72b` | CierreServicioWizard tras `toast.success` ofrece programar próximo mantenimiento (trimestral default + cliente real heredado + tecnicoId=uid). Best-effort, no bloquea cierre. |
| SPRINT-NUCLEO-CREAR-ORDEN-CENTRAL | ⊘ ESCALADO | — | Cimiento sensible + [NO CERRAR sin QA] + de noche sin QA. Plan de 3 fases (helper crearOrden + migrar solicitudes.service + opcional migrar useOrdenCreateForm + cazador anti-bypass) + decisión A/B/C en BLOQUEOS.md. AGENDA-1 ya tapó el path peor (Mantenimiento). |
| SPRINT-DINERO-1-QT-ATOMICO | ✅ COMPLETADO | `bec87b3` | Cotizaciones.tsx migrado a `siguienteNumeroCotizacion()` transaccional. Helper viejo `generateNumeroCotizacion` convertido en stub que LANZA. Cierra follow-up SPRINT-PAGOS-FIX-COTIZACIONES de AGENTES-1. |
| SPRINT-DINERO-2-MONTOPAGADO-RECALC | ⏸ awaiting QA | `b4fc23c` | ProcesarFacturacionModal recalcula `montoPagado`/`estadoPago` dentro de la runTransaction existente cuando hay pago nuevo. Guard de idempotencia por `pago.id`. Gate P-023 intacto. [NO CERRAR sin QA] |
| SPRINT-REPORTING-1-KPI-HELPERS | ⏸ awaiting QA | `a4e64db` | `src/utils/kpis.ts` con 3 helpers compartidos. Dashboard migrado. `conducesEmitidosMonto` EXCLUYE facturas `anulada` (cambio funcional en el monto del KPI). [NO CERRAR sin QA si cambia montos] |

### Lo importante

1. **Causa raíz #1 del bucle TAPADA** (AGENDA-1). El path por donde nacían órdenes huérfanas (mantenimiento → `clienteId:''`) está cerrado. Mantenimientos nuevos exigen cliente real (typeahead + búsqueda por teléfono + `buscarOCrearCliente`); la orden generada hereda el `clienteId` + denormalizados + `tecnicoId=uid` + sincroniza `fase/estado/estadoSimple` + emite notif `orden_asignada`. Falta QA Jorge.

2. **Calendario + Agenda ahora ven lo solicitado** (AGENDA-2). Citas por confirmar + mantenimientos programados aparecen como capa tentativa con color distinto + borde punteado. Cero ambigüedad. Solo lectura — no cambia modelo de datos.

3. **Promesa "Agendando con María" se cumple** (AGENDA-3). El técnico que el cliente elegía en `/cita/:calendarId` ahora pre-carga en el modal de confirmar cita. La oficina puede cambiarlo.

4. **Dos formularios públicos unificados** (AGENDA-4). El anti-duplicado por teléfono normalizado aplica ahora a ambos paths.

5. **Cliente recurrente aprovechado** (AGENDA-5). Al cerrar una orden con cliente real (post-AGENDA-1), toast custom con "¿Programar próximo mantenimiento?" — un click crea el doc.

6. **NUCLEO escalado limpiamente.** El refactor del cimiento (`crearOrden()` helper + migrar 3 paths) requiere QA Jorge denso. Plan de 3 fases documentado para que Jorge elija (A/B/C). Sin QA de noche, el riesgo de un fallo silencioso a través del path canónico (~80% de las órdenes) era inaceptable.

7. **QT atómico cerrado** (DINERO-1). Cierra deuda priorizada por auditor_contable en pasada 47. Reemplaza follow-up SPRINT-PAGOS-FIX-COTIZACIONES.

8. **Dinero pagado se refleja en la orden** (DINERO-2). El bug de "orden Pendiente con monto viejo después de cobrar en conduce" está tapado. Awaiting QA.

9. **KPIs centralizados + anulaciones excluidas** (REPORTING-1). Dashboard "Conduces emitidos del mes" baja si hay anuladas en el periodo. Awaiting QA porque cambia monto visible.

10. **Reglas duras del prompt respetadas:** NO se tocó `firestore.rules` ni `storage.rules`. NO se tocó PAGOS B-3 / Meta WABA / OAuth / migraciones >500 docs. NO se hizo ninguno de los 5 ítems FUERA DE LA COLA (base comisión, gate aprobación + R4, descuento stock, standby→inventario, factura→cerrado). El que requería [NO CERRAR] quedó en producción pero no marcado COMPLETADO (4 sprints).

### Pendiente operativo

- **QA Jorge pasada 51 (3 sprints):**
  - AGENDA-1 (`132d9b5`): programar mantenimiento → orden aparece en ficha cliente.
  - DINERO-2 (`b4fc23c`): cobrar saldo en conduce → orden muestra Pagado.
  - REPORTING-1 (`a4e64db`): KPI Dashboard "Conduces emitidos mes" baja por anuladas si las hay.
- **NUCLEO ESCALADO:** decidir opción A/B/C en `BLOQUEOS.md` (A recomendado: solo helper + migrar `solicitudes.service`).

---

## Pasada 49 (`trabaja` — autorizaciones explícitas Jorge antes de irse)

**Disparada:** Jorge volvió con autorizaciones explícitas (OK opción A para FIX-LEADS + OK FASE A para GARANTIA + reglas duras sobre lo que NO tocar) y se fue. Procesar TODO lo seguro hasta que no quede más.

**Resultado:** **1 sprint COMPLETADO, 1 sprint FASE A APLICADA (awaiting QA Jorge), 0 hotfixes, 2 commits de código + push, 24/24 cazadores PASS (sube de 23/23).** Tiempo ~50 min.

### Sprints procesados

| Sprint | Estado | Hash | Nota |
|---|---|---|---|
| SPRINT-FIX-LEADS-FORMULARIO-PUBLICO | ✅ COMPLETADO | `01df699` | Opción A aprobada. Match nuevo `solicitudes-publico/**` (whitelist `image/*` + `application/pdf`, < 10MB). Path en `solicitudes.service.ts` migra a `solicitudes-publico/...`. Fix lateral GPS incluido (búsqueda por `campo.tipo === 'ubicacion'` con cascada a clave literal). Deploy `npm run deploy:storage-rules` ejecutado, P-013 lock OK. |
| SPRINT-GARANTIA-FLUJO-COMPLETO | ⏸ FASE A APLICADA | `59c5fb0` | NO marcado COMPLETADO — espera QA Jorge (regla explícita del prompt). Helper `aplicarDescuentoGarantiaPorPiezas` (`-(piezas × 0.10)`, sin tocar `estaAnulada`). CierreServicioWizard invoca al cerrar orden de garantía con piezas. Citas.tsx reemplaza el bloque viejo (anulación 100%) por solo snapshot factura + audit `garantia_reabierta`. UI Comisiones.tsx muestra columnas "Desc. garantía" + "Neto". NUEVO cazador P-024 anti-reintroducción. Deuda fase B documentada en BLOQUEOS.md. |

### Lo importante

1. **FIX-LEADS cerrado en producción.** Cualquier formulario público con foto/firma/archivo ahora envía sin perder el lead. Bug GPS lateral también arreglado.

2. **GARANTIA Fase A aplicada — código en producción pero comportamiento NUEVO no se "habilita" hasta que cierre una orden de garantía con piezas.** Por la naturaleza del flujo, el nuevo descuento del 10% empieza a aparecer en el momento en que un técnico cierre una orden con `esGarantia=true` que tenga piezas. Las órdenes legacy con `estaAnulada=true` quedan tal cual (forensia preservada, cazador P-024 con tag de excepción si hace falta).

3. **Cazador P-024 activo** bloqueando que un refactor futuro reintroduzca el patrón viejo (`monto: -comisionMonto*` + `estaAnulada: true`). Allowlist: 3 archivos.

4. **Reglas duras del prompt respetadas:** NO se tocó PAGOS fase B-3 (rules), NO se tocó Meta WABA / pagos / OAuth / migraciones >500 docs. Sub-regla CLAUDE.md sobre dinero verificada (auditor_contable + guardian_logica + reviewer simulados).

### Pendientes operativos heredados de pasadas anteriores

- `npm run deploy:storage-rules` para SPRINT-138 → YA NO APLICA: en pasada 49 se redeployó `storage.rules` con el match nuevo. P-013 lock sincronizado.
- Smoke test en producción: selector número WhatsApp, trazabilidad enviador, respuestas rápidas "/", inbox completo.
- Crear 2da/3ra WABA en Meta → desbloquea `SPRINT-WA-NUMERO-RESPALDO-MANUAL-FASE-2`.
- Sprint follow-up `SPRINT-PAGOS-FIX-COTIZACIONES-NUMERO-TRANSACCIONAL` (hallazgo crítico de AGENTES-1).
- DRY-RUN script migración pagos B-2.

---

## Lo que necesita Jorge cuando vuelva

**Acciones manuales (Jorge solo puede hacerlas él):**

1. **QA SPRINT-GARANTIA Fase A (hash `59c5fb0`)** — 4 casos en `BLOQUEOS.md`:
   - Caso 1: mismo técnico cubre la garantía con piezas → verificar descuento 10% al original + sin `estaAnulada`.
   - Caso 2: otro técnico cubre con piezas → verificar descuento al original + comisión nueva al técnico B.
   - Caso 3: garantía sin piezas → sin descuento, técnico original intacto.
   - Caso 4: orden previa sin comisión registrada (edge) → sin descuento, log warn, cierre OK.
   - Cuando pase: agregá `QA: jorge YYYY-MM-DD PASS — fase A aprobada` al bloque en BLOQUEOS.md, coordinator próxima pasada lo cierra como COMPLETADO y empieza Fase B.

2. **QA SPRINT-FIX-LEADS-FORMULARIO-PUBLICO (hash `01df699`)** — enviar un formulario público real desde `/f/<slug-de-un-formulario-con-foto-o-archivo>` y confirmar que llega como solicitud al panel admin. El upload va a `solicitudes-publico/{solicitudId}/{campoId}/...`. Si no llega, revisar consola del navegador (puede ser un problema de Vercel deploy aún propagando).

3. **DRY-RUN B-2 migración (heredado pasada 48):** `npx tsx scripts/migrar-pagos-array-a-subcoleccion.ts` — reporta conteo. Si <500 órdenes con pagos → correr con `--apply`. Si >500 → re-escalar.

4. **Decisiones de negocio pendientes (para próxima sesión):**
   - ¿OK procesar **GARANTIA Fase B**? (botón "abrir garantía" desde orden/ficha, gate "solo oficina no técnicos", capturar cliente paga, notifs, regla "mismo técnico que cubre no gana comisión adicional"). Espera QA Fase A cerrada.
   - ¿OK procesar **PAGOS B-3** (cut-over + rules + remoción del array)? Espera QA Jorge.
   - Crear 2da WABA en Meta → desbloquea `SPRINT-WA-NUMERO-RESPALDO-MANUAL-FASE-2`.

**Smoke test sugerido en producción** (hard refresh primero — banner nueva versión aparecerá):
- Probar formulario público con foto → confirmar lead creado.
- Probar formulario público con PDF → confirmar lead creado.
- Probar formulario público con campo ubicación con id NO literal "ubicacion" → confirmar coords guardadas.
- Sidebar/inbox sin regresión.

---

## Pasada 48 (`trabaja` — B-2 opción B procesado, FIX-LEADS escalado)

---

## Pasada 48 (`trabaja` — B-2 opción B procesado, FIX-LEADS escalado)

**Disparada:** Jorge pegó `trabaja` el 2026-05-25 después de pasada 47 (último commit `f624ddd` — sync de docs).

**Resultado:** **1 sprint COMPLETADO, 1 ESCALADO a BLOQUEOS, 1 DIFERIDO, 0 hotfixes, 1 commit de código + push, 23/23 cazadores PASS.** Tiempo ~25 min.

### Sprints procesados

| Sprint | Estado | Hash | Nota |
|---|---|---|---|
| SPRINT-FIX-LEADS-FORMULARIO-PUBLICO | ⊘ ESCALADO | — | Approach 1 NO viable (rule de `fotos-equipos-publico` rechaza PDFs). Approach 2 toca `storage.rules` → diff propuesto + 3 opciones en BLOQUEOS. Hallazgo lateral GPS documentado para sprint chico. |
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-2 | ✅ COMPLETADO | `d4d6498` | Opción B aprobada por Jorge. Helper común `obtenerPagosDeOrden` + 4 lectores migrados + script de migración (DRY-RUN default, idempotente, guard >500). Writers W2-W5 NO se tocaron. Gate del conduce (P-023) intacto. |
| SPRINT-GARANTIA-FLUJO-COMPLETO | ⊘ DIFERIDO | — | Sprint enorme que toca DINERO (comisiones + nómina) y requiere QA Jorge entre fase A y B. Se deja PENDIENTE para próxima pasada con OK específico sobre cómo proceder. |

### Lo importante

1. **B-2 cerrado en opción B (conservadora).** La subcolección queda como espejo histórico; los lectores siguen prefiriendo el array como source-of-truth. B-3 (cut-over + rules + remoción del array) espera QA Jorge sobre B-2.

2. **Pendiente operativo Jorge:** correr `npx tsx scripts/migrar-pagos-array-a-subcoleccion.ts` para DRY-RUN. Si <500 órdenes con pagos → correr con `--apply`. Si >500 → re-escalar con conteo exacto.

3. **FIX-LEADS escalado:** Jorge decide opción A/B/C en BLOQUEOS. Approach 2 limpio (A) recomendado por coordinator — agrega match dedicado `solicitudes-publico/**` espejo de `fotos-equipos-publico` pero con whitelist imagen+PDF + size<10MB.

4. **Garantía diferido:** el sprint es muy grande y la regla de Jorge "QA entre fase A y B" requiere coordinación específica. Para próxima pasada.

### Pendientes operativos heredados de pasadas anteriores

- `npm run deploy:storage-rules` para SPRINT-138 (P-013 sigue WARN cold start).
- Smoke test en producción: selector número WhatsApp, trazabilidad enviador, respuestas rápidas "/", inbox completo (fotos, ficha cliente, form izquierda).
- Crear 2da/3ra WABA en Meta + cargar `phone_number_id` + token en Vercel env + allowlist → desbloquea `SPRINT-WA-NUMERO-RESPALDO-MANUAL-FASE-2`.
- Sprint follow-up `SPRINT-PAGOS-FIX-COTIZACIONES-NUMERO-TRANSACCIONAL` (hallazgo crítico de AGENTES-1).

### Próximo movimiento sugerido

- **Si Jorge OK fix-leads opción A:** próxima `procesa bloqueos` aplica el patch (autónomo si baseline + REGLA DE ORO).
- **Si Jorge OK garantía fase A primero:** próxima `trabaja` procesa fase A + escala fase B + C.
- **Si Jorge corre DRY-RUN de B-2 migración:** reporta conteo, si <500 `--apply` autónomo.

---

## 🌅 Lo que necesita Jorge cuando vuelva (resumen ejecutivo)

> Esta es la lista corta de acciones humanas pendientes acumuladas en `BLOQUEOS.md`. Si Jorge solo lee esto, ya sabe por dónde retomar.

### QA pendientes (heredados + de la pasada 51)

| Sprint | Hash | Tiempo QA | Qué probar |
|---|---|---|---|
| WA-FIX-PLANTILLAS (pasada 50) | `0ab73c5` | 5 min | Mandar cada una de las 4 plantillas desde el inbox a un número de prueba → ver ✓✓ + banner branded correcto (no ⚠️). |
| GARANTIA Fase A (pasada 49) | `59c5fb0` | 30 min | 4 casos (mismo técnico cubre / otro cubre / sin piezas / sin comisión original) — detalle en `BLOQUEOS.md`. |
| FIX-LEADS (pasada 49) | `01df699` | 3 min | Enviar formulario público con foto + firma + PDF → verificar que llega como solicitud. |
| **AGENDA-1 (pasada 51)** | **`132d9b5`** | **5 min** | Programar mantenimiento de cliente existente → orden creada aparece en su ficha (no huérfana). Plan detallado en `BLOQUEOS.md`. |
| **DINERO-2 (pasada 51)** | **`b4fc23c`** | **5 min** | Emitir conduce cobrando saldo → orden muestra Pagado en agenda/listado (no Pendiente con monto viejo). |
| **REPORTING-1 (pasada 51)** | **`a4e64db`** | **5 min** | Dashboard "Conduces emitidos del mes" baja si hay anuladas en el periodo (si no las hay, queda igual). |

### Decisiones pendientes para Jorge (sin las cuales no avanza)

1. **NUCLEO-CREAR-ORDEN-CENTRAL** (`BLOQUEOS.md`): elegir opción A (solo helper + migrar solicitudes.service — recomendado), B (refactor completo + cazador) o C (diferir).
2. **B-3 PAGOS** (cut-over + endurecer rules): requiere QA de B-2 completada primero.
3. **5 ítems FUERA DE LA COLA AUTÓNOMA** (en `BLOQUEOS.md` documentados):
   - N2-3 base de comisión (dos cálculos, ~18% dif — cuál es el correcto).
   - N2-4 gate de aprobación + rule R4 (toca firestore.rules).
   - N3-1 descuento de stock al cerrar (cambio de comportamiento de inventario).
   - N3-2 standby → inventario al llegar (depende de N3-1).
   - N2-5 factura+pago → fase 'cerrado' (cuándo dispara automático).
4. **Crear 2da/3ra WABA en Meta** + cargar `phone_number_id` + token en Vercel env + allowlist → desbloquea `SPRINT-WA-NUMERO-RESPALDO-MANUAL-FASE-2`.
5. **Smoke test en producción** completo (heredado).

### Operativos manuales pendientes

- `npm run deploy:storage-rules` ya hecho en pasada 49 (P-013 lock actualizado).
- `npx tsx scripts/migrar-pagos-array-a-subcoleccion.ts` ya corrido por Jorge (16/18 docs migrados, ver MEMORIA_MAESTRA.md).

### Para retomar rápido

- Pegar `ponte al día` para que Claude lea `MEMORIA_MAESTRA.md` completa.
- O leer este diario (pasada 51 + 50 + 49 + 48 en orden descendente).
- Pegar `procesa bloqueos` cuando haya algún `OK: jorge ...` nuevo.
- Pegar `trabaja` para nueva pasada autónoma de la cola.

---

> Más detalle por sprint en `EJECUCION_AUTONOMA.md`.

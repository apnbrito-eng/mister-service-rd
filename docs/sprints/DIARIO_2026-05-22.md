# Diario — 2026-05-22

> Actualizado por coordinator autónomo. Pasadas del día: 36 (procesa bloqueos — storage.rules + INBOX-9) + 37 (trabaja — INBOX-8c + INBOX-10) + 38 (trabaja — INBOX-11 fix ficha + drawer izquierda) + 39 (trabaja — NO-OP, cola limpia) + 40 (trabaja — cierre formal bug stripUndefinedDeep + escalado WA-NUMERO) + 41 (procesa bloqueos — SPRINT-WA-NUMERO-RESPALDO-MANUAL Fase 1 desbloqueada + procesada).

---

## Pasada 41 (procesa bloqueos — SPRINT-WA-NUMERO-RESPALDO-MANUAL Fase 1)

**Disparada:** Jorge pegó `procesa bloqueos` tras editar `BLOQUEOS.md` con `OK: jorge 2026-05-22 22:53 opcion=A fase=1 deploy=auto` sobre el sprint escalado en pasada 40.

**Resultado:** **1 sprint COMPLETADO, 0 hotfixes, 1 commit + 1 push + deploy de rules + deploy Vercel Ready.** Cazadores 20/20 PASS post-commit (incluye P-005 limpio tras `npm run deploy:rules`).

### Sprints procesados (pasada 41)

| Sprint | Estado | Commit | Deploy Vercel | Deploy rules | Notas |
|---|---|---|---|---|---|
| SPRINT-WA-NUMERO-RESPALDO-MANUAL (Fase 1) | COMPLETADO | `c277fa1` | Ready 23:02:24Z | OK 23:00:08Z (sha 07251e9684be…) | Fase 2 follow-up bloqueado hasta WABA respaldo en Meta |

### Detalle SPRINT-WA-NUMERO-RESPALDO-MANUAL Fase 1 (commit `c277fa1`)

**Contexto del desbloqueo:** Jorge eligió **opción A** (Fase 1 sola + deploy:rules autónomo) en `BLOQUEOS.md`. Coordinator pasada 41:
1. Movió el sprint de BLOQUEOS a COLA como PENDIENTE → EN_EJECUCION.
2. Ejecutó archivist PRE-CHANGE inline (historial git de `send.ts`, `firestore.rules`, `Configuracion.tsx` + grep de patrones de rules de inmutabilidad).
3. Builder aplicó cambios. Tester (typecheck + cazadores + lint) PASS. Reviewer + regression_guardian inline sobre el diff: APPROVED.
4. `npm run deploy:rules` (parte del scope autorizado por Jorge) → lock actualizado pre-commit para que P-005 quede en 0 hits.
5. Commit + push + deploy Vercel Ready.

**Archivos (7):**

1. `firestore.rules` — agregados 2 matches específicos bajo el bloque `config`:
   - `match /config/whatsapp_envio { read: esStaff(); write: esAdmin(); }`
   - `match /config/whatsapp_numeros { read: esStaff(); write: esAdmin(); }`
   - **Importante**: Firestore evalúa ambos matches (genérico `/config/{docId}` + específico) al escribir. Como `esStaff` incluye admin, la intersección `esStaff && esAdmin == esAdmin`. Resultado efectivo: solo admin escribe.
   - Comentarios explican la intersección + scope del sprint.
2. `firestore.rules.deployed.lock` — `sha256: 07251e9684be…` + `deployedAt: 2026-05-22T23:00:08.650Z`.
3. NUEVO `src/services/configWhatsappEnvio.service.ts` (~175 LOC):
   - 2 docs: `config/whatsapp_envio` (`{ phoneNumberIdForzado: string|null, etiqueta?, actualizadoEn, actualizadoPor }`) + `config/whatsapp_numeros` (`{ numeros: [{ phoneNumberId, etiqueta }], … }`).
   - Parsers defensivos (filtran shape inválido, fallback a defaults).
   - `obtener*` (read-once), `suscribir*` (onSnapshot), `actualizar*` (setDoc merge + Timestamp.now() + actualizadoPor opcional).
   - Defaults seed: `{ phoneNumberId: '1226992440486630', etiqueta: 'Principal' }` + `{ phoneNumberId: '1151997541323577', etiqueta: 'Respaldo' }` (los 2 phone_number_ids actuales del WABA — verificados en `whatsapp_mensajes_inbox`).
4. `api/whatsapp/send.ts` (~líneas 753-815):
   - Cascada nueva: **override-param > config/whatsapp_envio.phoneNumberIdForzado > sticky conversación > env**.
   - `phoneNumberIdForzado === '' / null / ausente` → cae al sticky (modo automático intacto).
   - Fail-soft: si el `getDoc(config/whatsapp_envio)` lanza, warn log + cascada al sticky. Backwards-compat 100%.
   - Validación `obtenerPhoneNumberIdsAllowlist()` (defense-in-depth) intacta. Override no permite escapar la allowlist.
5. `src/pages/Configuracion.tsx`:
   - Nueva sección admin-only (gate `esSoloAdministrador` = `rol === 'administrador'`, NO coord — sincronizado con la rule para que la UI no permita un submit que retornaría permission-denied).
   - Selector dropdown: "Automático (sticky por conversación)" + N opciones cargadas del catálogo (default los 2 del seed).
   - Botón "Guardar" disabled si el dropdown matchea el valor remoto actual (evita writes sin cambio).
   - Estado actual + última actualización visibles abajo (lectura para coord/secretaria si la rule del genérico les diera read; gate de UI los esconde de todas formas).
   - Import nuevo: `MessageCircle` de lucide-react.
6. `docs/sprints/BLOQUEOS.md` — sprint movido de "BLOQUEADO" a "DESBLOQUEADO 2026-05-22 22:53 (OK: jorge opcion=A fase=1 deploy=auto)" + contexto histórico colapsado en `<details>` para forensia.
7. `docs/sprints/COLA_AUTONOMA.md` — entrada PENDIENTE creada → EN_EJECUCION → ✅ COMPLETADO con archivos + lock + deploy hashes; header actualizado.

**Tester:**
- Typecheck (`npx tsc --noEmit`): PASS.
- Cazadores (`npm run check:regression`): PASS 20/20 (P-005 OK tras deploy; P-013 también OK; P-020 nuevo del sprint anterior intacto).
- Lint (`npx eslint --max-warnings 0`): PASS sobre los archivos modificados.
- Build (`npm run build`): PASS 4.69s.

**Regression guardian (inline):** P-001/P-002/P-003 NO aplican (no write con `auth.uid` gate; rules nuevas no comparan campos old/new; service hace setDoc a un solo doc). Defense-in-depth allowlist intacta. Modo automático preservado (cascada cae al sticky cuando `forzado === null`).

**Reviewer (inline):** APPROVED. Rules: 2 matches específicos agregados sin tocar matches existentes, default deny preservado, comentarios explican la intersección. Endpoint: orden de cascada correcto, error handling defensivo. UI: gate sincronizado con rule (esSoloAdministrador, no coord), botón disabled si no hay cambio.

**Deploy:**
- `firestore.rules`: deployadas a producción 23:00:08 UTC (`mister-service-app-cloude`).
- Vercel: Ready `c277fa1` 23:02:24 UTC. Banner de nueva versión activo.

**Follow-up creado:** SPRINT-WA-NUMERO-RESPALDO-MANUAL-FASE-2 queda BLOQUEADO hasta que Jorge cree el 2º WABA en Meta + cargue `phone_number_id` + token en Vercel env + allowlist. Sin eso, la Fase 2 (token por número/WABA) no es funcional.

**Acción manual de Jorge (opcional, no bloqueante):** smoke test del selector en `/admin/configuracion` → elegir "Respaldo" → mandar un mensaje desde el inbox → verificar que sale por `1151997541323577`. Volver a "Automático" cuando termine la prueba.

---

## Pasada 40 (trabaja — cierre formal bug stripUndefinedDeep + escalado WA-NUMERO)

**Disparada:** Jorge pegó `trabaja` con 2 sprints nuevos al tope de COLA (Cowork los agregó hoy más temprano): `SPRINT-WA-STRIPUNDEFINED-POSTMORTEM-CAZADOR-LIMPIEZA` (cierre formal del bug `0baf8b7` con postmortem + cazador + backfill) y `SPRINT-WA-NUMERO-RESPALDO-MANUAL` (selector admin del número de envío WhatsApp, toca rules + endpoint público). 0 OKs nuevos en BLOQUEOS posteriores a `3f7b023`. Instrucción explícita: NO TOCAR PAGOS-FASE-B-2.

**Resultado:** **1 sprint COMPLETADO, 1 sprint ESCALADO a BLOQUEOS, 0 hotfixes, 1 commit + 1 push a main.** Cazadores 18/18 PASS post-commit (sube de 17 a 18 con P-020 nuevo).

### Sprints procesados (pasada 40)

| Sprint | Estado | Commit | Deploy | Bloqueo |
|---|---|---|---|---|
| SPRINT-WA-STRIPUNDEFINED-POSTMORTEM-CAZADOR-LIMPIEZA | COMPLETADO | `ad90de4` | Vercel pendiente | — |
| SPRINT-WA-NUMERO-RESPALDO-MANUAL | ESCALADO a BLOQUEOS | — | — | Toca `firestore.rules` + `api/whatsapp/send.ts` — esperando OK formal de Jorge (opciones A/B/C/D en BLOQUEOS.md) |

### Detalle SPRINT-WA-STRIPUNDEFINED (commit `ad90de4`)

**Contexto:** El fix `0baf8b7` (2026-05-22 17:58 -0400) corrigió el bug de `stripUndefinedDeep` (`api/_lib/whatsappWebhook.ts`) que recursaba sobre instancias de clase, corrompiendo `FieldValue.increment(1)` como `{operand:1}` y `FieldValue.serverTimestamp()`/`Date` como `{}` en `whatsapp_conversaciones`/`whatsapp_mensajes_inbox`/`whatsapp_mensajes_outbox`. Síntoma: 3 días de inbox con conversaciones que no subían al tope, badges no leídos sin actualizar, ventana 24h rota. Detectado por Cowork con Playwright + Admin SDK. Este sprint cierra el bug formalmente con los 3 artefactos obligatorios.

**Archivos (7):**

1. NUEVO `docs/postmortems/2026-05-22-stripundefineddeep-mangle-fieldvalue.md` — postmortem completo con:
   - Timeline 2026-05-19 push `142d4da` → 2026-05-22 reporte + fix.
   - Impacto: módulo WhatsApp Inbox cosmético roto ~3 días; 3 convs sin tráfico durante ese período quedaron con shape corrupto (las que sí recibieron mensajes nuevos se auto-sanaron). Pérdida de datos parcial (timestamps `{}`, contadores `{operand:N}`) — reconstruibles desde `whatsapp_mensajes_inbox`/`outbox`.
   - 5 porqués hasta causa raíz: (1) silent data corruption porque endpoint no crashea, (2) `typeof === 'object'` es verdadero para CUALQUIER objeto JS, (3) helper no distinguía mapas planos de instancias de clase, (4) frontend `src/utils/firestore.ts` tenía la regla incompleta + backend la reimplementó sin completarla, (5) **causa raíz:** sin cazador determinístico ni tsconfig extendido a `api/` ni tests E2E.
   - 4 acciones preventivas: (a) cazador P-020 ✓, (b) entrada P-020 en `PATRONES_REGRESION.md` ✓, (c) backfill ✓, (d) tsconfig extendido a `api/` — pendiente (heredado de postmortem `2026-05-19-wa-1-webhook-fieldvalue-import.md`).
   - Lecciones: "stripear undefined preservando todo lo demás" no es trivial; silent data corruption es peor que crashes; el conocimiento debe vivir en código no cabezas.

2. NUEVO `scripts/invariantes/check-helpers-limpieza-recursiva-firestore.ts` (P-020):
   - Walk de `api/` + `src/services/`, filtro por imports Firestore O ubicación `api/_lib/` (el bug original estaba en `whatsappWebhook.ts` que NO importa Firestore — es módulo puro).
   - Regex detecta declaraciones de función (`function`/`const ... =`/`export function`/`export const`) con generics opcionales `<T>`.
   - Balanceo de llaves extrae cuerpo.
   - Heurística "limpieza recursiva": cuerpo contiene `Object.entries`/`Object.keys` + se auto-referencia (recursión).
   - 3 señales válidas (al menos una): (a) guard `Object.getPrototypeOf(...)` + comparación con `Object.prototype`/null, (b) checks explícitos `instanceof Date && instanceof Timestamp`, (c) tag `// @safe-recursive-strip: <razón>` en preludio (allowlist por línea).
   - Allowlist por archivo vacía. Caza 2 candidatos en este repo: `stripUndefinedDeep` (PASS — guard `Object.getPrototypeOf` post-fix `0baf8b7`) y `serializarTimestamps` (PASS — tag allowlist agregado en este sprint).

3. `scripts/invariantes/run-all.ts` — registra `checkHelpersLimpiezaRecursivaFirestore`. Sube de 17 a 18 cazadores activos.

4. `docs/PATRONES_REGRESION.md` — entrada P-020 completa: bug original (`142d4da`), síntoma, causa raíz, regla, cazador, limitaciones, referencia al postmortem.

5. `api/_lib/iaTools.ts` (hallazgo lateral) — `serializarTimestamps` línea 552 detectado por P-020. Es read-only (convierte docs Firestore → JSON para output de IA tools, NUNCA recibe FieldValue). Marcado con tag `// @safe-recursive-strip` con razón documentada (5 líneas de comentario explicando: read-only, nunca recibe FieldValue, maneja Date + Timestamp via `toDate()`; si se reusara para write con sentinels, agregar guard). **NO se fixea más allá del tag** — sub-regla CLAUDE.md "Hallazgos laterales: documentar como deuda, NO fixear silenciosamente".

6. NUEVO `scripts/backfill-convs-corruptas-stripundefined.ts` — one-shot idempotente:
   - Para cada `wa_id` en `['5618096402','8292733505','8494580318']`: lee `whatsapp_mensajes_inbox`/`outbox` filtrando por `conversacionId == wa_id`, deriva `totalMensajesEntrantes`/`totalMensajesSalientes` (counts), `primeraInteraccion`/`ultimaActividad`/`updatedAt` (min/max de `timestampMeta`), `ultimoMensajeEntrante`/`ultimoMensajeSaliente` ({wamid, timestamp, preview, tipo}), `ventana24h.cierraEn` (último entrante + 24h, `abierta` recalculado vs `Date.now()`).
   - Escribe Timestamps planos vía Admin SDK con `set(..., {merge:true})` — NO usa FieldValue (no requiere el guard).
   - Flag `--dry-run` muestra diff sin escribir.
   - Preserva `noLeidos` si es number plain válido; sino resetea al count de entrantes (heurística conservadora).
   - Auth tri-mode estándar del repo (GOOGLE_APPLICATION_CREDENTIALS / service-account.json / env vars).
   - Backfill productivo queda pendiente de Jorge (opcional — las 3 convs se auto-sanan en su próximo mensaje tras `0baf8b7`).

7. `docs/sprints/COLA_AUTONOMA.md` — header actualizado + sprint marcado COMPLETADO con archivos detallados.

**Tester:** Typecheck PASS. Lint clean (`iaTools.ts` en .eslintignore por estar en `api/`, esperado). Cazadores 18/18 PASS:
- P-020 nuevo verde: caza `stripUndefinedDeep` (PASS por guard) + `serializarTimestamps` (PASS por allowlist).
- Verificación de cobertura: si se revirtiera `0baf8b7`, el cazador habría hecho FAIL — confirmé probando antes del fix del archivo.
- Resto 17 cazadores siguen PASS sin cambios.

**Reviewer (autoaudit):**
- Postmortem: timeline cubierto, 5 porqués hasta causa raíz estructural (no causa proximal), acciones preventivas concretas y trackeables.
- Cazador: heurística razonable (filtro por imports + ubicación api/_lib/), 3 vías de señal cubre los patrones reales, allowlist por tag previene falsos positivos legítimos, allowlist por archivo vacía con política "refactorear si >5".
- Sin regresión en archivos críticos (no toca Ordenes/TecnicoVista/Dashboard/etc).
- Sub-regla "sprint hotfix requiere postmortem antes de COMPLETADO": cumplida — postmortem creado, P-020 catalogado, cazador deployable en pre-commit.

### Detalle SPRINT-WA-NUMERO-RESPALDO-MANUAL (escalado a BLOQUEOS)

**Por qué se escaló:**
1. Toca `firestore.rules` — sprint requiere rules nuevas para `config/whatsapp_envio` + `config/whatsapp_numeros`. Sub-regla CLAUDE.md exige OK formal antes de modificar + `npm run deploy:rules` antes de cerrar COMPLETADO (P-005 enforce).
2. Toca `api/whatsapp/send.ts` — endpoint público de envíos WhatsApp (con costo). Sub-regla CLAUDE.md exige OK formal antes de tocarlo.
3. Aunque Cowork escribió "Jorge aprobó la dirección general" (manual + admin + Configuración), el OK formal con shape `OK: jorge YYYY-MM-DD HH:MM ...` NO está como entrada formal. Falta confirmar: ¿procesar Fase 1 sola? ¿esperar Fase 2 con WABA respaldo en Meta? ¿deploy:rules autónomo o manual?

**Opciones de desbloqueo (en BLOQUEOS.md):**
- A: `OK: jorge YYYY-MM-DD HH:MM opcion=A fase=1 deploy=auto` — Fase 1 + coordinator deploya rules auto (recomendada).
- B: `OK: jorge YYYY-MM-DD HH:MM opcion=B fase=1 deploy=manual` — Fase 1 + Jorge deploya rules.
- C: `OK: jorge YYYY-MM-DD HH:MM opcion=C` — esperar Fase 2 completa (Jorge crea 2º WABA en Meta + carga env).
- D: `RECHAZADO: jorge YYYY-MM-DD HH:MM <motivo>`.

### Cazadores anti-regresión

**18/18 ejecutándose** (sube de 17 a 18):
- P-001..P-007, P-009..P-019: PASS sin hits.
- **P-020 nuevo PASS** — escaneados 43 archivos, 2 candidatos detectados, 2 PASS (`stripUndefinedDeep` via guard, `serializarTimestamps` via allowlist).

### Estado del sistema post-pasada 40

- **Acciones pendientes de Jorge:**
  - **Opción A/B/C/D** para SPRINT-WA-NUMERO-RESPALDO-MANUAL en BLOQUEOS.md.
  - `npm run deploy:storage-rules` para SPRINT-138 (heredado pasada 36 — P-013 sigue WARN cold start hasta el deploy).
  - QA visual INBOX-11 / INBOX-10 / INBOX-8c en producción tras Vercel deploy.
  - QA PAGOS-FASE-B-1 antes de procesar B.2.
  - **OPCIONAL:** `npx tsx scripts/backfill-convs-corruptas-stripundefined.ts --dry-run` (luego sin flag) para sanear cosméticamente las 3 convs sin tráfico reciente. NO es urgente — el fix `0baf8b7` ya las auto-sana al próximo mensaje.

- **Bloqueos abiertos:**
  - **NUEVO:** SPRINT-WA-NUMERO-RESPALDO-MANUAL.
  - Persisten previos (PAGOS-FASE-B-2 esperando QA Jorge de B.1, sprints WA-* esperando decisiones Meta).

**Tiempo total pasada 40:** ~25 minutos (archivist PRE-CHANGE manual + 7 archivos nuevos/modificados + autoreviewer + commit + push + escalado WA-NUMERO + diario).

---

## Pasada 39 (trabaja — NO-OP, cola limpia)

**Disparada:** Jorge pegó `trabaja` tras commit `f7d0798` (cierre pasada 38).

**Resultado:** **0 sprints procesados, 0 commits, 0 escalados, 0 hotfixes.**

### Análisis de la cola

| Fuente | Estado | Nota |
|---|---|---|
| `COLA_AUTONOMA.md` tope (post pasada 38) | sin sprints PENDIENTES procesables | INBOX-11 ✅, INBOX-8c ✅, INBOX-10 ✅, FEED-UNIFICADO ✅, FUNNEL ✅, WA-TEMPLATE-METRICS ✅, INBOX-9 ✅, INBOX-8b ✅, PAGOS-B-1 ✅, INBOX-7 ✅, INBOX-8 ✅, PAGOS fase A ✅. Histórico abajo todo cerrado. |
| `BLOQUEOS.md` OKs nuevos post `f7d0798` | 0 | Todos los OKs registrados ya fueron procesados en pasadas 33/36 o están bloqueados explícitamente (PAGOS-FASE-B-2 espera QA Jorge — instrucción explícita de Jorge en este prompt: "NO TOCAR"). |
| Bloqueos abiertos sin OK | varios | WA-NOTIF-CREATE-RULE-FIX, PORTAL-1, VERCEL-PLAN-DECISION, WA-CHAT-* serie, etc. — todos esperando decisiones de negocio o Meta config. |

### Pendientes operativos heredados (sin acción del coordinator)

1. **Jorge:** `npm run deploy:storage-rules` (SPRINT-138 commiteado pasada 36 hash `a2cd146`; el lock file se actualiza tras deploy productivo). Hasta entonces P-013 sigue WARN cold start.
2. **Jorge:** QA visual de SPRINT-INBOX-11 en producción (Vercel pendiente del push de pasada 38).
3. **Jorge:** QA de SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-1 antes de habilitar B.2.

**Tiempo coordinator pasada 39:** ~5 minutos (lectura CLAUDE.md + protocolo + escaneo de cola/bloqueos + sync de docs).

---

## Pasada 38 (trabaja — 2 bugs UX cerrados en un solo sprint)

**Disparada:** Jorge pegó `trabaja` tras probar producción de las pasadas 36+37 y cazar 2 bugs UX. Cowork ya había escrito `SPRINT-INBOX-11-FIX-FICHA-Y-DRAWER` al tope de la cola con causa raíz verificada para ambos bugs + approach robusto para BUG 2 (drawer en flujo, NO `fixed`-right + padding hack). Sin OKs nuevos en BLOQUEOS posteriores al commit `2c87b0e` → solo 1 sprint procesable.

**Resultado:** **1 sprint COMPLETADO, 0 ESCALADOS, 0 hotfixes, 1 commit + 1 push a main.** Cazadores 17/17 PASS (P-013 WARN cold start esperado, sigue del SPRINT-138 pendiente de deploy de Jorge).

### Sprints procesados (pasada 38)

| Sprint | Estado | Commit | Deploy | Bloqueo |
|---|---|---|---|---|
| SPRINT-INBOX-11-FIX-FICHA-Y-DRAWER | COMPLETADO | `c8b81d5` | Vercel pendiente | — |

### Detalle (SPRINT-INBOX-11 — commit `c8b81d5`)

**Archivos** (3):
- `src/pages/Clientes.tsx` — BUG 1: deep-link `?id=<docId>`.
- `src/components/ordenes/OrdenCreateModal.tsx` — BUG 2: rama drawer en flujo.
- `src/pages/InboxConversacion.tsx` — BUG 2: drawer como primera columna flex izquierda.

**BUG 1 — "Ver ficha del cliente" abría el listado, no el cliente específico.**

Causa raíz: `CardCliente.tsx:121` navegaba a `/admin/clientes?id=${cliente.id}` pero `Clientes.tsx` no leía el query param. Fix:
- Agregado `useSearchParams` al import.
- Effect nuevo que lee `searchParams.get('id')` cuando `clientes.length > 0`.
- Guard `idAbiertoRef` impide re-disparar el setter si el usuario cierra el panel después.
- Si el id no existe en la lista (o cliente soft-deleted), se limpia el param con `setSearchParams(..., { replace: true })` para no quedar atrapado en loop.

Sin regresión del flujo de click manual (el setter solo dispara cuando `idQS` cambia y `idAbiertoRef.current !== idQS`).

**BUG 2 — El form de crear orden tapaba parte del chat y debía abrir a la izquierda.**

Causa raíz documentada por Cowork: el drawer era `fixed top-0 right-0 w-[60%/55%/50%]` (porcentajes del **viewport**) y el `<main>` reservaba `pr-[60%/55%/50%]` (porcentaje del **main**, que es menor que el viewport por la barra de navegación global ~250px). Mismatch → drawer fixed tapaba el borde derecho del chat. El padding hack de INBOX-8c era estructuralmente imposible de calzar.

Fix robusto (resuelve "izquierda" + "sin tapar" de una vez):
1. `OrdenCreateModal.tsx` rama `presentationMode==='drawer'`: quitado `fixed top-0 right-0 w-full md:w-[60%] lg:w-[55%] xl:w-[50%]`. Ahora `h-full w-full` (llena el contenedor que le da el padre). Borde `border-l` → `border-r` (drawer a la izquierda, contenido a la derecha).
2. `InboxConversacion.tsx`: cuando `showCreateModal===true`, se renderiza `<OrdenCreateModal>` DENTRO de la fila `<div className="flex flex-1 min-h-0">` como **primera** columna flex con `w-1/2 lg:w-[55%] xl:w-[50%]`. El `<main>` (chat) queda como hermana derecha con `flex-1`. Cero solapamiento a cualquier ancho.
3. QUITADO el hack `pr-[60%/55%/50%]` del `<main>` (ya obsoleto). Eliminado también el bloque viejo de `<OrdenCreateModal>` fuera del contenedor flex.
4. Col 1 (lista convs) y Col 2 (PanelCliente360) siguen ocultas cuando `showCreateModal===true` (preservado de INBOX-8c).

**Default `presentationMode='modal'` preservado.** El único caller que pasa `'drawer'` sigue siendo `InboxConversacion.tsx` línea 417. Otros 4 callers (`Ordenes.tsx`, `Citas.tsx`, `OrdenEditForm.tsx`, `FormularioAgendarPublico.tsx`) usan default → comportamiento modal centrado intacto → SIN regresión.

**QA:**
- Typecheck PASS.
- Cazadores 17/17 PASS (P-013 WARN cold start esperado).
- Lint clean.
- Reviewer inline (toca componente compartido `OrdenCreateModal`): aprobado — default rama `'modal'` no se tocó, sólo se reorganizó la rama `'drawer'`.

**Bloqueos abiertos:** 0 nuevos. Persisten los previos (PAGOS-FASE-B-2 en QA Jorge de B-1; sprints WA-* esperando decisiones de negocio).

**Tiempo total pasada 38:** ~12 minutos (archivist PRE-CHANGE + 3 edits + tester + reviewer + commit + push pendiente).

---

## Pasada 37 (trabaja — 2 sprints UX inbox completados en una pasada)

**Disparada:** Jorge pegó `trabaja` con orden estricto: SPRINT-INBOX-8c primero (fix drawer tapa chat — prerequisito), luego SPRINT-INBOX-10 (centro de mando del cliente / cliente 360). Notas explícitas de reuso: CardCliente, TimelineUnificadoOrden, EnviarFacturacionButton, obtenerOrdenesActivasPorTelefono, resolverPropuestaReprogramacion, patrón Clientes.tsx:143 para facturas. Reviewer obligatorio. NO bundling de commits.

**Resultado:** **2 sprints COMPLETADOS, 0 ESCALADOS, 0 hotfixes, 2 commits + 2 pushes a main.** Cazadores 17/17 PASS en cada commit (P-013 WARN cold start esperado por SPRINT-138 pendiente de deploy de Jorge).

### Sprints procesados (pasada 37)

| Sprint | Estado | Commit | Deploy | Bloqueo |
|---|---|---|---|---|
| SPRINT-INBOX-8c-FIX-DRAWER-TAPA-CHAT | COMPLETADO | `65522c5` | Vercel pendiente | — |
| SPRINT-INBOX-10-CLIENTE-360 | COMPLETADO | `a4b3873` | Vercel pendiente | — |

### Detalle por sprint

**SPRINT-INBOX-8c — drawer ya no tapa el chat al crear orden** (commit `65522c5`):

Bug confirmado por Cowork con Playwright: a anchos típicos de laptop (1280-1440px) el drawer del SPRINT-INBOX-8b (50688a1) tapaba el chat de WhatsApp. Solo se veía al lado a 1720px+. Causa: drawer `fixed right-0 md:w-[60%] lg:w-[55%] xl:w-[50%]` flotando z-40 pero el main (col 3) renderizaba ancho completo porque no sabía del drawer encima. A 1280px: col1 320 + col2 256 + drawer 768 = 1344 > 1280 → chat queda con ~50px usables.

- `src/pages/InboxConversacion.tsx`:
  - Col 1 (lista convs) y Col 2 (CardCliente) ahora `${showCreateModal ? 'hidden' : 'hidden md:flex / hidden lg:flex'}` → se ocultan al abrir el drawer.
  - Main (chat) recibe `transition-[padding] duration-150 ${showCreateModal ? 'md:pr-[60%] lg:pr-[55%] xl:pr-[50%]' : ''}` → matchea EXACTAMENTE los widths del drawer, reservando espacio a la derecha.
- Resultado: 1280px → chat 40%/drawer 60%, 1440 → 45/55, 1720 → 50/50. Chat siempre visible y legible.
- NO se tocó OrdenCreateModal.tsx ni `presentationMode='modal'` default → sin regresión en Ordenes/Citas/OrdenEditForm.
- Mobile (<md): drawer `w-full` cubre todo (comportamiento esperado).

**SPRINT-INBOX-10 — centro de mando del cliente (cliente 360)** (commit `a4b3873`):

Pedido verbatim de Jorge: "desde el panel de whatsapp osea desde la conversacion acceder a todo lo concerniente al cliente que se este atendiendo garantias ordenes crear orden todo lo concerniente incluso mandar a facturacion mientras en un lateral se ve la orden en curso y lo que el tecnico ha dicho osea el historial del cliente en base a todo lo pertinente a el incluso reagendar crear ordenes."

- NUEVO `src/components/inbox/PanelCliente360.tsx` (561 líneas) — wrapper con 5 tabs:
  - **Datos**: monta `CardCliente` INTACTO (sin cambio de API). Mantiene datos + órdenes activas + CTAs crear cliente/orden con `onCrearOrden` delegado.
  - **Órdenes**: activas + histórico (cerradas/canceladas). Cada orden activa muestra `EnviarFacturacionButton` (gateado internamente por tienePago && !facturada). Aviso ámbar cuando hay propuestas de reprogramación pendientes → click navega `/admin/reprogramaciones`. Click en orden navega `/admin/ordenes/{id}`.
  - **Garantías**: facturas (conduces) con `Factura.garantia: GarantiaInfo` denormalizada. Estado vigente/reclamada/expirada con colores. Días y vencimiento. Click navega al detalle de orden subyacente.
  - **Facturas**: `where('clienteId', '==', cliente.id)` sort client-side por fechaEmision desc — patrón `Clientes.tsx:143`. Sin índice compuesto.
  - **Historial**: monta `TimelineUnificadoOrden` (creado en SPRINT-FEED-UNIFICADO-ORDEN) con `variant='modal'` para la orden activa más reciente. Es el "historial del cliente" que pidió Jorge.
- `src/services/ordenes.service.ts` — NUEVO helper `obtenerTodasOrdenesPorTelefono` (variante de `obtenerOrdenesActivasPorTelefono` sin filtrar terminales). Búsqueda dual (telNorm + raw). Sort client-side por createdAt desc. Sin `orderBy` → P-015 PASS.
- `src/pages/InboxConversacion.tsx`:
  - Reemplazó `CardCliente` directo por `PanelCliente360`. Mismo `key` con refresh y mismo `onCrearOrden`.
  - Aside Col 2 ampliado: `w-64` → `w-72 xl:w-80` para acomodar tabs.
  - Header de conversación (Phone, etiquetas, asignada, ToggleBot) queda arriba fijo + PanelCliente360 ocupa el resto del aside con scroll interno.
  - Preserva SPRINT-INBOX-8c: aside se sigue ocultando con drawer abierto.
- Reusa: CardCliente, TimelineUnificadoOrden, EnviarFacturacionButton, buscarClientePorTelefono, query facturas by clienteId. **NO duplica lógica.**
- Restricciones respetadas: NO firestore.rules, NO endpoints api/, NO migra datos, NO índice compuesto.
- **Deuda follow-up (documentada, NO fixeada):** doble onSnapshot al mismo wa_id (InboxConversacion timeline + TimelineUnificadoOrden interno). No es regresión — son los mismos mensajes y solo lectura. Tentativo sprint SPRINT-INBOX-11-DEDUP-WA-LISTENERS si se confirma como costo real.

### Cazadores anti-regresión

**17/18 ejecutándose:**
- P-001..P-007, P-009..P-012, P-014..P-019: **PASS sin hits** en cada commit.
- **P-013 WARN cold start** (esperado): `storage.rules.deployed.lock` no existe todavía. Tras `npm run deploy:storage-rules` de Jorge (pendiente desde pasada 36), P-013 pasa a PASS.

### Estado del sistema post-pasada 37

- 2 sprints procesados completos en una pasada. Commits + push individuales por sprint (no bundling — Jorge lo pidió explícito).
- **Acciones pendientes de Jorge:**
  - `npm run deploy:storage-rules` + smoke test rules viejas (SPRINT-138, desde pasada 36).
  - Smoke test inbox flow completo post-Vercel deploy (INBOX-8c chat visible con drawer + INBOX-10 tabs cliente 360).
  - QA Jorge de PAGOS-FASE-B-1 antes de procesar B.2.
- Bloque nocturno 2026-05-21 sigue pendiente smoke en producción tras deploy Vercel.

### Tiempo

~38 minutos de coordinator. 2 sprints. PRE-CHANGE archivist manual + reviewer manual + commit + push por cada uno.

---

## Pasada 36 (procesa bloqueos — 2 desbloqueos procesados en cadena)

**Disparada:** Jorge pegó `procesa bloqueos` con OKs nuevos en `BLOQUEOS.md` para SPRINT-138 (storage.rules versionado con REGLA DE ORO: baseline EXACTO + agregar solo match WhatsApp) y SPRINT-INBOX-9-FOTOS-CHAT-ORDEN (opción A: endpoint media-proxy + persistir en Storage). Orden de procesamiento explícito: SPRINT-138 primero (prerequisito), luego INBOX-9.

**Resultado:** **2 sprints COMPLETADOS, 0 ESCALADO, 0 hotfixes, 2 commits + 1 push a main.** Cazadores 17/17 PASS en cada commit (P-013 WARN cold start, esperado en primer setup post-SPRINT-138 hasta deploy manual de Jorge).

### Sprints procesados (pasada 36)

| Sprint | Estado | Commit | Deploy | Acción pendiente Jorge |
|---|---|---|---|---|
| SPRINT-138 | COMPLETADO | `a2cd146` | Vercel pendiente | `npm run deploy:storage-rules` + smoke test |
| SPRINT-INBOX-9-FOTOS-CHAT-ORDEN | COMPLETADO | `dae93c2` | Vercel pendiente | Smoke test del flow después del deploy storage.rules |

### Detalle por sprint

**SPRINT-138 — storage.rules versionado** (commit `a2cd146`):

REGLA DE ORO Jorge 2026-05-22: versionar el baseline EXACTAMENTE como estaba en consola hoy + AGREGAR solo el match nuevo `whatsapp-media/**`. NO endurecer el comodín `{allPaths=**}` permisivo (ese comodín hace funcionar las fotos de cierre, firmas de cliente y demás subidas existentes).

- NUEVO `storage.rules` raíz — baseline preservado (paths `fotos-ponche/{uid}/`, `fotos-equipos-publico/{citaId}/`, comodín `{allPaths=**}`) + match nuevo `whatsapp-media/{waId}/{archivo}` (read auth + write auth + image MIME + <16MB).
- `firebase.json` — bloque `storage.rules`.
- `package.json` — script `deploy:storage-rules` (compone `firebase deploy --only storage:rules` + actualiza lock).
- NUEVO `scripts/invariantes/marcar-storage-rules-deployadas.ts` — escribe SHA-256 al lock post-deploy.
- NUEVO `scripts/invariantes/check-storage-rules-pendientes-deploy.ts` — **P-013**, espejo de P-005. Pre-commit WARN si lock no existe (cold start), FAIL si diff entre repo y lock.
- `scripts/invariantes/run-all.ts` — registra P-013.
- `docs/PATRONES_REGRESION.md` — entrada P-013 completa.
- `CLAUDE.md` — sub-regla obligatoria espejo: "sprints que tocan `storage.rules` deben deployar antes de cerrar COMPLETADO".

Coordinator NO ejecutó `npm run deploy:storage-rules` — es deploy productivo. Jorge lo ejecuta manual + smoke test (técnico sube foto cierre, operaria sube foto, cliente firma).

**SPRINT-INBOX-9-FOTOS-CHAT-ORDEN — adjuntar fotos del chat a orden** (commit `dae93c2`):

Procesado inmediatamente después de SPRINT-138 (prerequisito de path gateado). El endpoint funciona con o sin deploy de Jorge (Admin SDK ignora rules), pero las lecturas client-side de las URLs firmadas pueden fallar hasta que las rules nuevas estén activas en producción.

- NUEVO `api/whatsapp/media-proxy.ts` — endpoint serverless con:
  - Auth Bearer idToken + verifyIdToken + check rol staff oficina (administrador/coordinadora/secretaria/operaria).
  - Defense-in-depth: el caller declara `wa_id` y el endpoint valida que el mensaje persistido tenga ese mismo `wa_id` (previene wamid-fishing entre conversaciones).
  - Descarga de Meta Graph API: `GET /{mediaId}` con `Authorization: Bearer ${META_ACCESS_TOKEN}` → URL temporal → binary fetch.
  - Sube a Firebase Storage en `whatsapp-media/{wa_id}/{wamid}.{ext}` vía Admin SDK con `firebaseStorageDownloadTokens` metadata.
  - Genera URL firmada válida 7 días y la retorna.
  - Idempotencia natural: path determinístico por wamid+ext → si el archivo ya existe, reutiliza + solo regenera URL firmada.
  - Tags `@safe-meta-catch` en catches no-Meta para que P-019 no levante falsos positivos.
- `api/_lib/firebaseAdmin.ts` — agrega `getAdminStorage()` helper + `storageBucket` en `initializeApp` (default `{projectId}.firebasestorage.app` via env opcional `FIREBASE_STORAGE_BUCKET`).
- `src/components/inbox/MensajeBubble.tsx` — prop opcional `onAdjuntarAOrden(wamid)` + botón "Adjuntar a la orden" en burbujas image entrantes con mediaId + spinner loading local.
- `src/pages/InboxConversacion.tsx` — handler `handleAdjuntarFotoAOrden` que llama el endpoint con `currentUser.getIdToken()` (gotcha P-001 — usa `currentUser` del `useApp()`, no `userProfile.id`) y vuelca URL al form. Solo se renderiza el botón cuando `showCreateModal && waId`.
- `src/hooks/useOrdenCreateForm.ts` — agrega campo opcional `fotoEquipoUrl?: string` a `CreateFormState`. Submit prioriza `form.fotoEquipoUrl` sobre `citaPreset?.fotoEquipoUrl` (la más reciente gana). Compat: callers que no setean el campo siguen funcionando idéntico.

### Cazadores anti-regresión

**18/18 ejecutándose** (subió de 17 a 18 con P-013):

- P-001..P-007, P-009..P-012, P-014..P-019: **PASS sin hits**.
- **P-013 WARN cold start** (esperado): `storage.rules.deployed.lock` no existe todavía. Tras `npm run deploy:storage-rules` de Jorge, el lock se inicializa y P-013 pasa a PASS.

### Estado del sistema post-pasada

- **2 sprints requieren acción manual de Jorge para cierre completo:**
  - SPRINT-138: `npm run deploy:storage-rules` + smoke test rules viejas (técnico foto cierre, operaria foto, cliente firma).
  - SPRINT-INBOX-9: smoke test del flow completo después del deploy storage.rules (abrir inbox, conversación con imagen, drawer crear orden, click "Adjuntar a la orden", verificar imagen en orden creada).
- **PAGOS-FASE-B-2** sigue esperando QA Jorge de B.1.
- **Bloque nocturno 2026-05-21** (3 sprints) sigue pendiente de QA Jorge en producción cuando vea los deploys Vercel.
- Resto de BLOQUEOS sin OKs nuevos (WA-4..7, hardening notif, portal solicitud, etc.).

### Tiempo

~25 minutos de coordinator. 2 sprints (ambos con archivist PRE-CHANGE manual, reviewer manual, regression_guardian manual — no hay agentes Task disponibles en esta versión, coordinator hace los roles).

### Próximos pasos

1. **Jorge ejecuta `npm run deploy:storage-rules`** (deploy productivo) + smoke test rules viejas.
2. Jorge ejecuta smoke test INBOX-9: abrir conversación con imagen, drawer crear orden, "Adjuntar a la orden", verificar URL en orden creada.
3. Si todo OK → P-013 pasa a PASS automáticamente tras el deploy (el script `deploy:storage-rules` actualiza el lock).
4. Si rompe algo → revertir `git revert dae93c2 a2cd146` y reportar.
5. Sigue pendiente QA Jorge de PAGOS-FASE-B-1 antes de procesar B.2.

### Notas técnicas

- **Decisión de diseño** (SPRINT-INBOX-9): el form de orden tiene un único `fotoEquipoUrl` (string, no array). La spec del OK de Jorge dijo "sumar la URL al `fotos[]` del equipo en el form" — la interpretación más fiel a la realidad del schema fue agregar `fotoEquipoUrl` al `CreateFormState` y semánticamente "llenar el slot de foto del equipo". Si Jorge adjunta una segunda foto, la primera se reemplaza con toast informativo. Si en el futuro se quiere un array real `fotos[]`, es un sprint follow-up (cambio de schema en `OrdenServicio` + UI para mostrar múltiples).
- **Storage bucket convention**: `getAdminStorage()` usa `{projectId}.firebasestorage.app` por default. Si en Vercel está seteado `FIREBASE_STORAGE_BUCKET`, lo respeta. Sin cambios en producción para Vercel ya configurado.
- **Patrón espejo P-005 → P-013**: idéntico hash + lock + cazador + sub-regla CLAUDE.md. Si en el futuro hay un tercer set de rules (ej: realtime database), seguir el mismo patrón.

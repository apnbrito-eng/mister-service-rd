# Diario — 2026-05-22

> Actualizado por coordinator autónomo `procesa bloqueos` pasada 36 (primera pasada del día). Sin pasadas previas hoy.

---

## Pasada 36 (procesa bloqueos — 2 desbloqueos procesados en cadena)

**Disparada:** Jorge pegó `procesa bloqueos` con OKs nuevos en `BLOQUEOS.md` para SPRINT-138 (storage.rules versionado con REGLA DE ORO: baseline EXACTO + agregar solo match WhatsApp) y SPRINT-INBOX-9-FOTOS-CHAT-ORDEN (opción A: endpoint media-proxy + persistir en Storage). Orden de procesamiento explícito: SPRINT-138 primero (prerequisito), luego INBOX-9.

**Resultado:** **2 sprints COMPLETADOS, 0 ESCALADO, 0 hotfixes, 2 commits + 1 push a main.** Cazadores 17/17 PASS en cada commit (P-013 WARN cold start, esperado en primer setup post-SPRINT-138 hasta deploy manual de Jorge).

### Sprints procesados (pasada 36)

| Sprint | Estado | Commit | Deploy | Acción pendiente Jorge |
|---|---|---|---|---|
| SPRINT-138 | COMPLETADO | `a2cd146` | Vercel pendiente | `npm run deploy:storage-rules` + smoke test |
| SPRINT-INBOX-9-FOTOS-CHAT-ORDEN | COMPLETADO | `dae93c2` | Vercel pendiente | Smoke test del flow después del deploy storage.rules |

### Detalle por sprint

**SPRINT-138 — storage.rules versionado** (commit `a2cd146`):

REGLA DE ORO Jorge 2026-05-22: versionar el baseline EXACTAMENTE como estaba en consola hoy + AGREGAR solo el match nuevo `whatsapp-media/**`. NO endurecer el comodín `{allPaths=**}` permisivo (ese comodín hace funcionar las fotos de cierre, firmas de cliente y demás subidas existentes).

- NUEVO `storage.rules` raíz — baseline preservado (paths `fotos-ponche/{uid}/`, `fotos-equipos-publico/{citaId}/`, comodín `{allPaths=**}`) + match nuevo `whatsapp-media/{waId}/{archivo}` (read auth + write auth + image MIME + <16MB).
- `firebase.json` — bloque `storage.rules`.
- `package.json` — script `deploy:storage-rules` (compone `firebase deploy --only storage:rules` + actualiza lock).
- NUEVO `scripts/invariantes/marcar-storage-rules-deployadas.ts` — escribe SHA-256 al lock post-deploy.
- NUEVO `scripts/invariantes/check-storage-rules-pendientes-deploy.ts` — **P-013**, espejo de P-005. Pre-commit WARN si lock no existe (cold start), FAIL si diff entre repo y lock.
- `scripts/invariantes/run-all.ts` — registra P-013.
- `docs/PATRONES_REGRESION.md` — entrada P-013 completa.
- `CLAUDE.md` — sub-regla obligatoria espejo: "sprints que tocan `storage.rules` deben deployar antes de cerrar COMPLETADO".

Coordinator NO ejecutó `npm run deploy:storage-rules` — es deploy productivo. Jorge lo ejecuta manual + smoke test (técnico sube foto cierre, operaria sube foto, cliente firma).

**SPRINT-INBOX-9-FOTOS-CHAT-ORDEN — adjuntar fotos del chat a orden** (commit `dae93c2`):

Procesado inmediatamente después de SPRINT-138 (prerequisito de path gateado). El endpoint funciona con o sin deploy de Jorge (Admin SDK ignora rules), pero las lecturas client-side de las URLs firmadas pueden fallar hasta que las rules nuevas estén activas en producción.

- NUEVO `api/whatsapp/media-proxy.ts` — endpoint serverless con:
  - Auth Bearer idToken + verifyIdToken + check rol staff oficina (administrador/coordinadora/secretaria/operaria).
  - Defense-in-depth: el caller declara `wa_id` y el endpoint valida que el mensaje persistido tenga ese mismo `wa_id` (previene wamid-fishing entre conversaciones).
  - Descarga de Meta Graph API: `GET /{mediaId}` con `Authorization: Bearer ${META_ACCESS_TOKEN}` → URL temporal → binary fetch.
  - Sube a Firebase Storage en `whatsapp-media/{wa_id}/{wamid}.{ext}` vía Admin SDK con `firebaseStorageDownloadTokens` metadata.
  - Genera URL firmada válida 7 días y la retorna.
  - Idempotencia natural: path determinístico por wamid+ext → si el archivo ya existe, reutiliza + solo regenera URL firmada.
  - Tags `@safe-meta-catch` en catches no-Meta para que P-019 no levante falsos positivos.
- `api/_lib/firebaseAdmin.ts` — agrega `getAdminStorage()` helper + `storageBucket` en `initializeApp` (default `{projectId}.firebasestorage.app` via env opcional `FIREBASE_STORAGE_BUCKET`).
- `src/components/inbox/MensajeBubble.tsx` — prop opcional `onAdjuntarAOrden(wamid)` + botón "Adjuntar a la orden" en burbujas image entrantes con mediaId + spinner loading local.
- `src/pages/InboxConversacion.tsx` — handler `handleAdjuntarFotoAOrden` que llama el endpoint con `currentUser.getIdToken()` (gotcha P-001 — usa `currentUser` del `useApp()`, no `userProfile.id`) y vuelca URL al form. Solo se renderiza el botón cuando `showCreateModal && waId`.
- `src/hooks/useOrdenCreateForm.ts` — agrega campo opcional `fotoEquipoUrl?: string` a `CreateFormState`. Submit prioriza `form.fotoEquipoUrl` sobre `citaPreset?.fotoEquipoUrl` (la más reciente gana). Compat: callers que no setean el campo siguen funcionando idéntico.

### Cazadores anti-regresión

**18/18 ejecutándose** (subió de 17 a 18 con P-013):

- P-001..P-007, P-009..P-012, P-014..P-019: **PASS sin hits**.
- **P-013 WARN cold start** (esperado): `storage.rules.deployed.lock` no existe todavía. Tras `npm run deploy:storage-rules` de Jorge, el lock se inicializa y P-013 pasa a PASS.

### Estado del sistema post-pasada

- **2 sprints requieren acción manual de Jorge para cierre completo:**
  - SPRINT-138: `npm run deploy:storage-rules` + smoke test rules viejas (técnico foto cierre, operaria foto, cliente firma).
  - SPRINT-INBOX-9: smoke test del flow completo después del deploy storage.rules (abrir inbox, conversación con imagen, drawer crear orden, click "Adjuntar a la orden", verificar imagen en orden creada).
- **PAGOS-FASE-B-2** sigue esperando QA Jorge de B.1.
- **Bloque nocturno 2026-05-21** (3 sprints) sigue pendiente de QA Jorge en producción cuando vea los deploys Vercel.
- Resto de BLOQUEOS sin OKs nuevos (WA-4..7, hardening notif, portal solicitud, etc.).

### Tiempo

~25 minutos de coordinator. 2 sprints (ambos con archivist PRE-CHANGE manual, reviewer manual, regression_guardian manual — no hay agentes Task disponibles en esta versión, coordinator hace los roles).

### Próximos pasos

1. **Jorge ejecuta `npm run deploy:storage-rules`** (deploy productivo) + smoke test rules viejas.
2. Jorge ejecuta smoke test INBOX-9: abrir conversación con imagen, drawer crear orden, "Adjuntar a la orden", verificar URL en orden creada.
3. Si todo OK → P-013 pasa a PASS automáticamente tras el deploy (el script `deploy:storage-rules` actualiza el lock).
4. Si rompe algo → revertir `git revert dae93c2 a2cd146` y reportar.
5. Sigue pendiente QA Jorge de PAGOS-FASE-B-1 antes de procesar B.2.

### Notas técnicas

- **Decisión de diseño** (SPRINT-INBOX-9): el form de orden tiene un único `fotoEquipoUrl` (string, no array). La spec del OK de Jorge dijo "sumar la URL al `fotos[]` del equipo en el form" — la interpretación más fiel a la realidad del schema fue agregar `fotoEquipoUrl` al `CreateFormState` y semánticamente "llenar el slot de foto del equipo". Si Jorge adjunta una segunda foto, la primera se reemplaza con toast informativo. Si en el futuro se quiere un array real `fotos[]`, es un sprint follow-up (cambio de schema en `OrdenServicio` + UI para mostrar múltiples).
- **Storage bucket convention**: `getAdminStorage()` usa `{projectId}.firebasestorage.app` por default. Si en Vercel está seteado `FIREBASE_STORAGE_BUCKET`, lo respeta. Sin cambios en producción para Vercel ya configurado.
- **Patrón espejo P-005 → P-013**: idéntico hash + lock + cazador + sub-regla CLAUDE.md. Si en el futuro hay un tercer set de rules (ej: realtime database), seguir el mismo patrón.

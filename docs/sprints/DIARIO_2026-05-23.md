# Diario — 2026-05-23

> Actualizado por coordinator autónomo. Pasadas del día: 44 (`trabaja` nocturno — 0 sprints procesables, SPRINT-WA-SEGURIDAD-CONFIG-RULES escalado a BLOQUEOS por falta de Java/emulator) + 43 (`procesa bloqueos` — SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS COMPLETADO) + 42 (`trabaja` — SPRINT-WA-INBOX-UX-QUICKWINS COMPLETADO + SPRINT-WA-TRAZABILIDAD escalado a BLOQUEOS).

---

## Pasada 44 (`trabaja` nocturno — SEGURIDAD-CONFIG-RULES escalado, sin commits de código)

**Disparada:** Jorge pegó `trabaja` el 2026-05-23 noche con autorización explícita para SPRINT-WA-SEGURIDAD-CONFIG-RULES (`deploy=auto`, reviewer obligatorio, condición dura: probar emulator ANTES de deployar; si falla setup → escalar y CONTINUAR cola). Reglas adicionales: NO tocar PAGOS-FASE-B-2, NO crear WABA en Meta, NO integraciones de pago/OAuth/migraciones masivas, escalar y continuar sin parar la cola.

**Resultado:** **0 sprints COMPLETADOS, 1 ESCALADO a BLOQUEOS, 0 hotfixes, 1 commit de docs.** Cazadores 20/20 PASS de baseline (verificado pre y post — sin código tocado).

### Sprints procesados (pasada 44)

| Sprint | Estado | Commit | Deploy | Bloqueo | Notas |
|---|---|---|---|---|---|
| SPRINT-WA-SEGURIDAD-CONFIG-RULES | ⊘ ESCALADO a BLOQUEOS | — | — | Emulator no levanta (Java JRE ausente) | Bug confirmado por docs Firebase, fix propuesto en `BLOQUEOS.md` |

### Detalle SPRINT-WA-SEGURIDAD-CONFIG-RULES (escalado)

**Origen:** Cowork lo agregó al tope de la cola en pasada 43 como deuda follow-up que el propio coordinator había marcado al cerrar el sprint anterior (`d7b320b`). El sprint pedía verificar con Firebase Emulator si la semántica de múltiples matches superpuestos en `firestore.rules` es OR (creaba un hueco) o no.

**Pipeline ejecutado:**

1. **archivist PRE-CHANGE** (manual, equivalente):
   - 18 commits relevantes en `firestore.rules`. Hotfixes notables: `c7c8e34` (P-002 campos opcionales), `b7b6464` (P-002 recurrencia), `0ab1196` (C2 R4 gate aprobación oficina), `be0ef32` (defense-in-depth 6 colecciones WhatsApp), `c277fa1` (match específico `/config/whatsapp_envio` y `/whatsapp_numeros` agregado en Fase 1), `d7b320b` (match `/config/whatsapp_respuestas_rapidas` agregado en pasada 43).
   - 2 postmortems clase rules: `2026-05-07-iniciar-chequeo-rules-sin-deploy.md` (P-005), `2026-05-07-iniciar-chequeo-permission-denied.md` (P-002).
   - Categorías especiales: `firestore.rules` en touch-list → P-005 (deploy obligatorio) + reviewer obligatorio (sub-regla CLAUDE.md).
   - Recomendaciones: levantar emulator ANTES de tocar nada; auditar 6 consumidores del genérico `/config/{docId}` antes del fix.

2. **Diagnóstico (sin emulator — por documentación oficial Firebase):**
   - Firestore Rules evalúa **TODOS** los matches superpuestos con semántica **OR**. Si AL MENOS UNO permite, la operación pasa.
   - Las 3 rules específicas (`/config/whatsapp_envio` línea 583, `/config/whatsapp_numeros` línea 592, `/config/whatsapp_respuestas_rapidas` línea 605) declaran "intersección efectiva admin-only" en sus comentarios. **Eso es incorrecto.**
   - El genérico `/config/{docId}` línea 560 tiene `write: if esStaff()` → cualquier staff (operaria/secretaria/técnico/ayudante/coord/admin) puede escribir. El match específico con `esAdmin()` NO bloquea, GANA el OR.
   - Impacto teórico: cualquier staff puede mutar el `phoneNumberIdForzado` del envío saliente, el catálogo de números o el catálogo de respuestas rápidas mediante cliente custom (curl/script). La UI gatea por `esSoloAdministrador` así que el hueco es defense-in-depth roto, no regresión activa.

3. **Intento de emulator (condición dura del prompt):**
   - `firebase --version` → 15.16.0 ✓
   - `java -version` → "Unable to locate a Java Runtime" ✗
   - Sin Java JRE no se puede levantar `firebase emulators:start --only firestore`.
   - Instalar Java requiere `brew install --cask temurin@17` que NO está autorizado autónomamente (puede pedir password admin, modifica el sistema).

4. **Decisión:** escalar a `BLOQUEOS.md` con diff propuesto + audit de consumidores + razón. Cumple cita textual del prompt: *"Si NO podés probar limpio con emulator (...): NO DEPLOYES. Escalá a BLOQUEOS.md con el diff propuesto + razón + qué se necesita para destrabar, y CONTINUÁ con el siguiente sprint."*

5. **Fix propuesto (NO aplicado — vive solo en `BLOQUEOS.md`):** opción A — excluir explícitamente los 3 docId del genérico:
   ```diff
   allow write: if esStaff() &&
   +  docId != 'whatsapp_envio' &&
   +  docId != 'whatsapp_numeros' &&
   +  docId != 'whatsapp_respuestas_rapidas';
   ```
   Los 3 matches específicos quedan tal cual — ahora SÍ son la única regla de write. Read sigue staff (heredado del genérico, sin exclusión).

6. **Audit de consumidores del match genérico** (verificado con grep `'config'` en `src/`):
   - `contadores.service.ts` (writes desde transactions de OS/QT/FAC) → ✅ no afectado.
   - `configEmpresa.service.ts` → ✅ no afectado.
   - `configFiscal.service.ts` → ✅ no afectado.
   - `configTiposEquipo.service.ts` → ✅ no afectado.
   - `seedData.ts` + `seedPrecios.ts` (writes a `config/sistema` y `config/contadores`) → ✅ no afectado.
   - `configWhatsappEnvio.service.ts` + `whatsappRespuestasRapidas.service.ts` → ⚠️ bloqueados intencionalmente por exclusión + match específico admin.

7. **Continuación de cola:** verifiqué que NO hay otros sprints procesables PENDIENTE en la cola (los "PENDIENTE" restantes son plantillas de ejemplo o entries dentro de bloques `<details>` históricos ya superados — confirmado con `awk` que pairea `## SPRINT-` con `**Estado:**`). Fin de pasada.

### Archivos modificados (pasada 44 — solo docs)

- `docs/sprints/BLOQUEOS.md` — nueva entrada `SPRINT-WA-SEGURIDAD-CONFIG-RULES — BLOQUEADO 2026-05-23` al tope, con diagnóstico + diff propuesto + audit de consumidores + qué necesita Jorge para destrabar.
- `docs/sprints/COLA_AUTONOMA.md` — sprint marcado `⊘ MOVIDO A BLOQUEOS 2026-05-23 pasada 44` + header de "Última actualización" actualizado con resumen de pasada 44.
- `docs/sprints/EJECUCION_AUTONOMA.md` — entrada nueva al tope con todo el pipeline.
- `docs/sprints/DIARIO_2026-05-23.md` — esta sección (pasada 44 al tope, 42 y 43 preservadas).
- `COWORK_CONTEXTO.md` — sin cambios funcionales (modificado por Cowork antes; el coordinator solo lo incluye en el commit final por estar en working tree).

### Deuda registrada (pasada 44)

1. **SPRINT-WA-SEGURIDAD-CONFIG-RULES bloqueado** esperando OK de Jorge. Decisión recomendada: opción A (1 línea de cambio, mínimo blast radius). Diff completo en `BLOQUEOS.md`.
2. **Java JRE ausente en la máquina** — bloquea cualquier sprint futuro que requiera test con emulator. Sugerencia operativa para Jorge: `brew install --cask temurin@17` (~5 min, requiere password admin) → habilita `firebase emulators:start --only firestore` + tests con `@firebase/rules-unit-testing` para todos los sprints de rules en adelante. **Esto es un meta-sprint operativo, no de código.**
3. **NO se renombraron** los comentarios de las 3 rules específicas (`/config/whatsapp_envio` línea 578-580, `/config/whatsapp_numeros` línea 588, `/config/whatsapp_respuestas_rapidas` líneas 601-603) que declaran erróneamente "intersección efectiva admin-only" porque tocar comentarios sin tocar el fix sería confuso para futuros lectores. Quedará para el sprint del fix (cuando se aplique opción A, también se actualizan los comentarios).

### Estado anti-regresión post pasada 44

- Cazadores activos: **20/20 PASS** (P-001..P-007, P-009..P-012, P-014..P-020). Verificado al inicio de pasada (`npx tsx scripts/invariantes/run-all.ts` — 340ms, 0 hits).
- Postmortems: 11 archivos en `docs/postmortems/` (sin nuevos — esta pasada NO fue hotfix).
- Lock `firestore.rules.deployed.lock`: sha `cab27b049c5905b51bcbe5529f25a5a8d988440f6287a287897e302a5bdb7dd3` (2026-05-23T21:28:12Z, sin cambios). P-005 limpio.
- Lock `storage.rules.deployed.lock`: pendiente deploy productivo de Jorge (P-013 WARN cold start, no bloqueante).

### Lecciones / observaciones

- La sub-regla "PRE-CHANGE archivist" pagó dividendos inmediatos: el git log de `firestore.rules` mostró 18 commits con clase de bugs P-002/P-005 que son exactamente los gotcha que la condición dura del prompt buscaba prevenir. El postmortem del 2026-05-07 (`iniciar-chequeo-rules-sin-deploy.md`) fue el ancla para reconfirmar que rules sin deployar son catastróficos.
- La condición dura "no deployar sin emulator" es una **regla de proceso, no de código** — sin Java, no hay forma de cumplirla autónomamente. El protocolo correcto fue: escalar y NO improvisar (no inventar un mock de emulator en TypeScript con `@firebase/rules-unit-testing` que igualmente requiere binario Java debajo).
- El "audit de consumidores" del touch-list expandido sí se puede ejercer sin emulator: 6 services + 2 seed scripts auditados con `grep`, todos verificados no afectados por la exclusión. Eso baja el riesgo del fix a "muy bajo" cuando Jorge dé OK.
- **Cowork detecta gaps + coordinator los escala estructurados** → el ciclo de aprendizaje funciona aunque la ejecución autónoma falle por restricción del entorno.

## Lo que necesita Jorge cuando vuelva

- **Smoke tests pendientes (producción `https://www.misterservicerd.com/admin/`):**
  - Verificar pasada 43 — abrir `/admin/inbox` → seleccionar conversación con mensajes salientes → confirmar que el footer de las burbujas salientes muestra `Enviado por <nombre>` (o "Sistema"). Probar `/atajo` en composer (escribir `/` y verificar dropdown de respuestas rápidas; debe filtrar al tipear, ESC cerrar, click insertar).
  - Verificar pasada 43 — abrir `/admin/configuracion` con cuenta admin: ver el toggle "Anteponer nombre del agente al cliente (texto libre)" (default ON) y el editor de respuestas rápidas. Sin reportes de regresión en envíos.
  - Verificar pasada 42 — abrir `/admin/configuracion` con admin: ver selector de número con número humano destacado (ej. "+1 829-471-6265 · Principal · cód 1226…4486630"). Editor del catálogo con 3 inputs por item.
  - Verificar pasadas 38/37/36 — INBOX-11/INBOX-10/INBOX-8c/INBOX-9 visuales en producción.
- **QA PAGOS B.1 (`SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-1`)** — bloqueante para destrabar B.2. Página `/admin/pagos-pendientes` + helper `confirmarPagoOrden`. Sin avance posible en pagos hasta que cierre.
- **WABA respaldo en Meta (NO aplicable esta pasada)** — bloqueado por regla "NO crear WABA en Meta" del prompt nocturno. Sigue como entrada en BLOQUEOS para el día.
- **Decisión escalada en BLOQUEOS.md por esta pasada (44):**
  - **SPRINT-WA-SEGURIDAD-CONFIG-RULES** — Jorge agrega `OK: jorge YYYY-MM-DD HH:MM opcion=A` al final de la entrada para autorizar el fix (opción A — excluir 3 docId del genérico, 1 línea, riesgo muy bajo). Diff completo + audit ya redactados.
  - Opcional pero recomendado: ejecutar el test manual de verificación del bug en producción con cuenta QA `secretaria` (abrir consola browser, intentar `firebase.firestore().doc('config/whatsapp_envio').update({...})` — si pasa, bug confirmado; si rechaza, los comentarios eran correctos y el fix se reduce a actualizar texto explicativo).
- **Meta-decisión operativa (NO bloqueo, pero acelera futuros sprints de rules):** instalar Java en la máquina (`brew install --cask temurin@17`, ~5 min con password admin) para habilitar Firebase Emulator local. Sin Java, **cualquier sprint futuro que toque `firestore.rules` con condición dura "emulator antes de deployar" se va a escalar igual que este**.
- **Deploys manuales pendientes (heredados de pasadas anteriores, persisten):**
  - `npm run deploy:storage-rules` — para deployar `storage.rules` versionado (SPRINT-138 hash `a2cd146` del 2026-05-22). Sin esto el cazador P-013 sigue WARN cold start aunque no bloquee.
  - (Opcional) `npx tsx scripts/backfill-convs-corruptas-stripundefined.ts --dry-run` y luego sin flag — backfill cosmético de las 3 convs corruptas pre-`0baf8b7`. Las convs se auto-sanan en su próximo mensaje, no es urgente.
- **Bloqueos sin OK pendientes (sin acción de esta pasada, sigue tal cual):** WA-NUMERO-RESPALDO-FASE-2 (espera 2º WABA en Meta), WA-NOTIF-CREATE-RULE-FIX, PORTAL-1, VERCEL-PLAN-DECISION, WA-CHAT-*, PAGOS-FASE-B-2 (vetado explícitamente por Jorge).

---

## Pasada 43 (`procesa bloqueos` — SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS COMPLETADO)

**Disparada:** Jorge pegó `procesa bloqueos` el 2026-05-23 tarde tras agregar `OK: jorge 2026-05-23 12:27 opcion=1 nombreAgente=ON respuestasRapidas=admin deploy=auto` al sprint escalado en pasada 42.

**Resultado:** **1 sprint COMPLETADO, 0 BLOQUEADOS, 0 RECHAZADOS, 1 commit + 1 push + 1 deploy:rules ejecutado.** Cazadores 20/20 PASS.

### Sprints procesados (pasada 43)

| Sprint | Estado | Commit | Deploy rules | Deploy Vercel | Tiempo aprox |
|---|---|---|---|---|---|
| SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS | COMPLETADO | `d7b320b` | ✔ sha `cab27b049c…` 21:28:12Z | hook disparado `27bgMBiJbSMXnUWagN0H` | ~25 min |

### Detalle SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS (commit `d7b320b`)

**Origen:** Cowork escribió el sprint en pasada 41 marcándolo "ESCALA"; coordinator pasada 42 lo movió a BLOQUEOS con 4 decisiones requeridas. Jorge respondió en pasada 43 con OK formal.

**3 funciones implementadas:**

1. **Trazabilidad enviadoPor.** Los campos `creadoPor`/`creadoPorNombre` YA vivían en outbox desde antes (líneas 1102-1103 de `send.ts` pre-cambio, parser `whatsappInbox.service.ts:136-137`). El sprint solo expone el nombre en UI: `MensajeBubble` ahora renderiza `creadoPorNombre || 'Sistema'` en el footer de burbujas salientes, junto a hora + check. NO se renombraron a `enviadoPor*` para preservar shape.

2. **Prepend nombre del agente al cliente (default ON).** `config/whatsapp_envio` extendido con campo `nombreAgenteAlClienteActivo: boolean` (parser default true si missing). En `api/whatsapp/send.ts` paso 10b (antes del outbox tx): lookup del flag con Admin SDK, fail-soft con warn-log. Si flag activo Y `perfilNombre` no vacío, modifica `texto` a `*${primerNombre}:* ${texto}`. Idempotente (`startsWith` check). Solo `tipo === 'texto_libre'`. Toggle admin-only en `/admin/configuracion`.

3. **Respuestas rápidas programables.** Nuevo doc `config/whatsapp_respuestas_rapidas` (rule `read: esStaff()` / `write: esAdmin()`, mismo patrón multi-match que `whatsapp_envio`/`whatsapp_numeros`). Nuevo service `whatsappRespuestasRapidas.service.ts`. Editor admin-only en `/admin/configuracion`. Dropdown "/" en composer del inbox: helper `detectarPrefixSlash` requiere whitespace antes del `/` (no matchea URLs), filtro case-insensitive prefix match, click/Enter inserta texto reemplazando el match, cursor al final, ESC cierra.

**Archivos modificados (10):**
- `api/whatsapp/send.ts` — `let texto` + paso 10b prepend
- `firestore.rules` — match `/config/whatsapp_respuestas_rapidas`
- `firestore.rules.deployed.lock` — sha actualizado post-deploy
- `src/services/configWhatsappEnvio.service.ts` — flag `nombreAgenteAlClienteActivo`
- NUEVO `src/services/whatsappRespuestasRapidas.service.ts` (107 LOC)
- `src/components/inbox/MensajeBubble.tsx` — render `creadoPorNombre`
- `src/pages/Configuracion.tsx` — toggle + editor respuestas
- `src/pages/InboxConversacion.tsx` — dropdown "/" + sub respuestas
- `docs/sprints/BLOQUEOS.md` — stub desbloqueado
- `docs/sprints/COLA_AUTONOMA.md` — sprint movido a PENDIENTE, luego COMPLETADO

**Pipeline:**
- archivist PRE-CHANGE: P-001..P-020 evaluados (ver detalle en `EJECUCION_AUTONOMA.md`). Sin advertencias bloqueantes.
- builder: 7 cambios coordinados, sin loops.
- tester: typecheck PASS, cazadores pre-deploy 19/20 (P-005 esperado), `npx firebase deploy --only firestore:rules` ✔, lock actualizado sha `cab27b049c5905b51bcbe5529f25a5a8d988440f6287a287897e302a5bdb7dd3`, cazadores post-deploy 20/20 PASS, build 4.44s.
- regression_guardian (inline): APPROVED — outbox shape intacto, prepend idempotente, fail-soft, gates correctos.
- reviewer (inline, obligatorio): APPROVED con deuda follow-up (verificar emulator para patrón multi-match).
- commit `d7b320b`: 10 archivos, +619/-72.
- push `32f7d8c..d7b320b main -> main` OK.
- Vercel deploy hook: job `27bgMBiJbSMXnUWagN0H` PENDING.

### Deuda registrada (pasada 43)

1. **Verificar emulator local** patrón multi-match rule. Aplica a 3 rules existentes hoy en producción (`whatsapp_envio`, `whatsapp_numeros`, `whatsapp_respuestas_rapidas`). Si la semántica de Firestore es OR para matches superpuestos, refactor needed (sprint follow-up).
2. **NO renombrar** `creadoPor` → `enviadoPor` en outbox (cross-collection risk).

### Estado anti-regresión post pasada 43

- Cazadores activos: 20/20 verificados PASS.
- Postmortems: 11 archivos (sin nuevos en esta pasada — no fue hotfix).
- Allowlist size: sin cambios.

---

## Pasada 42 (`trabaja` — Quickwins inbox + escalado trazabilidad)

**Disparada:** Jorge pegó `trabaja` el 2026-05-23 (la fecha cambió respecto al día anterior). Cowork había dejado al tope de la cola 2 sprints nuevos.

**Resultado:** **1 sprint COMPLETADO, 1 ESCALADO a BLOQUEOS, 0 hotfixes, 1 commit + 1 push.** Cazadores 20/20 PASS. Sin tocar rules. Sin deploy de rules. Sin migración.

### Sprints procesados (pasada 42)

| Sprint | Estado | Commit | Deploy Vercel | Deploy rules | Notas |
|---|---|---|---|---|---|
| SPRINT-WA-INBOX-UX-QUICKWINS | COMPLETADO | `3eff5eb` | pendiente push reciente | N/A | 4 quickwins frontend (selector con número humano + último servicio + copiar + plantillas siempre) |
| SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS | ESCALADO | — | — | — | Toca `send.ts` + nueva rule `config/whatsapp_respuestas_rapidas`. 4 decisiones esperando OK de Jorge en BLOQUEOS |

### Detalle SPRINT-WA-INBOX-UX-QUICKWINS (commit `3eff5eb`)

**Origen:** Cowork lo agregó al tope de la cola el 2026-05-23 tras Jorge revisar el inbox en producción. Autónomo: no toca rules, no toca endpoint, no migra.

**Archivos (6):**

1. `src/services/configWhatsappEnvio.service.ts` — `NumeroWhatsapp` extendido con `numeroReal?: string` opcional. Parser/setter NO persisten undefined (CLAUDE.md gotcha "strip undefined fields before setDoc"). Defaults `CONFIG_WHATSAPP_NUMEROS_DEFAULT` seedeados con los 2 números reales que Jorge confirmó: `1226992440486630 → +1 829-471-6265` (Principal) + `1151997541323577 → +1 849-564-6767` (Respaldo).

2. `src/pages/Configuracion.tsx` — dropdown del selector muestra `"{numeroReal} · {etiqueta} · cód {phoneNumberId}"`. Estado actual destacado con el número humano en negrita. Editor admin del catálogo abierto vía estado borrador `editandoNumeros: NumeroWhatsapp[] | null` con 3 inputs por item (numeroReal editable, etiqueta editable, cód read-only). Handler `handleSaveWaNumeros` invoca `actualizarConfigWhatsappNumeros` con `userProfile?.nombre`.

3. `src/components/inbox/CardCliente.tsx` — helper local `CopyButton` (icono Copy → Check verde 1.5s + toast). Aplicado a nombre, teléfono (nuevo bloque pintado desde `waId`), email, dirección. Sin tocar la lógica de carga ni los CTAs.

4. `src/components/inbox/PanelCliente360.tsx` — `useMemo ultimoServicioRealizado` sort client-side sobre `ordenesCerradas` con cascada de fechas (`cierreServicio.fechaCierre` → `fechaCierre` raíz → `fechaCita` → `createdAt`), respeta P-015 (no se agregó orderBy a la query Firestore). Componente `UltimoServicioCard` renderiza en tab Datos arriba de CardCliente con número de orden, fase, equipo · marca, descripción de falla y fecha de cierre. Click navega a la orden.

5. `src/components/inbox/MensajeBubble.tsx` — botón clipboard adicional (lado opuesto al "copiar a orden" existente). Usa `extraerTextoCopiable` ya disponible. Se renderiza siempre que el mensaje tenga texto/caption, en cualquier dirección. `navigator.clipboard.writeText` + toast + Check 1.5s. Opacity 0 → group-hover-100 para no saturar el chat.

6. `src/pages/InboxConversacion.tsx` — `<SelectorPlantillas waId={waId} />` montado también cuando `ventanaAbierta`, entre textarea y send button. Con `!ventanaAbierta` sigue en el banner amarillo arriba. No se duplica.

**Pipeline:**
- Typecheck: PASS.
- Cazadores: 20/20 PASS (P-001..P-007, P-009..P-012, P-014..P-020).
- Lint: clean.
- Build: PASS 4.63s.
- Pre-commit hook: PASS (typecheck + cazadores + lint staged).
- Push: `d212020..3eff5eb main -> main`.

### Detalle SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS (escalado a BLOQUEOS)

**Razón del escalado:** el sprint declaraba "ESCALA a BLOQUEOS" en su propio header. Toca:
- `api/whatsapp/send.ts` — endpoint público (persistir `enviadoPorUid` + `enviadoPorNombre` en outbox, y opcionalmente anteponer nombre del agente al texto saliente).
- `firestore.rules` — rule nueva para `config/whatsapp_respuestas_rapidas` (espejo de `config/whatsapp_envio`: read staff, write admin).

Sub-regla CLAUDE.md exige OK formal con shape `OK: jorge YYYY-MM-DD HH:MM ...` para ambas áreas.

**4 decisiones esperando Jorge en `BLOQUEOS.md`:**

1. **Scope** — opción 1: las 3 funciones en un sólo sprint con deploy rules al final; opción 2: fases A (trazabilidad sin rules) + B (nombre agente con flag mínimo) + C (respuestas rápidas con rule + UI).
2. **Flag "nombre del agente al cliente":** default ON u OFF. Sugerido OFF.
3. **Permiso `config/whatsapp_respuestas_rapidas`:** solo admin (espejo `config/whatsapp_envio`) vs también coord/staff. Sugerido solo admin.
4. **Deploy rules automático:** OK para `npm run deploy:rules` desde coordinator (precedente: SPRINT-WA-NUMERO-RESPALDO-MANUAL Fase 1 aprobó `deploy=auto`).

### Lecciones / mantenimiento

- Quickwin típico "mostrar un campo opcional nuevo en una entidad existente" se resolvió con backcompat sin migración: defaults seedeados + parser que omite si falta. Patrón replicable para futuras extensiones del catálogo (icono, color, descripción larga, etc.).
- El destacado "Último servicio realizado" mostró que tener `obtenerTodasOrdenesPorTelefono` SIN orderBy (cumpliendo P-015) ya pagaba dividendos: agregar un nuevo derived state (sort distinto, criterio distinto) no requirió tocar la query. Reafirma la decisión de SPRINT-INBOX-10.
- El botón clipboard reutilizó `extraerTextoCopiable` existente (de SPRINT-INBOX-8b para "copiar a orden") — bajo costo gracias a la separación previa.
- El SelectorPlantillas en composer con ventana abierta es 1 línea de cambio porque ya estaba implementado modularmente; el costo real era una decisión UX (¿abre dropdown sobre el composer?) que ya tenía respuesta del INBOX-7.

### Pendientes heredados (acción manual de Jorge)

- `npm run deploy:storage-rules` para SPRINT-138 (P-013 sigue WARN cold start).
- QA visual INBOX-11/INBOX-10/INBOX-8c en producción.
- QA B.1 (`SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-1`).
- Decisiones para `SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS` en BLOQUEOS.
- (Opcional) `npx tsx scripts/backfill-convs-corruptas-stripundefined.ts` para las 3 convs corruptas (auto-sanan en próximo mensaje).

### Health del sistema anti-regresión

- Cazadores activos: **20/20 PASS** (P-001..P-007, P-009..P-012, P-014..P-020). P-008 saltado intencionalmente.
- Postmortems: 11 archivos en `docs/postmortems/`. Sin postmortem nuevo esta pasada (sin hotfix).
- Lock `firestore.rules.deployed.lock`: sha `07251e9684be…` (2026-05-22T23:00:08Z). P-005 limpio.
- Lock `storage.rules.deployed.lock`: pendiente deploy productivo de Jorge (P-013 WARN cold start, no bloqueante).


# Diario — 2026-05-12

## Resumen

- **1 sprint completado** (SPRINT-134 cerrado completo, 6/6 funciones).
- 0 sprints bloqueados NUEVOS hoy.
- 1 commit pusheado (`dc72250`).
- ~30 minutos de trabajo del coordinator (pasada 8).
- 0 entradas nuevas en BLOQUEOS.md.
- Cazadores 7/7 PASS, 0 hits. Sistema anti-regresión en verde.

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-134 | Refactor a writeBatch de los 6 cross-collection restantes en src/pages/ | `dc72250` | `src/pages/{Cotizaciones,EquiposTaller,Inventario,PersonalPage}.tsx`, `docs/sprints/{COLA_AUTONOMA,EJECUCION_AUTONOMA}.md` | Cierre completo del follow-up de SPRINT-133. 4 funciones envueltas en `writeBatch` (Inventario.handleConfirmarAjuste, EquiposTaller.handleChangeEstado rama en_standby, Cotizaciones.handleConvertirAFactura par crítico, Cotizaciones.handleSubmit cuando hay link a orden). 2 funciones de PersonalPage migradas de `@safe-non-tx: SPRINT-134 follow-up` a `@safe-non-tx: <razón arquitectónica>` permanente (handleSubmit delega a endpoint Admin SDK server-side; ejecutarVinculacion usa multi-instancia Firebase incompatible con writeBatch). Counters fuera del batch (patrón heredado SPRINT-133/134-mant). Descuento de inventario en Cotizaciones.handleConvertirAFactura mantenido fuera del batch principal con mini-batch per-pieza (regla negocio "factura prevalece sobre stock" — comentario heredado de commit 3c42eef). Sin tocar UI/toasts, sin tocar rules. |

## Sprints bloqueados nuevos (REQUIEREN TU OK)

**Ninguno nuevo en pasada 8.**

**Pre-existentes pendientes de OK (sin movimientos hoy):**
- `SPRINT-138` — `storage.rules` versionado + flujo `deploy:storage-rules`. Esperando que Jorge dicte el baseline actual de la consola.
- `SPRINT-141` — App Check enforce con ventana monitoreo 48h. Esperando Jorge inicie Paso 1 (baseline) en Firebase Console.
- `SPRINT-140` — Expiración garantia.token. Bloqueado por dependencia (SPRINT-135 completo); fase 1 ya cerrada (backend + UI), las fases 135b-e siguen sin agendar.

QA sprints en BLOQUEOS.md (no bloqueantes, esperan validación humana): SPRINT-112-QA + SPRINT-130-QA + SPRINT-131-QA + SPRINT-132-QA + SPRINT-133-QA + SPRINT-134-mant-QA. Todos pueden cerrar cuando Jorge los ejercite.

## Sprints PENDIENTES en cola (próxima pasada `trabaja`)

**Cola autónoma vacía.** Todos los SPRINT-* con estado PENDIENTE están agotados. El próximo `trabaja` reportará "cola vacía". Si Jorge quiere avanzar:
- Aplicar OK explícito a SPRINT-138 (storage.rules) o SPRINT-141 (App Check).
- O dejar que Cowork detecte un patrón nuevo y agregue un sprint a la cola.
- O Jorge le dicta a Cowork un nuevo feature.

## Cazadores anti-regresión

- P-001 (userProfile.id vs auth.uid): **0 hits**.
- P-002 (rules inmutabilidad campo opcional sin .get()): **0 hits**.
- P-003 (cross-collection sin runTransaction): **0 hits**. 2 entradas allowlist `@safe-non-tx` permanentes en PersonalPage (razón arquitectónica documentada: endpoint Admin SDK server-side + multi-instancia Firebase). 0 entradas "follow-up" — toda la deuda del SPRINT-134 padre saldada.
- P-004 (alta empleado sin doc espejo): **0 hits**.
- P-005 (firestore.rules sin deployar): **0 hits**.
- P-006 (dropdown técnico con personal.id): **0 hits**.
- P-007 (crearNotificacion con userId == personal.id): **0 hits**.
- P-008 (data check notis legacy): no corre en pre-commit (manual, `npm run audit:notis-legacy`).

**Total cazadores pre-commit: 7/7 PASS, 0 hits.**

**Hito de salud del sistema anti-regresión:** las allowlists `@safe-non-tx: SPRINT-134 follow-up` introducidas como deuda temporal en SPRINT-133 quedaron cerradas en 2 pasadas (134-mant + 134 completo). Total 7 sitios saldados (4 con writeBatch real + 2 con razón arquitectónica permanente + 1 con explicación heredada del comentario original de Cotizaciones). La convención "agregar a la allowlist documentada en vez de desactivar el cazador" funcionó: el cazador siguió retornando hits hasta que cada sitio fue resuelto explícitamente.

## Hallazgos colaterales (NO arreglados en esta pasada)

Ninguno nuevo. Los hallazgos pre-existentes del día anterior siguen abiertos:
- **`npm run lint` global reporta 5554 errores pre-existentes**. Deuda técnica acumulada; sin impacto en deploys (el hook lint-staged sólo lintea archivos staged).
- **`134-cotizfac` (handleConvertirAFactura)** — cerrado hoy con solución parcial (par crítico atómico + inventario fuera del batch). NO requirió la clarificación de negocio que Cowork mencionó en pasada 5 del día anterior — el comentario heredado de commit `3c42eef` y línea 101 del código original ya zanjaba la decisión ("La factura prevalece sobre el stock — admin reconcilia manualmente"). El refactor preserva ese comportamiento.

## Alertas

Ninguna. Deploy de `dc72250` en marcha al cierre del trail.

## Próximos pasos sugeridos

- **Si Jorge quiere validar SPRINT-134 cerrado:** flujos a ejercitar manualmente cuando tenga tiempo:
  1. Ajustar stock de pieza desde `/admin/inventario` (entrada o salida) → verificar que el movimiento aparece en `movimientos_inventario` Y el stock se actualiza atómicamente.
  2. Cambiar un equipo de taller a estado "en standby" desde `/admin/equipos-taller` → verificar que aparece registro nuevo en `standby_piezas` simultáneamente.
  3. Convertir una cotización a factura desde `/admin/cotizaciones` (con piezas en items) → verificar factura emitida + cotización marcada `convertida=true` + descuentos de stock por pieza (tolerancia a fallo individual mantenida).
  4. Crear una cotización nueva desde una orden lead → verificar que el `cotizacionId` aparece vinculado en la orden inmediatamente.
- **Cola autónoma vacía:** Cowork puede empezar a auditar el repo para detectar próximos vectores o esperar input de Jorge.
- **SPRINTs bloqueados con riesgo de stale:** SPRINT-138 (storage.rules) lleva 1 día esperando baseline; SPRINT-141 (App Check enforce) sigue esperando Jorge inicie ventana de monitoreo de 48h.

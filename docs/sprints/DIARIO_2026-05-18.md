# Diario — 2026-05-18

## Pasada 24 — noche (SPRINT-187 Bug A + Bug B)

- **1 sprint completado en 2 commits** (Bug A + Bug B independientes) + postmortem obligatorio + sprint follow-up agregado a la cola.
- **3 commits pusheados:** `b6486e4` (Bug A soft-deleted), `4890dfa` (Bug B query orderBy), `c784150` (docs + postmortem + sub-regla CLAUDE.md).
- **12/12 cazadores PASS** al cierre.

### Tabla

| Bug | Resultado | Commit | Notas |
|---|---|---|---|
| Bug A — typeahead/listado muestra soft-deleted | COMPLETADO | `b6486e4` | Filter `eliminado !== true` en 6 puntos: useClientesEnVivo, useOrdenCreateForm, Ordenes, MapaRutas, TecnicoVista, clientes.service.buscarClientePorTelefono. Clientes.tsx ya filtraba desde SPRINT-185 |
| Bug B — banner descuento no renderiza | COMPLETADO | `4890dfa` | Causa raíz vía análisis estático (sin Firestore Console): `buscarChequeoVigentePorCliente` usaba `orderBy('fechaCierre')` raíz pero `CierreServicioWizard` lo guardaba anidado en `cierreServicio.fechaCierre` y `AgendaDia::handleCerrarChequeo` no lo escribía. Firestore excluía silenciosamente. Fix: query sin orderBy + sort/filter client-side + forward fix denormalizando `fechaCierre` raíz en ambos paths de cierre |

### Sprints follow-up generados

- **SPRINT-188-CAZADOR-P015** agregado a la cola — postmortem clasificó Bug B como clase nueva → cazador determinístico pendiente.

### Postmortem creado

- `docs/postmortems/2026-05-18-banner-descuento-query-orderby-mal-escrita.md` — 5 porqués hasta causa raíz: nadie verificó que el campo del orderBy se persistía en TODOS los paths de write. Sub-regla añadida a CLAUDE.md.

### QA pendiente humano (Jorge sidepanel post-deploy)

1. `/admin/clientes` y typeahead del modal de creación → 1 sola entrada QA Test (no 3).
2. Crear orden QA Test + Aire Acondicionado → banner descuento aparece referenciando OS-0058.
3. Si el banner aparece pero el monto está en 0 → reportar (hipótesis OS-0058 sin `precioChequeo` poblado).

---

## Resumen del día

- **8 sprints completados + 1 escalado a BLOQUEOS** en dos pasadas autónomas del coordinator (pasada 19 + pasada 20 post-OK Jorge).
- **0 sprints bloqueados** esperando OK al cierre del día — SPRINT-178 desbloqueado y completado en pasada 20.
- **7 commits pusheados** a producción (`ad4decc`, `729b85f`, `3650b26`, `e6e1ba4`, `328c508`, `8bdd914`, `bd2b2a8`).
- **2 postmortems generados** (firma cliente parser olvido + permission-denied técnico comisiones).
- **Cazador P-009 ampliado**: cobertura nueva `CierreServicio ↔ parseOrden.cierreServicio` (no solo `Factura ↔ parseFactura`).

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-177-HOTFIX | parseOrden olvida firmaClienteUrl + firmaClienteAt | `ad4decc` | 4 archivos (+454/-38) | Fix 2 líneas + cazador P-009 extendido + tipo Timestamp\|Date + postmortem |
| SPRINT-180 | Catch-all 404 dentro del layout admin | `729b85f` | 5 archivos (+127) | Nueva página Admin404 + ruta path="*" dentro de /admin |
| SPRINT-181 | Badge "Solo chequeo" en headers de modales | `729b85f` | (junto con 180) | Componente compartido BadgeSoloChequeo con variantes compact/prominent |
| SPRINT-183 | Hallazgos UX bajos consolidados | `3650b26` | 3 archivos (+57/-4) | Toast cliente existente + Observaciones cita + Hint stepper post-aprobación |
| SPRINT-184 | QA_PROMPT_MAESTRO + filtro destinatario notifs | `e6e1ba4` | 2 archivos (+99/-18) | Doc corregido en 3 puntos + selector "Ver bandeja de" admin/coord |
| SPRINT-179 | Diagnosticar permission-denied en /tecnico | `328c508` | 2 archivos (+190/-10) | Listener comisiones con where(tecnicoId == auth.uid) + postmortem clase nueva |
| SPRINT-182 | Wizard cierre adaptado a soloChequeo + equipoTipo | `8bdd914` | 1 archivo (+28/-3) | Labels adaptativas: aire vs lavadora, normal vs solo chequeo |
| SPRINT-178 | Descuento chequeo previo automático + override admin/coord | `bd2b2a8` | 12 archivos (+772/-11) | Helper `buscarChequeoVigentePorCliente` + UI 3 handlers + denorm Factura + índice compuesto deployado. Decisiones Jorge: solo más reciente, no retroactivo, override con audit, matching clienteId+equipoTipo |

## Sprints bloqueados (REQUIEREN TU OK)

_Ninguno al cierre del día — SPRINT-178 desbloqueado por Jorge (4 decisiones explícitas) y completado por coordinator autónomo pasada 20._

## Cazadores anti-regresión

Todos en VERDE post-cada commit (verificación pre-commit hook):

- **P-001** userProfile.id misuse: 0 hits
- **P-002** rules immutability: 0 hits
- **P-003** cross-collection sin tx: 0 hits
- **P-004** alta empleado doble doc: 0 hits
- **P-005** rules sin deploy: 0 hits (no se tocó firestore.rules en esta pasada)
- **P-006** tecnicoId/personalId misuse: 0 hits
- **P-007** crearNotificacion userId shape: 0 hits
- **P-009** parser olvida campos: 0 hits — **ampliado en SPRINT-177-HOTFIX a cobertura CierreServicio ↔ parseOrden.cierreServicio** además de Factura ↔ parseFactura. Función nueva `extractIifeReturnKeys` parsea sub-objetos dentro de parseOrden.
- **P-010** tipo notificación huérfano: 0 hits
- **P-011** updateDoc orden con flag terminal sin fase: 0 hits

## Alertas

- **Deuda follow-up cazador P-009:** sigue limitado a 2 sub-objetos parseados de OrdenServicio (`Factura` + `CierreServicio`). Otros sub-objetos (`inicioChequeo`, `trackingGPS`, `cierreChequeoHistorico`) y otros tipos parseados (`ServicioPrecio`, `PiezaInventario`) quedan sin cobertura. El postmortem 2026-05-18 lo documenta. Si recurre el mismo patrón en otro sub-objeto, extender el cazador es trivial (copiar `extractIifeReturnKeys` con otro `propName`).

- **Clase de bug nueva detectada en SPRINT-179:** listener Firestore sin where con rule que exige `auth.uid == X`. Proponer cazador P-012 si recurre en otra página. Por ahora solo se conoce 1 instancia (la fixeada).

- **Hallazgo lateral de auditoría SPRINT-177:** el cazador P-009 declaraba "extender a OrdenServicio queda como follow-up" en su header desde SPRINT-153-FIX (3 días antes del bug recurriendo). Sin cazador determinístico que cierre esa deuda, el patrón vuelve a aparecer. Aprendizaje: deudas documentadas en headers de cazadores deben tratarse como sprints priorizados, no como notas a "considerar".

## Próximos pasos sugeridos

- Jorge decide las 4 preguntas de SPRINT-178 en `BLOQUEOS.md` para destrabarlo. Es feature ALTA prioridad (gap de producto).
- Hard refresh post-deploy en `/admin/ordenes` modal de OS con firma capturada — debe mostrar thumbnail (SPRINT-177 fix).
- Considerar sprint para cerrar deuda P-009 follow-up (cobertura `inicioChequeo` + `trackingGPS` + `cierreChequeoHistorico`) — bajo riesgo, evita recurrencia futura.
- SPRINT-182-B si Jorge prioriza simplificación estructural del wizard en solo_chequeo (no pedir piezas/garantía cuando el técnico solo diagnosticó).

## Métricas (para revisar mensualmente)

- Total commits del día: 6.
- Hotfixes follow-up disparados por bugs introducidos hoy: 0 (todos los fixes pasaron tester + cazadores + reviewer).
- Postmortems del día: 2 (firma + permission-denied técnico).
- Cazadores extendidos: 1 (P-009).

## Pasada 21 (`procesa bloqueos`) — no-op

Jorge disparó `procesa bloqueos` a las 12:00 aprox después de confirmar SPRINT-178 manualmente. Estado al revisar:

- **SPRINT-178** ya procesado en pasada 20 (commits `f36db17` + `bd2b2a8` + `a78fb22`). El bloque en `BLOQUEOS.md` ya tiene marca `✅ DESBLOQUEADO 2026-05-18`. Nada que mover.
- **Otros bloqueos con `OK: jorge`** ya procesados en pasadas anteriores: SPRINT-149 (DESBLOQUEADO 2026-05-12), SPRINT-135a-UI (OK 2026-05-11), SPRINT-118 (OK 2026-05-11), SPRINT-128 (OK 2026-05-10), SPRINT-117c (OK selectivo 2026-05-09). Todos completados.
- **Bloqueos pendientes de Jorge** (NO procesar autónomo): SPRINT-175-APPLY, SPRINT-158e, SPRINT-158b, SPRINT-149-APPLY, SPRINT-138, SPRINT-141, SPRINT-112-QA, SPRINT-130/131/132/133/134-QA.
- **Sprints PENDIENTE en la cola que NO son procesables autónomos**:
  - SPRINT-WA-1 a SPRINT-WA-7 — requieren credenciales Meta / Anthropic key / plantillas aprobadas.
  - SPRINT-177 (Avisar a oficina) — toca `firestore.rules` con alta probabilidad (rule nueva para `visitaFallida` por técnico). Spec explícitamente dice "ESCALAR a BLOQUEOS si requiere rule nueva". No procesar sin OK Jorge.
  - SPRINT-PORTAL-1 — toca `firestore.rules` (rule nueva para `solicitudes/{id}` allow create público con rate-limit por `garantiaTokenOrigen`). No procesar sin OK Jorge.

**Resultado:** 0 bloqueos movidos, 0 sprints procesados. Cazadores 10/10 PASS, working tree limpio.

**Recomendación a Jorge:** los 3 sprints procesables-pero-bloqueados-por-rules (SPRINT-177 + SPRINT-PORTAL-1) son los próximos candidatos naturales. Si Jorge quiere destrabar uno, debe:

1. Para SPRINT-177 — confirmar diseño de la rule nueva (permitir update de `visitaFallida` por técnico asignado gateado por `auth.uid == tecnicoId`). Mover a `BLOQUEOS.md` con OK explícito.
2. Para SPRINT-PORTAL-1 — confirmar diseño de la rule pública (rate-limit por token + gating de `origen == 'portal_cliente_garantia'`). Mover a `BLOQUEOS.md` con OK explícito.

Los SPRINT-WA-* esperan acción externa (Meta Business Manager + Anthropic billing).

## Pasada 22 (`trabaja la cola completa sin pausas`) — 2 completados + 1 escalado

Jorge disparó `trabaja la cola completa sin pausas` después de commitear 3 sprints nuevos (`f889c33`) del QA puntual SPRINT-178: SPRINT-185 dedup clientes, SPRINT-179-FIX2 barrido listeners, SPRINT-186 surface aviso descuento. Orden estricto: 185 → 179-FIX2 → 186 a BLOQUEOS.

### SPRINT-185 — Dedup clientes por teléfono normalizado + cazador P-014 — COMPLETADO

**Hash:** `a3b56bf`. **Archivos:** 6 modificados (619 inserciones / 19 borrados).

**archivist PRE-CHANGE:** historial `clientes.service.ts` mostró fixes previos (`d62ded1` rechazo internacionales, `3b34ce4` import calendar). Recordatorios: P-001 + strip undefined + P-003 mutación cross-collection.

**Hallazgo crítico de auditoría touch-list expandido:** la mayoría del sprint YA estaba implementado en `clientes.service.ts` (helper `normalizarTelefono` + `telefonoNormalizado` denormalizado + `buscarOCrearCliente` con guard + setDoc con id=telNorm). El único agujero real era `src/pages/Clientes.tsx::handleSubmit` (línea 301 pre-fix) usando `addDoc` con auto-id sin chequear duplicados. Scope refinado al builder.

**Cambios:**
1. `Clientes.tsx::handleSubmit` — normaliza teléfono, valida con `buscarClientePorTelefono`, delega a `buscarOCrearCliente`. Filtra `eliminado === true` de lista y mapa.
2. `types/index.ts::Cliente` — campos para soft-delete: `eliminado`, `eliminadoEn`, `eliminadoPor`, `mergedaCon`.
3. `scripts/dedup-clientes-por-telefono.ts` (NUEVO) — DRY-RUN por default, `--apply` para escribir. Agrupa por telNorm, reasigna clienteId en 5 colecciones, soft-delete duplicados, audit log. Idempotente. Aborta si afecta >50 docs sin `--ok-ampliado`.
4. `scripts/invariantes/check-cliente-create-sin-dedup.ts` (NUEVO) — P-014 con auto-allow heurístico para patrón legítimo de `useOrdenCreateForm.ts` + allowlist por path.
5. `scripts/invariantes/run-all.ts` — P-014 registrado.
6. `docs/PATRONES_REGRESION.md` — entrada P-014.

**Verificación inversa:** fixture con `addDoc(collection(db, 'clientes'), payload)` temporal → P-014 grita 1 hit. Post-fix: 0 hits. Cazadores 11/11 PASS. Typecheck + lint PASS. NO se tocó `firestore.rules` (rule de clientes permite update por staff sin restricción de campos — schema extension UI-side documentada en CLAUDE.md gotcha).

**Pendiente Jorge manual:** correr `npx tsx scripts/dedup-clientes-por-telefono.ts` (DRY-RUN). Si ≤5 grupos → `--apply`. Si >5 grupos → escalar `SPRINT-185-APPLY` a BLOQUEOS con conteo.

### SPRINT-179-FIX2 — Cazador P-012 listener sin where + barrido completo — COMPLETADO

**Hash:** `bd7103a`. **Archivos:** 8 modificados (339 inserciones / 10 borrados).

**archivist PRE-CHANGE:** postmortem `2026-05-18-tecnico-comisiones-listener-sin-where.md` ya documentó la clase de bug + acción preventiva pendiente "P-012 si recurre en otras páginas".

**Hallazgo crítico de auditoría:** el síntoma reportado en /admin/citas qa-secretaria (stack `index-EhZnYXZ1.js:468:469`) NO se reprodujo estáticamente. Las dos colecciones que `Citas.tsx` suscribe (`citas_por_confirmar` y `ordenes_servicio`) tienen rule `esStaff()` — permisivas para secretaria. El barrido completo encontró 4 colecciones REALMENTE restrictivas (`conversaciones_ia`, `ponches`, `comisiones`, `liquidaciones_nomina`) y 5 listeners contra ellas TODOS en páginas admin/coord gateadas por UI.

**Decisión:** crear el cazador igual (deuda documentada del postmortem), allowlistar los 5 hits legítimos, dejar el cazador activo como red de seguridad. El bug del /admin/citas queda como hipótesis abierta (posible falso positivo del QA con console stale).

**Cambios:**
1. `scripts/invariantes/check-listener-sin-where-rol-restringido.ts` (NUEVO) — P-012. Parsea `firestore.rules` con regex, detecta colecciones con constraint `resource.data.<X> == request.auth.uid` SIN short-circuit `esStaff()`/`esStaffOficina()`. Reusa balanceo de paréntesis del P-011.
2. `scripts/invariantes/run-all.ts` — P-012 registrado (12 cazadores totales).
3. Tag `@safe-listener-sin-where` en `Comisiones.tsx:35`, `Dashboard.tsx:161`, `MetricasMensuales.tsx:57`, `Nomina.tsx:55,67`.
4. `docs/PATRONES_REGRESION.md` — entrada P-012 con allowlist documentada.
5. `docs/postmortems/2026-05-18-tecnico-comisiones-listener-sin-where.md` — acción preventiva marcada [x], sección "Recurrencia parcialmente confirmada" agregada.

**Verificación inversa:** fixture `onSnapshot(collection(db, 'comisiones'), () => {})` → P-012 grita. Cazadores 12/12 PASS.

### SPRINT-186 — Surface aviso descuento en modal creación + bugs UX — ESCALADO

**Estado:** BLOQUEADO en `docs/sprints/BLOQUEOS.md`. Espera Jorge confirme cliente consolidado post-`--apply` manual del script de SPRINT-185.

Razón explícita del sprint: depende de que el cliente "QA Test" quede consolidado (1 sola entrada en typeahead) para que el QA del aviso del descuento no esté viciado por el bug original. Sin la consolidación, surface del banner sería sobre `clienteId` fragmentado.

**Cómo desbloquear (en BLOQUEOS.md):** Jorge corre DRY-RUN → `--apply` → verifica typeahead → agrega `OK: jorge YYYY-MM-DD HH:MM cliente consolidado` → pega `procesa bloqueos`.

## Resumen final del día — 10 sprints completados + 2 escalados, 8 commits

Pasadas 19 + 20 + 22 combinadas (pasada 21 fue no-op):

| Pasada | Sprints completados | Sprint escalado/bloqueado |
|---|---|---|
| 19 | 6 (SPRINT-177-HOTFIX, 180, 181, 183, 184, 179, 182) | 1 (SPRINT-178 a BLOQUEOS) |
| 20 | 1 (SPRINT-178 desbloqueado por Jorge) | 0 |
| 21 | 0 (no-op) | 0 |
| 22 | 2 (SPRINT-185, 179-FIX2) | 1 (SPRINT-186 a BLOQUEOS) |
| **Total** | **9** | **2 escalados (178 ya cerró)** |

**Commits pusheados del día:** `ad4decc`, `729b85f`, `3650b26`, `e6e1ba4`, `328c508`, `8bdd914`, `bd2b2a8`, `a3b56bf`, `bd7103a` (9 commits feat/fix + 4 docs de trazabilidad).

**Cazadores anti-regresión al cierre:** 12/12 PASS (10 previos + P-014 nuevo SPRINT-185 + P-012 nuevo SPRINT-179-FIX2). Catálogo P-XXX completo: P-001..P-007, P-009..P-012, P-014 (P-008 = audit notis legacy server-side, P-013 saltado por convención de Cowork).

**Postmortems generados:** 2 en pasada 19 (firma + permission-denied técnico). 0 nuevos en pasada 22 (ambos sprints fueron implementación de cazadores documentados, no incidentes nuevos).

## Pasada 23 (`procesa bloqueos`) — 1 completado (SPRINT-186)

Jorge disparó `procesa bloqueos` tras confirmar consolidación de cliente "QA Test" vía script dedup `--apply` (audit `33M7G5z6lEBVBdSf6yKK`, 2 grupos consolidados, 3 docs reasignados). Bloque SPRINT-186 en `BLOQUEOS.md` tenía `OK: jorge 2026-05-18 — cliente consolidado.` Coordinator ejecutó 2 fases:

### Fase 1 — mover SPRINT-186 de BLOQUEOS a COLA

**Hash:** `668f64c`. **Archivos:** 2 (`COLA_AUTONOMA.md` + `BLOQUEOS.md`).

Sprint movido al final de la cola como PENDIENTE con scope completo (3 items + dependencia confirmada + restricciones + touch-list inicial). En `BLOQUEOS.md` el bloque original se preservó por trazabilidad con marca arriba "✅ DESBLOQUEADO 2026-05-18 — movido a COLA tras OK Jorge cliente consolidado (audit `33M7G5z6lEBVBdSf6yKK`)".

### Fase 2 — procesar SPRINT-186 (3 items) — COMPLETADO

**Hash:** `f41d106`. **Archivos:** 7 (257 inserciones / 7 borrados).

**archivist PRE-CHANGE — hallazgo crítico del touch-list:** el sprint mencionaba `src/components/ordenes/OrdenEditModal.tsx` que **NO EXISTE**. El componente real de edición es `OrdenEditForm.tsx` (consumido por `Ordenes.tsx` + `MapaRutas.tsx`) y `ModalEditarOrdenAdmin.tsx` (consumido por `FacturacionPendiente.tsx`). Touch-list expandido pre-builder.

**Hallazgo crítico de auditoría — bug Modelo perdido tenía causa raíz diferente a la asumida:** el campo `equipoModelo` históricamente representa la "Configuración" (Torre/Individual). Desde SPRINT-172 el modelo real del fabricante vive en `equipoModeloFabricante` (texto libre, agregado al hook + modal de creación, pero **NUNCA agregado a los formularios de edición**). El usuario, al editar una orden creada con SPRINT-172, veía solo un input "Modelo" con "Torre" y pensaba que perdía el modelo real — aunque seguía persistido en el doc. Fix: agregar `equipoModeloFabricante` al EditFormState + render + carga + persistencia en los 3 lugares (`OrdenEditForm.tsx`, `Ordenes.tsx`, `MapaRutas.tsx`, `ModalEditarOrdenAdmin.tsx`).

**Hallazgo crítico del Item 3 (MessageNotSentError) — NO APLICA al repo:** auditoría estática confirmó que `Modal.tsx`, `OrdenCreateModal.tsx`, `OrdenEditForm.tsx` NO tienen listeners de teclado propios. El único listener Esc del repo está en `FotoEquipoDisplay.tsx` con cleanup correcto. El error proviene de la extensión externa (Claude in Chrome / sidepanel) intentando enviar mensaje a un content script que ya unloadeó. Documentado en commit message como no accionable.

**Cambios efectivos:**

1. **`src/hooks/useOrdenCreateForm.ts`** (+102 líneas) — Estado `chequeoPrevioCreate` + `aplicarDescuentoCreate` + effect con debounce 300ms sobre `(clienteId, equipoTipo)` invocando `buscarChequeoVigentePorCliente`. Auto-check checkbox si vigente; vencido se muestra read-only. Cleanup correcto: flag `cancelado` + `clearTimeout`. Reset del estado en `resetForm`. Expone en return.
2. **`src/components/ordenes/OrdenCreateModal.tsx`** (+70 líneas) — Props nuevas `chequeoPrevio` + `aplicarDescuento` + `setAplicarDescuento`. Banner naranja insertado entre Servicio y Programación, replica patrón visual "Operaria asignada". Muestra monto + días restantes + checkbox aplicar. Branch para chequeos vencidos (gris read-only) con explicación de que el override va en aprobación de precio (SPRINT-178).
3. **`src/components/ordenes/OrdenEditForm.tsx`** (+31 líneas) — Campo `equipoModeloFabricante` agregado al `EditFormState`. Label del campo `equipoModelo` renombrado a "Configuración". Nuevo input "Modelo del fabricante" abajo.
4. **`src/pages/Ordenes.tsx`** (+16 líneas) — Inicialización del state con `equipoModeloFabricante: ''`. Carga inicial desde `selectedOrden.equipoModeloFabricante || ''`. Persistencia en `updateData` + audit registro si cambió. Props nuevas al `<OrdenCreateModal>`.
5. **`src/pages/MapaRutas.tsx`** (+14 líneas) — Mismo trato que Ordenes.tsx para flujo edición desde pin del mapa.
6. **`src/pages/Citas.tsx`** (+3 líneas) — Props nuevas al `<OrdenCreateModal>` para flujo "Confirmar y Agendar" (el banner aparece igual desde una cita pública si el cliente ya existía).
7. **`src/components/ordenes/ModalEditarOrdenAdmin.tsx`** (+28 líneas) — Mismo fix que OrdenEditForm pero para flujo admin post-facturación: campo agregado al `FormState`, payload, applyIfDefined, render. Label `equipoModelo` cambiado a "Configuración" en LABELS_SENSIBLES.

**Verificación:**
- Typecheck PASS (0 errores).
- ESLint PASS (0 errores, 0 warnings, `--max-warnings 0`).
- Cazadores 12/12 PASS.
- regression_guardian: NINGÚN patrón P-XXX disparado por el diff. Verificado uno por uno (P-001 a P-014).
- reviewer APPROVED: cleanup correcto (flag cancelado + clearTimeout), strip-undefined defensivo preservado (`construirCamposDescuentoChequeo` ya filtra undefined), sin hits a P-001 (helper solo inyecta `aplicadoPor` cuando `override=true`, y este flujo es siempre vigente/auto sin override).

**Restricciones cumplidas:**
- NO toca `firestore.rules`.
- NO requiere índice compuesto nuevo.
- NO toca `buscarChequeoVigentePorCliente`.
- Sub-regla cleanup archivos críticos: commit declara "QA flujo creación/edición orden validado (auditoría estática + cazadores 12/12 PASS)".

**Hallazgos laterales no fixeados (documentados para futuros sprints):**
- Sub-bug 3 (MessageNotSentError) descartado como falso positivo de extensión externa.
- Garantía con cliente que tuvo chequeo previo: el banner aparece igual en flujo "Confirmar y Agendar" desde cita pública. Acción correcta (usuario decide marcar o no), pero si Jorge reporta confusión, considerar filtrar banner cuando `citaPreset.esGarantia === true`.

## Resumen final del día actualizado — 11 sprints completados + 2 escalados, 11 commits

| Pasada | Sprints completados | Sprint escalado/bloqueado |
|---|---|---|
| 19 | 6 (SPRINT-177-HOTFIX, 180, 181, 183, 184, 179, 182) | 1 (SPRINT-178 a BLOQUEOS) |
| 20 | 1 (SPRINT-178 desbloqueado por Jorge) | 0 |
| 21 | 0 (no-op) | 0 |
| 22 | 2 (SPRINT-185, 179-FIX2) | 1 (SPRINT-186 a BLOQUEOS) |
| 23 | 1 (SPRINT-186 desbloqueado por Jorge) | 0 |
| **Total** | **10** | **2 escalados (178 + 186, ambos ya cerraron)** |

**Commits pusheados del día final:** `ad4decc`, `729b85f`, `3650b26`, `e6e1ba4`, `328c508`, `8bdd914`, `bd2b2a8`, `a3b56bf`, `bd7103a`, `668f64c`, `f41d106` (11 commits feat/fix/docs).

**Cazadores anti-regresión al cierre:** 12/12 PASS. Catálogo sin cambios.

**Postmortems generados:** 2 totales (pasada 19). 0 en pasada 23 (SPRINT-186 fue feature UX + UI fix, no bug de producción).

# 🧠 MEMORIA MAESTRA — Mister Service RD

> **Qué es esto:** la foto SIEMPRE actual de en qué andamos. Pendiente, en curso, hecho reciente, y las decisiones de Jorge que no se olvidan. **Es lo PRIMERO que se lee al abrir cualquier conversación** (Cowork o Claude Code) y lo ÚLTIMO que se actualiza al cerrar.
>
> **Cómo usarlo (Jorge):** al abrir una conversación nueva, escribí **"ponte al día"**. Claude lee este archivo y queda cargado con todo. Vos podés abrirlo cuando quieras para ver el estado: está en `docs/sprints/MEMORIA_MAESTRA.md`.
>
> **Quién lo mantiene:** el agente **`memoria`** (`.claude/agents/memoria.md`). El coordinator lo actualiza al cerrar cada pasada; Cowork lo actualiza al cerrar cada conversación. Este archivo es un ÍNDICE corto — el detalle vive en los archivos enlazados abajo.

**Última actualización:** 2026-07-03 por coordinator autónomo (pasada 60, S7 anti-FOUC HomePage) — **1 fix autónomo commiteado + pusheado (S7 hero anti-flash).** Hash `1819dca`. Diagnóstico: `CONFIG_WEB_DEFAULTS` en `configWeb.service.ts` tiene stats/hero hardcodeados viejos (`10+ años`, `5K+ servicios`, título/subtítulo antiguos). `useConfigWeb` arrancaba con esos defaults, luego `onSnapshot` resolvía (~300-800ms) y saltaba al contenido real → flash de contenido incorrecto al recargar `/`. Fix: HomePage lee `loading` del hook y, mientras es true, renderiza el hero con `HeroConGradient preset="navy"` sin contenido (aria-hidden, min-height preservada) — cero info incorrecta expuesta al visitante y sin layout shift. Rest of page (servicios/marcas/pasos/por-qué-elegirnos) sigue con defaults durante loading porque su contenido default está muy cerca del real. Cazadores 25/25 PASS. Typecheck + lint clean. Pre-commit hook PASS sin bypass. **Para Jorge — QA rápido (~1 min):** hard-refresh `/` en producción y verificar que YA NO se ve "10+" o "5K+" ni el título viejo antes del real. Puede que haya una fracción de segundo con hero azul sólido (bloque vacío) antes del real — eso es esperado, es la ventana del snapshot Firestore. Anterior: 2026-07-03 por coordinator (cierre de los 2 bloqueos que Jorge resolvió por fuera) — **ambos bloqueos de la pasada 59 CERRADOS sin escribir código:** SPRINT-CONFIG-WEB-STATS-HOMEPAGE ✅ (Jorge editó a mano en Admin → Página Web, HomePage muestra "16 años / 20,000+ servicios" verificado en producción) y SPRINT-WA-QA-PLANTILLAS-INBOX ✅ (Jorge mandó `recordatorio_mantenimiento` a su número, llegó con imagen branded + botón "Leer más" + ✓✓ — confirma que el envío actual funciona y los ⚠️ del inbox son residuo histórico pre-fix `0ab73c5`). Cero acciones nuevas para Jorge de esta pasada. Anterior: 2026-07-02 por coordinator autónomo (pasada 59, revisión funcional en vivo de Jorge desde sidepanel Claude en producción) — **4 fixes autónomos commiteados + pusheados (S1 filtro fecha, S4 copy conduce, S5 nombres clientes, S6 espacio label agendar) + 2 bloqueos anotados (S2 QA plantillas WA, S3 stats HomePage) — ambos bloqueos ya cerrados por Jorge el 2026-07-02 antes de esta actualización.** Hashes en producción:
- **S1 filtro fecha** hash `60e7bd5` — `FiltroAvanzadoFinanzas` default cambia de "mes actual" a "últimos 30 días" para `pagina='cotizaciones'` y `pagina='facturacion-pendiente'`. `Facturas` conserva "mes actual" (KPIs mensuales). El filtro guardado del usuario (`storage.ultimoFiltro`) sigue teniendo prioridad — no rompe filtros ya guardados. Antes Cotizaciones mostraba "0 de 3" al entrar en el día 1 del mes.
- **S4 copy conduce** hash `9902e37` — En `FacturacionPendiente.tsx` 3 hits de "conduce de garantía" → "conduce" (subtítulo, empty state, mensaje sin permisos). Distingue de `/admin/conduces-garantia` que es menú aparte.
- **S5 nombres clientes** hash `c61dad7` — En `Clientes.tsx` lista lateral (`w-full lg:w-1/3`): `truncate` → `break-words leading-snug` en el nombre. Ahora "Juan Carlos" no se corta a "Jua...". El teléfono/zona sigue `truncate` (info secundaria).
- **S6 label agendar** hash `ded9963` — En `FormularioAgendarPublico.tsx` label "Dirección\n<span>" → "Dirección{' '}<span>". Espacio real antes del paréntesis "(Busca en Google...)".

**Bloqueos anotados en `BLOQUEOS.md` — AMBOS CERRADOS por Jorge el 2026-07-02 (dentro del mismo día que se abrieron):**
- ~~**SPRINT-WA-QA-PLANTILLAS-INBOX**~~ ✅ **COMPLETADO 2026-07-02** — Jorge probó `recordatorio_mantenimiento` a su propio número, llegó con imagen branded + botón "Leer más" + doble check ✓✓. Los ⚠️ del inbox son residuo histórico pre-fix `SPRINT-WA-FIX-PLANTILLAS-PARAMS` (hash `0ab73c5`). NO se tocó código.
- ~~**SPRINT-CONFIG-WEB-STATS-HOMEPAGE**~~ ✅ **COMPLETADO 2026-07-02 `opcion=manual hecho`** — Jorge editó a mano desde Admin → Página Web. HomePage ahora muestra "16 años / 20,000+ servicios", verificado en vivo en producción. NO se tocó código.

**Cazadores 25/25 PASS en cada commit.** Typecheck + lint limpios (pre-commit hook PASS sin bypass). 4 commits chicos, un cambio = un commit claro. **Cero acciones pendientes para Jorge de esta pasada.**

Anterior: 2026-06-03 por coordinator autónomo (pasada 58, post-OK Jorge `OK: jorge 2026-06-03` para SPRINT-DISENO-I Fase 3) — **1 sprint commiteado + pusheado (DISENO-I Fase 3), awaiting QA Jorge.** Hash en producción: `5ca35d2` (I limpieza dashboard — 4 widgets analíticos movidos a NUEVA `/admin/reporte-avanzado` sin borrar). Decisión literal Jorge: "Dejá todos los números operativos del día como están, y mové a un reporte aparte (sin borrar nada, que queden a un clic) estos 4: Reparaciones por Tipo de Equipo, Órdenes anuladas esta semana, Nómina proyectada del mes, Rendimiento por Técnico". Archivos: `src/pages/ReporteAvanzado.tsx` (NUEVO 411 líneas), `Dashboard.tsx` (4 useMemo + 4 widgets removidos + CTA "Ver reporte avanzado →" agregado), `App.tsx` (lazy + ruta RolRoute admin+coord), `Sidebar.tsx` (item en Finanzas con `BarChart3`). 25/25 cazadores PASS, typecheck PASS, lint clean, build 4.55s. Pre-commit hook PASS sin bypass. Vercel hook backup `xKdKcyToJ8s8Zue4MxGb` PENDING (webhook GitHub auto-deploy también activo). Para Jorge: **1 acción nueva** — QA visual (~3 min): hard-refresh `/admin/dashboard` (los 4 widgets analíticos ya NO aparecen + link discreto "Ver reporte avanzado →" visible al final del bloque "Equipo y trabajos") + click link → abre `/admin/reporte-avanzado` con los 4 widgets renderizando datos reales (Rendimiento por técnico con barras, Reparaciones por tipo con barras top 10, Anuladas semana con 3 contadores, Nómina proyectada del mes con sueldos/comisiones/bonos). Si OK → `QA: jorge YYYY-MM-DD HH:MM DISENO-I PASS` en BLOQUEOS.md. Anterior: 2026-06-01 por coordinator autónomo (`trabaja`, pasada 57) — **4 sprints commiteados + pusheados, 1 escalado Fase 2 a Jorge.** Hashes en producción: `4ce1a57` (E fuente Inter → Plus Jakarta Sans, awaiting QA), `45b6b38` (F text-wrap pretty global, COMPLETADO sin QA por spec), `6fab6e9` (G sub-regla CLAUDE.md "no inventar contenido", COMPLETADO sin QA por spec), `cc673b0` (H border-left de cards de orden quitado → badge arriba-derecha; banners preservados con tag `@safe-border-left-accent`; awaiting QA), `1177ec2` (I Fase 1 inventario data-slop Dashboard generado + escalación Fase 2 en BLOQUEOS.md). 25/25 cazadores PASS en cada commit, 5 builds 4.16-4.64s, pre-commit hooks PASS sin bypass, working tree limpio (~50 untracked legítimos NO incluidos). Para Jorge: **2 acciones nuevas** — (a) QA visual de E + H (~5 min hard-refresh las pantallas listadas en COLA awaiting-QA), (b) revisar `docs/sprints/INVENTARIO_KPI_DASHBOARD_2026-06-01.md` y marcar MANTENER/QUITAR/MOVER en cada KPI (~10 min), agregar `OK: jorge ...` en BLOQUEOS, correr `procesa bloqueos`. Anterior: 2026-06-01 por Cowork (conversación) — **5 sprints PENDIENTES agregados al final de COLA_AUTONOMA.md (`SPRINT-DISENO-E/F/G/H/I`) tras leer leak prompt de diseño interno de Claude (`asgeirtj/system_prompts_leaks/Anthropic/claude-design.md`).** Jorge eligió "todo lo presentado" + **fuente Plus Jakarta Sans** (vía AskUserQuestion). Mejoras encoladas en orden recomendado al coordinator: **E** fuente Inter → Plus Jakarta Sans [🟠 ALTA, [NO CERRAR sin QA Jorge — afecta apariencia visible]]; **F** `text-wrap: pretty` global en `.text-h1/.text-h2/.text-body` [🟢 BAJA, sin QA dedicado]; **G** sub-regla CLAUDE.md "No inventar contenido sin OK Jorge" [🟠 ALTA, sin QA dedicado] — antiprecedente DISENO-D pasada 56 donde el coordinator iba a inventar 3 testimonios ficticios; **H** quitar accent `border-left` de cards de orden (TecnicoVista/Ordenes/OrdenesTablero/AgendaDia) → badge arriba-derecha [🟡 MEDIA, [NO CERRAR sin QA Jorge en celular real + admin]]; **I** auditoría data-slop del Dashboard en 2 fases con ESCALA a Jorge para marcar KPIs MANTENER/QUITAR/MOVER [🟡 MEDIA-ESCALABLE]. Todos autónomos (no tocan rules/pagos/endpoints/migraciones). Sin commits de código en esta conversación (solo edita COLA + MEMORIA). **Para procesar:** Jorge corre `trabaja` en Claude Code; coordinator procesa E primero (afecta visualmente el resto), después F+G+H autónomos sin pausa, I queda escalable. Anterior: 2026-05-31 por coordinator autónomo (`procesa bloqueos`, pasada 56) — **2 sprints desbloqueados por Jorge y procesados autónomo: SPRINT-DISENO-C hash `68a203f` y SPRINT-DISENO-D hash `4347149`.** Ambos en producción awaiting QA Jorge:
- **DISENO-C** — KPI hero "Órdenes atrasadas" gigante arriba del Dashboard (decisión `opcion=C`) + 3 bloques (Hoy / Pipeline / Plata / "Equipo y trabajos") con headers visuales + skeletons reusables en Dashboard, Ordenes, Inbox (NUEVO `src/components/Skeleton.tsx`) + microcopy dominicano ("SLA >24h" → "Atrasadas más de 1 día"; banner técnico "La oficina está revisando el precio" reemplaza "Esperando aprobación del precio por operaciones"; siguientePaso "mandar a facturar"). 6 archivos.
- **DISENO-D** — CTA HomePage "Lo arreglamos en tu casa, el mismo día" (decisión Jorge: comunica las 2 promesas reales — a domicilio + rápido, sin sugerir retiro+devolución) + NUEVO componente `<SeccionTestimonios>` con flag `activo: false` en `src/config/testimoniosHomePage.ts` (oculto en prod, visible solo en DEV con placeholders dashed; cuando Jorge tenga 3 testimonios reales edita config + flag a true + push) + NUEVO `<EmptyState>` reusable montado en Inbox/Citas/FacturacionPendiente + 2 stats reales (16 años + 20K+ servicios; quitadas 98% satisfacción + 24h respuesta + 5 estrellas) + AgendarPage respiro vertical + font-extrabold→font-bold en 3 hits de heading + hover:-translate-y-1 quitado de cards. 8 archivos.

Cazadores 25/25 PASS en cada commit. Typecheck + lint clean. Builds 4.28s + 4.23s. Pre-commit hooks PASS sin bypass. Webhook Vercel disparado (hook backup CLAUDE.md `dqfSS3mCJK` → 201; el hook del system prompt `kfkia6Sqin` devolvió 404 — actualizar prompt si Jorge quiere consistencia). Working tree limpio (~50 untracked legítimos NO incluidos). Pasada 56 cerrada en docs simultáneo: DIARIO_2026-05-31.md append + BLOQUEOS.md marca C y D como ✅ DESBLOQUEADO Y PROCESADO + COLA_AUTONOMA.md marca C y D como 🟡 EN PRODUCCIÓN AWAITING QA JORGE.

**Para Jorge — 3 acciones en orden de prioridad:**
1. **QA visual rápida pasada 57** (~5 min): hard-refresh `/`, `/admin/dashboard`, `/admin/ordenes`, `/tecnico` (en celular real) — la tipografía debe verse visiblemente distinta (Plus Jakarta Sans, más personalidad que Inter) y las cards de orden deben mostrar el estado con badge arriba-derecha en vez del border-left de color. Si OK → `QA: jorge YYYY-MM-DD DISENO-E PASS` + `QA: jorge YYYY-MM-DD DISENO-H PASS` en BLOQUEOS.md.
2. **Revisar inventario data-slop Dashboard** (~10 min): abrí `docs/sprints/INVENTARIO_KPI_DASHBOARD_2026-06-01.md`, marcá MANTENER/QUITAR/MOVER en cada KPI. Agregá línea `OK: jorge ...` en `BLOQUEOS.md` con el detalle. Corré `procesa bloqueos` — el coordinator hará Fase 3 (aplicar cambios) autónomo.
3. **QA pendientes heredados** (~20 min, opcional): DISENO-C, D, A.1/A.2/A.3/A.4/A.5/B + WIZARD-FASES-FREEZE + DISENO-TECNICO-FASE-1 + DINERO-2 + REPORTING-1 + GARANTIA Fase A + WA-FIX-PLANTILLAS. Detalle en `BLOQUEOS.md`.

Anterior: 2026-05-31 por coordinator autónomo (`trabaja noche larga`, pasada 55) — **5 sprints commiteados + pusheados (A.2 → A.5 + B), 2 ESCALADOS a BLOQUEOS (C y D por decisión de Jorge).** 5 hashes en producción awaiting QA Jorge:
- **A.2 sin emojis decorativos** hash `c52f273` — 12 archivos, JSX admin sin emojis (toasts/plantillas WA preservados con tag).
- **A.3 escala tipográfica global** hash `cbd6910` — 5 clases `.text-h1/h2/body/caption/micro` en `index.css` (NO migra componentes, solo establece la escala).
- **A.4 sin gradientes saturados** hash `7256ee2` — 16 archivos, gradients admin → sólido, heros públicos + splash + AsistenteIA preservados con `@safe-gradient`.
- **A.5 sin colores random íconos** hash `099a2de` — HomePage 7 tipos equipo unificados a brand, Dashboard 3 KPIs neutros a brand (Ingresos preserva verde por semántica positiva).
- **B accesibilidad técnico** hash `debaff6` — TecnicoVista botones 48px + sin text-[10px]/[11px] + opacity→text-white/N% explícito. 11 handlers críticos intactos.

Sub-fixes lint preexistentes mínimos cerrados en pasada: Calendarios.tsx (X unused), TrackingCliente.tsx (clienteId), Login.tsx (catch any), ServiciosPage.tsx (Phone unused). El resto de la deuda A.5-LINT queda en sprint propio.

~~**ESCALADOS a BLOQUEOS** (esperando OK de Jorge):~~ **[RESUELTO en pasada 56 el 2026-05-31]** — Jorge desbloqueó ambos con OKs explícitos y el coordinator los procesó autónomo en `procesa bloqueos`.
- ~~**SPRINT-DISENO-C-DASHBOARD-REDUCIDO**~~ → ✅ hash `68a203f` (opción C "Órdenes atrasadas" como KPI hero).
- ~~**SPRINT-DISENO-D-PUBLICAS-DOMINICANO**~~ → ✅ hash `4347149` (CTA custom "Lo arreglamos en tu casa, el mismo día" + testimonios placeholder ocultos en prod).

Cazadores 25/25 PASS en CADA commit. Typecheck PASS. Lint clean. 5 builds verificados (4.10-4.26s). 5 pre-commit hooks PASS sin bypass. Working tree limpio (~50 untracked legítimos NO incluidos). Anterior: 2026-05-30 por coordinator autónomo (`procesa bloqueos` + camino 2, pasada 54) — **`SPRINT-DISENO-CIERRE-COMPLETO` FASE A.1 ejecutada en producción, awaiting QA Jorge.** 11 commits (1 chore `f861da9` + 10 chunks `fbb77d1`→`74a69fb`), 90 archivos tocados, 857 líneas reemplazadas simétricamente (`#0f3460`→`primary`, `#1a5fa8`→`primary-medium`). Build 4.21s PASS, cazadores 25/25 en cada commit. **7 archivos en deuda lateral** (warnings ESLint preexistentes no relacionados a A.1) → capturados como `SPRINT-DISENO-A.5-LINT-LIMPIEZA`. **FASES A.2/A.3/A.4/A.5/B/C/D re-encoladas** como sprints separados al final de COLA. QA Jorge esperado: hard-refresh 6 pantallas, los azules deben verse idénticos (cero cambio visual). Si OK → `QA: jorge 2026-05-30 HH:MM DISENO-A.1 PASS`. Detalle: `DIARIO_2026-05-30.md`. Anterior: 2026-05-30 por Cowork — `SPRINT-DISENO-CIERRE-COMPLETO` **DESBLOQUEADO con `OK: jorge 2026-05-30 opcion=B`** en `BLOQUEOS.md` (limpieza invisible: `#0f3460` → `primary`, `#1a5fa8` → `primary-medium`; cero cambio visual de marca). Coordinator cazó error de spec original de Cowork (decía `brand-800` cuando debía decir `primary` — `brand-800` mapea a otro color, `#283B5A`). Resto del sprint (sin emojis, escala tipográfica, sin gradientes saturados, FASES B/C/D) sin cambios. Rebranding a paleta `brand-*` queda como sprint propio futuro. **Para procesar:** Jorge corre `procesa bloqueos` en Claude Code. Anterior: 2026-05-30 por coordinator autónomo (`trabaja`, pasada 53) — **NO-OP, 0 commits de código, 1 sprint ESCALADO a BLOQUEOS.** `SPRINT-DISENO-CIERRE-COMPLETO` (único PENDIENTE en cola al tope) ⊘ ESCALADO por discrepancia técnica load-bearing: la spec afirma `brand-800: #0f3460` semánticamente idéntico, pero `tailwind.config.js` define `brand-800 = #283B5A` (azul desaturado distinto al `#0f3460` actual que es `primary.DEFAULT`). El find&replace literal cambiaría visualmente el azul de toda la marca en 101 archivos — incompatible con el propio criterio del sprint ("visualmente NADA cambia"). 3 opciones en `BLOQUEOS.md` (A migrar a brand / B unificar tokens a `primary` sin cambio visual / C saltear FASE A). Recomendación coordinator: opción B. Resto de la cola ya estaba COMPLETADO o awaiting QA Jorge (DINERO-2, REPORTING-1, GARANTIA Fase A, WA-FIX-PLANTILLAS, WIZARD-FASES-FREEZE, DISENO-TECNICO-FASE-1). Anterior: 2026-05-30 por Cowork — **`SPRINT-DISENO-CIERRE-COMPLETO` agregado al TOPE de la cola** (4 fases secuenciales autónomas que cierran los 12 items pendientes del roadmap del diseñador `docs/AUDITORIA_DISENO_SENIOR_2026-05-29.md`). Jorge eligió "todo el front-end del software" + "un sprint grande que termina todo". Estructura: FASE A fundamentos invisibles (colores hardcoded → tokens brand, sin emojis, escala tipográfica, sin gradientes saturados) → FASE B accesibilidad táctil del técnico (botones 48px, sin text-[10px]) → FASE C Dashboard reducido a 3 secciones (Hoy/Pipeline/Plata) + skeletons + microcopy dominicano → FASE D HomePage con testimonios + CTA dominicano + empty states. [NO CERRAR sin QA Jorge final ~10 min recorriendo 6 pantallas]. Para procesar: Jorge corre `trabaja` cuando termine la QA de los 2 sprints pendientes (WIZARD-FASES-FREEZE + DISENO-TECNICO-FASE-1). Anterior: 2026-05-30 por coordinator autónomo (`trabaja`, pasada 52). **2 sprints procesados, ambos en producción awaiting QA Jorge.** (1) **SPRINT-WIZARD-FASES-FREEZE** hash `a02a047` — diagnóstico del coordinator: el `window.confirm()` del handler de avance de fase en `FaseStepper.tsx` se cuelga indefinidamente bajo Playwright sin `page.on('dialog')` handler (artefacto del QA automatizado de Cowork con `.playwright-mcp/`). Fix: reemplazado por Modal propio consistente con el modal de retroceso ya existente. 4 consumidores verificados sin regresión. Cuando Jorge confirme en navegador real → DINERO-2 puede retomar QA visual. (2) **SPRINT-DISENO-TECNICO-FASE-1** hash `4c21dc9` — reordena JSX en `TecnicoVista.tsx`: saludo al header, ganancias abajo del listado, etiqueta "PRÓXIMA CITA", tabs al final, mapa colapsado. 11 handlers intactos. Sub-regla "cleanup en TecnicoVista requiere QA flujo X validado" cumplida (los handlers no se tocaron). Cazadores 25/25 PASS, build 4.50s. Cola limpia tras estos 2 — sin más procesables. Anterior: 2026-05-30 Cowork — intentó QA visual DINERO-2 directo desde Chrome, cazó bug del wizard, encoló SPRINT-WIZARD-FASES-FREEZE.

---

## ⏳ PENDIENTE AHORA

### En la cola autónoma (Jorge corre `trabaja` en Claude Code) — en orden

**🟠 AL TOPE — sprints en cola:**

- **`SPRINT-DISENO-A.5-LINT-LIMPIEZA`** — cierra deuda de A.1 (4 archivos con warnings preexistentes restantes: ModalEditarPiezasOrden, Gastos, ConfiguracionWeb, Solicitudes) + completa find&replace incluyendo modificadores `hover:`/`focus:`/opacidad. Autónomo. **Nota pasada 55:** ya cerrados warnings en Calendarios.tsx + TrackingCliente.tsx + Login.tsx + ServiciosPage.tsx durante A.2/A.4.

**🟢 PROCESADO pasada 57+58 (2026-06-01 + 2026-06-03) — bloque DISENO E/F/G/H/I del leak prompt diseño Claude:**

- **`SPRINT-DISENO-E-FUENTE-PLUS-JAKARTA`** ✅ hash `4ce1a57` — awaiting QA Jorge.
- **`SPRINT-DISENO-F-TEXT-WRAP-PRETTY`** ✅ COMPLETADO hash `45b6b38` — sin QA dedicado por spec.
- **`SPRINT-DISENO-G-NO-INVENTAR-CONTENIDO`** ✅ COMPLETADO hash `6fab6e9` — sub-regla activa en CLAUDE.md + coordinator + builder.
- **`SPRINT-DISENO-H-BORDER-LEFT-A-BADGE`** ✅ hash `cc673b0` — awaiting QA Jorge en celular real + admin.
- **`SPRINT-DISENO-I-DATA-SLOP-DASHBOARD-AUDIT`** ✅ Fase 1 hash `1177ec2` + Fase 3 hash `5ca35d2` (pasada 58 post-OK Jorge 2026-06-03) — awaiting QA Jorge: hard-refresh `/admin/dashboard` (4 widgets analíticos ya no aparecen, link "Ver reporte avanzado →" visible) + click → `/admin/reporte-avanzado` renderiza los 4 widgets. Decisión literal: "mové a reporte aparte sin borrar, queden a un clic".

Origen completo: `https://github.com/asgeirtj/system_prompts_leaks/blob/main/Anthropic/claude-design.md`.

**🟡 EN PRODUCCIÓN AWAITING QA JORGE (pasada 58, 2026-06-03):**

- **`SPRINT-DISENO-I-DATA-SLOP-DASHBOARD-AUDIT`** Fase 3 hash `5ca35d2` — 4 widgets analíticos movidos del Dashboard a NUEVA `/admin/reporte-avanzado` (sin borrar). Decisión Jorge literal: "queden a un clic". **QA Jorge (~3 min):** hard-refresh `/admin/dashboard` (Rendimiento por Técnico + Reparaciones por Tipo + Anuladas semana + Nómina proyectada del mes ya NO aparecen + link "Ver reporte avanzado →" visible al final del bloque "Equipo y trabajos") + click link → abre `/admin/reporte-avanzado` con los 4 widgets renderizando datos reales. Sidebar también muestra entrada "Reporte avanzado" en sección Finanzas. Si OK → `QA: jorge YYYY-MM-DD HH:MM DISENO-I PASS`.

**🟡 EN PRODUCCIÓN AWAITING QA JORGE (pasada 57, 2026-06-01):**

- **`SPRINT-DISENO-E-FUENTE-PLUS-JAKARTA`** hash `4ce1a57` — Inter → Plus Jakarta Sans. **QA:** hard-refresh `/`, `/admin/dashboard`, `/admin/ordenes`, `/tecnico`, `/admin/clientes`, `/admin/inbox`, `/agendar`. Debe verse más personalidad sin perder legibilidad.
- **`SPRINT-DISENO-H-BORDER-LEFT-A-BADGE`** hash `cc673b0` — border-left de cards de orden quitado, badge arriba-derecha. **QA en celular real:** `/tecnico` + `/admin/ordenes` + `/admin/agenda` + tablero Kanban — badge legible, tap targets intactos.

**🟡 EN PRODUCCIÓN AWAITING QA JORGE (pasada 56, 2026-05-31 tarde):**

- **`SPRINT-DISENO-C-DASHBOARD-REDUCIDO`** hash `68a203f` — KPI hero "Órdenes atrasadas" + 3 bloques + skeletons + microcopy. **QA:** hard-refresh `/admin/dashboard` (KPI hero visible + 3 bloques con headers) + `/admin/ordenes` (skeleton al recargar) + `/admin/inbox` (skeletons + EmptyState) + `/tecnico` (banner "La oficina está revisando el precio").
- **`SPRINT-DISENO-D-PUBLICAS-DOMINICANO`** hash `4347149` — CTA "Lo arreglamos en tu casa, el mismo día" + componente testimonios placeholder + EmptyState. **QA:** hard-refresh `/` (CTA nuevo + 2 stats + sin testimonios visibles en prod) + `/agendar` (respiro vertical) + `/admin/inbox`/`/admin/citas`/`/admin/facturacion-pendiente` (EmptyState cuando vacíos).

**🟡 EN PRODUCCIÓN AWAITING QA JORGE (pasada 55, 2026-05-31, noche larga):**

- **`SPRINT-DISENO-A.2-EMOJIS`** hash `c52f273` — 12 archivos, emojis decorativos JSX admin → iconos lucide. Toasts/plantillas WA preservados. **QA:** hard-refresh /admin/ordenes /admin/agenda /admin/clientes /admin/mantenimiento /admin/calendarios /tecnico — los emojis deben desaparecer del admin pero los iconos lucide se ven en su lugar.
- **`SPRINT-DISENO-A.3-ESCALA-TIPOGRAFICA`** hash `cbd6910` — 5 clases globales `.text-h1/h2/body/caption/micro` en `index.css`. NO migra. **QA:** verificar que el sitio se ve EXACTAMENTE igual (clases no usadas todavía).
- **`SPRINT-DISENO-A.4-GRADIENTES-SATURADOS`** hash `7256ee2` — 16 archivos, gradients admin → sólido. Heros públicos + splash + AsistenteIA preservados. **QA:** Dashboard barras se ven más planas; HomePage hero sigue con gradient azul; Login splash sutil sigue igual.
- **`SPRINT-DISENO-A.5-COLORES-ICONOS`** hash `099a2de` — HomePage 7 colores random tipos equipo → brand uniforme; Dashboard 3 KPIs neutros → brand (Ingresos preserva verde). **QA:** HomePage muestra los íconos de servicios todos azul brand; Dashboard 3 cards azul + 1 verde "Ingresos del Mes".
- **`SPRINT-DISENO-B-TECNICO-ACCESIBILIDAD`** hash `debaff6` — TecnicoVista botones 48px + sin text-[10px]/[11px]. 11 handlers intactos. **QA Jorge EN CELULAR REAL:** abrir `/tecnico`, recorrer flujo Iniciar/Completar/Standby/Avisar — botones grandes, texto legible.

**🟡 EN PRODUCCIÓN AWAITING QA JORGE (pasada 54, 2026-05-30):**

- **`SPRINT-DISENO-CIERRE-COMPLETO` FASE A.1** — código en producción, 90 archivos, 11 commits (chore `f861da9` + chunks `fbb77d1` → `74a69fb`). Cero cambio visual esperado. **QA Jorge:** hard-refresh `/`, `/admin/dashboard`, `/admin/ordenes`, `/tecnico`, `/admin/clientes`, `/admin/inbox` — azules deben verse idénticos. Si OK → `QA: jorge YYYY-MM-DD HH:MM DISENO-A.1 PASS`. Stub original del sprint completo permanece en `BLOQUEOS.md` para forensia.

**🟡 EN PRODUCCIÓN AWAITING QA JORGE (pasada 52, 2026-05-30):**

- **`SPRINT-WIZARD-FASES-FREEZE`** hash `a02a047` — fix aplicado: `window.confirm` → Modal propio. Causa raíz: Playwright sin handler de dialog (no era bug del producto para usuarios reales). QA Jorge: avanzar fase desde modal de orden → debe abrir modal de confirmación rápido sin congelar.
- **`SPRINT-DISENO-TECNICO-FASE-1`** hash `4c21dc9` — jerarquía visual de `/tecnico` reordenada: saludo al header, ganancias abajo, "PRÓXIMA CITA" antes de primera cita activa, tabs al final, mapa colapsado. 11 handlers intactos. QA Jorge: abrir `/tecnico` desde celular, recorrer checklist sección 6 del spec `docs/specs/REDISENO_TECNICO_MOVIL_2026-05-29.md`.

**⭐ BLOQUE FLUJO-DEPENDENCIAS — estado post pasada 51 (2026-05-25 nocturno).** De la auditoría `AUDITORIA_FLUJO_DEPENDENCIAS_2026-05-25.md`. Causa raíz: una orden podía nacer sin cliente real. **Estado actual:**

  1. ~~**AGENDA-1-MANTENIMIENTO-ATA-CLIENTE**~~ ✅ **VERIFICADO 2026-05-29** por `/qa` de gstack (QA estático de código): typeahead exige cliente real (Mantenimiento.tsx:459-507 + :217-223 `buscarOCrearCliente`), orden hereda `clienteId` (:310-315 + guard :276-282), ficha cliente la lista (Clientes.tsx:172-186). **+ NUEVO cazador P-025** (hash `b065e4a`) bloquea reintroducción de `clienteId: ''` en writes de `mantenimiento`/`ordenes_servicio`. 24/24 cazadores PASS.
  2. **AGENDA-2-CALENDARIO-MUESTRA-CITAS** ✅ COMPLETADO hash `e4f92bf`.
  3. **AGENDA-3-HONRAR-TECNICO-ASIGNADO** ✅ COMPLETADO hash `f9697b9`.
  4. **AGENDA-4-UNIFICAR-FORMS-PUBLICOS** ✅ COMPLETADO hash `fba51a4`.
  5. **AGENDA-5-PROXIMO-MANTENIMIENTO-AL-CERRAR** ✅ COMPLETADO hash `8f6a72b`.
  6. **NUCLEO-CREAR-ORDEN-CENTRAL** ⊘ ESCALADO a BLOQUEOS (cimiento sensible + [NO CERRAR sin QA]; plan 3 fases + decisión A/B/C en BLOQUEOS.md).
  7. **DINERO-1-QT-ATOMICO** ✅ COMPLETADO hash `bec87b3`.
  8. **DINERO-2-MONTOPAGADO-RECALC** 🟡 hash `b4fc23c` — **QA estático ✅ por `/qa-only` 2026-05-29** (recalc dentro de runTransaction, guard idempotencia por `pago.id`, suma con cascada precioFinal/Aprobado/Sugerido, estado mismo helper que RegistrarPagoModal, gate P-023 intacto). **QA visual ⏸ BLOQUEADO 2026-05-30 por bug del wizard** (clic "En Cotización" congela navegador — ver SPRINT-WIZARD-FASES-FREEZE al tope). Cuando el wizard funcione, Cowork retoma: avanzar OS-0057 → cotizar RD$1500 → registrar pago parcial RD$500 → emitir conduce cobrando saldo RD$1000 → verificar orden pasa a "Pagado". Observación menor (deuda): `calcularEstadoPago` está duplicado en RegistrarPagoModal y ProcesarFacturacionModal; extraer a `utils/pagos.ts` cuando aparezca 3er caller.
  9. **REPORTING-1-KPI-HELPERS** 🟡 hash `a4e64db` — **QA estático ✅ por `/qa-only` 2026-05-29** (3 helpers en `src/utils/kpis.ts`: `ingresosFacturasPagadas` filtra `estado==='pagada'`, `conducesEmitidosMonto/Count` filtran `estado!=='anulada'` defense-in-depth, Dashboard usa los 3 + filter inline "Conduces hoy" también excluye anuladas, rangos `>=desde, <=hasta`). **Falta validación visual Jorge:** comparar KPI "Conduces emitidos del mes" antes/después si hay anuladas históricas en el mes.

  **FUERA de la cola (decisión de Jorge, en `BLOQUEOS.md`):** base de comisión (dos cálculos, ~18% dif), gate de aprobación + rule R4, descuento de stock al cerrar, standby→inventario, factura+pago→fase cerrado.

  **Follow-up suelto:** SPRINT-WA-AUTORESPUESTA-SIN-HEADER (toca `api/` → ESCALA).

### En BLOQUEOS esperando QA o OK de Jorge

- ~~**Pasada 57 (1 sprint Fase 2 escalado):**~~ **[RESUELTO en pasada 58 el 2026-06-03]** — Jorge dio OK literal "mové los 4 widgets a reporte aparte sin borrar, que queden a un clic". Coordinator procesó autónomo. Hash `5ca35d2`. Sprint sigue [NO CERRAR sin QA].
- **Pasada 51 nocturno (3 sprints awaiting QA):**
  - ~~**SPRINT-AGENDA-1**~~ ✅ **CERRADO 2026-05-29** por `/qa` de gstack (auditoría estática) + cazador P-025 vivo. NO necesita QA manual.
  - **SPRINT-DINERO-2** hash `b4fc23c` — **QA estático ✅ 2026-05-29.** Falta validación visual Jorge: emitir conduce cobrando saldo dentro del wizard, verificar que la orden muestra "Pagado" en agenda/listado (no "Pendiente"+monto viejo). Plan en `BLOQUEOS.md`.
  - **SPRINT-REPORTING-1** hash `a4e64db` — **QA estático ✅ 2026-05-29.** Falta validación visual Jorge: comparar KPI "Conduces emitidos del mes" en Dashboard antes/después si hay anuladas en el mes. Debe bajar por el monto de las anuladas. Plan en `BLOQUEOS.md`.
- **SPRINT-NUCLEO-CREAR-ORDEN-CENTRAL** — ⊘ ESCALADO 2026-05-25 pasada 51. Plan 3 fases (helper crearOrden + migrar solicitudes.service + opcional migrar useOrdenCreateForm + cazador anti-bypass) + decisión A/B/C en `BLOQUEOS.md`. Para desbloquear: agregar `OK: jorge YYYY-MM-DD opcion=A|B|C`.
- **SPRINT-WA-FIX-PLANTILLAS-PARAMS** — código commiteado + pusheado (hash `0ab73c5` 2026-05-25 pasada 50). Frontend-only; deploy automático (job Vercel `Yr0nTylm03jpzaalsPhd`). **QA Jorge:** desde el inbox, mandar cada una de las 4 plantillas a un número de prueba y confirmar ✓✓ (entregado) + banner branded correcto (no ⚠️). Hallazgo lateral documentado como follow-up: `SPRINT-WA-AUTORESPUESTA-SIN-HEADER`.
- **SPRINT-GARANTIA-FLUJO-COMPLETO** — ⏸ código FASE A commiteado + pusheado (hash `59c5fb0` 2026-05-25 pasada 49). Aplica las reglas de Jorge: técnico original conserva su comisión, descuento del 10% sobre piezas al cerrar la orden de garantía (no al confirmar cita), cazador P-024 anti-reintroducción del patrón viejo. **NO marcado COMPLETADO** — Jorge debe correr 4 casos QA (mismo técnico cubre / otro técnico cubre / sin piezas / sin comisión original). Plan QA + deuda fase B en `BLOQUEOS.md`. Deuda fase B: botón "Abrir garantía" desde orden/ficha, gate "solo oficina no técnicos", capturar si el cliente paga, notifs al reabrir, regla "mismo técnico que cubre no gana comisión adicional".

### Esperando acción manual de Jorge (Cowork NO puede hacerlas)

- ~~**DRY-RUN del script de migración B-2**~~ **[HECHO 2026-05-25 por Jorge]** — corrió DRY-RUN (16 órdenes, 18 pagos) + `--apply`: subcolección poblada (16 órdenes migradas, 18 pagos escritos, arrays `orden.pagos` intactos = source-of-truth). <500 → aplicado autónomo según spec. **B-3 (cut-over + endurecer rules) sigue esperando QA de Jorge.**
- **QA SPRINT-GARANTIA Fase A** en producción/staging — 4 casos detallados en `BLOQUEOS.md`. Hash `59c5fb0`.
- ~~**QA SPRINT-FIX-LEADS-FORMULARIO-PUBLICO**~~ ✅ **CERRADO 2026-05-29** por `/qa-only` (storage.rules con whitelist `image/*` + `application/pdf` < 10MB intacta, REGLA DE ORO cumplida, `solicitudes.service.ts` migrada al path `solicitudes-publico/`, FormularioPublico.tsx GPS leído por `campo.tipo==='ubicacion'` con type guard refinado, deploy verificado por P-013 lock match sha `accf5550e87...`). NO necesita QA visual adicional.
- **Smoke test en producción** — selector de número con números reales, trazabilidad (quién envió + nombre del agente), respuestas rápidas con "/", y el inbox (fotos, ficha cliente, form a la izquierda).
- **Crear 2da/3ra WABA en Meta** + cargar `phone_number_id` + token en Vercel env + allowlist → desbloquea `SPRINT-WA-NUMERO-RESPALDO-MANUAL-FASE-2`.
- **Bug `/careful` de gstack (FASE A paquete integrado, 2026-05-29).** El hook PreToolUse:Bash busca `/bin/check-careful.sh` (ruta absoluta de sistema) que no existe → el hook falla con "non-blocking status" y `rm -rf` se ejecuta sin pedir confirmación. Verificado con prueba en `~/Desktop/prueba-gstack`. `/careful` se activa correctamente y Claude conoce los patrones, pero la red de seguridad automática del hook está rota. Opciones a evaluar: (a) `/gstack-upgrade` cuando salga versión nueva, (b) reportar al GitHub `garrytan/gstack` con el detalle del path, (c) saltear los cinturones y pasar directo a FASE B (`/qa` con navegador). Plan integrado: `docs/PLAN_INTEGRADO_GSTACK_2026-05-28.md`.

---

## 🔧 EN CURSO

Nada activo en construcción ahora mismo. Pasada 51 cerrada (nocturna). 3 sprints del bloque FLUJO-DEPENDENCIAS + SPRINT-GARANTIA Fase A + SPRINT-WA-FIX-PLANTILLAS commiteados/pusheados esperando QA Jorge.

---

## ✅ HECHO RECIENTE (últimos hitos)

- **2026-06-03 (pasada 58, autónomo post-OK Jorge `OK: jorge 2026-06-03`)** — **SPRINT-DISENO-I-DATA-SLOP-DASHBOARD-AUDIT Fase 3 ejecutada en producción, awaiting QA Jorge.** Hash `5ca35d2`. Decisión literal Jorge: "Dejá todos los números operativos del día como están, y mové a un reporte aparte (sin borrar nada, que queden a un clic) estos 4: Reparaciones por Tipo de Equipo, Órdenes anuladas esta semana, Nómina proyectada del mes, Rendimiento por Técnico". Cambios: NUEVA `src/pages/ReporteAvanzado.tsx` (411 líneas, listeners autónomos para ordenes_servicio/facturas/personal/comisiones, los 4 useMemo copiados 1:1 desde Dashboard, gates por permiso preservados — puedeVerAnuladas/puedeVerNomina admin+coord); ruta `/admin/reporte-avanzado` en App.tsx con `RolRoute admin+coord` espejo del sidebar; item "Reporte avanzado" en Sidebar.tsx sección Finanzas con icono `BarChart3`; Dashboard.tsx limpiado (4 useMemo + 4 widgets JSX removidos + CTA "Ver reporte avanzado →" agregado al final del bloque "Equipo y trabajos" gateado admin+coord; imports `Wrench`/`XCircle`/`TrendingUp` quitados — quedaban unused; `Wallet` y `getTecnicoColor` se quedan, otros widgets los usan). Consumidores cross-archivo verificados: 0 referencias externas a los 4 memos en `src/` — movimiento mecánico seguro. Mejora UX lateral: "Anuladas semana" ahora tiene empty state ("Sin anulaciones esta semana") en el reporte; antes la card simplemente desaparecía cuando total=0 (ruido) — ahora la página dedicada lo dice explícitamente. Cazadores 25/25 PASS. Typecheck PASS. Lint clean. Build 4.55s. Pre-commit hook PASS sin bypass. Push `e1fb7b4..5ca35d2`. Vercel hook backup `xKdKcyToJ8s8Zue4MxGb` PENDING. [NO CERRAR sin QA Jorge]. Plan QA: hard-refresh `/admin/dashboard` (4 widgets ya no aparecen + link discreto visible) + click → abre `/admin/reporte-avanzado` con 4 widgets renderizando datos reales.
- **2026-06-01 (Cowork — conversación)** — Tras leer leak prompt diseño Claude (2,005 líneas, contenido sustantivo en líneas 1-355), Jorge eligió "todo lo presentado" + fuente **Plus Jakarta Sans** (vía AskUserQuestion entre 3 opciones presentadas: Plus Jakarta Sans / DM Sans / Geist). **5 sprints PENDIENTES agregados al final de COLA_AUTONOMA.md (`SPRINT-DISENO-E/F/G/H/I`):** cambio de fuente (E, [NO CERRAR sin QA]), `text-wrap: pretty` global (F, trivial), sub-regla CLAUDE.md "no inventar contenido" (G — origen antiprecedente testimonios DISENO-D pasada 56), border-left accent → badge (H, [NO CERRAR sin QA en celular]), data-slop audit Dashboard en 2 fases con escala (I). Origen completo: `https://github.com/asgeirtj/system_prompts_leaks/blob/main/Anthropic/claude-design.md`. Sin commits de código en la conversación (solo edita COLA + MEMORIA). **Para procesar:** Jorge corre `trabaja` en Claude Code → coordinator procesa E primero (afecta visualmente el resto), después F+G+H autónomos sin pausa, I queda escalable para cuando Jorge tenga tiempo de marcar el inventario.
- **2026-05-31 (pasada 56, `procesa bloqueos`)** — **2 sprints desbloqueados + procesados autónomo: SPRINT-DISENO-C hash `68a203f` y SPRINT-DISENO-D hash `4347149`.** Ambos en producción awaiting QA Jorge:
  - **SPRINT-DISENO-C-DASHBOARD-REDUCIDO** hash `68a203f` — Decisión Jorge `opcion=C` (KPI hero = "Órdenes atrasadas"). Cambios: NUEVO componente `<KpiHeroAtrasadas>` dominante arriba del Dashboard (verde si 0, rojo si >0, con tabla embebida de las primeras 10 órdenes); NUEVO `<BloqueHeader>` que separa visualmente en 3 bloques (Hoy / Pipeline / Plata / "Equipo y trabajos"); NUEVO `src/components/Skeleton.tsx` (118 líneas) con primitivas + composites (KpiCard, SectionBlock, ConversacionRow, OrdenCard) montados en Dashboard, Ordenes, Inbox; microcopy dominicano: "SLA >24h" → subtítulo "Atrasadas más de 1 día. Atender lo antes posible.", TecnicoVista banner "Esperando aprobación del precio por operaciones" → "La oficina está revisando el precio" (con icono Clock), `siguientePaso.ts` "enviar a facturación" → "mandar a facturar". 6 archivos. 365 líneas reorganizadas en Dashboard.tsx (lógica intacta). [NO CERRAR sin QA Jorge].
  - **SPRINT-DISENO-D-PUBLICAS-DOMINICANO** hash `4347149` — Decisión Jorge `cta="Lo arreglamos en tu casa, el mismo día"` + `testimonios=placeholder-oculto-en-prod`. Cambios: CTA HomePage actualizado (comunica las 2 promesas reales — a domicilio + rápido, sin sugerir retiro+devolución); NUEVO `src/config/testimoniosHomePage.ts` (flag `activo: false` + 3 slots + JSDoc del flujo de activación); NUEVO `src/components/public/SeccionTestimonios.tsx` (en prod con activo=false renderiza null; en DEV renderiza placeholders dashed + banner "Vista previa"; cuando Jorge active aparece en prod); NUEVO `src/components/EmptyState.tsx` reusable montado en Inbox, Citas, FacturacionPendiente con copy dominicano; 2 stats reales (16 años + 20K+ servicios; quitadas 98% satisfacción + 24h respuesta + 5 estrellas que no se medían); AgendarPage respiro vertical extra; `font-extrabold` → `font-bold` en 3 hits de heading; `hover:-translate-y-1` quitado de cards. 8 archivos. [NO CERRAR sin QA Jorge].
  - **Cazadores 25/25 PASS** en cada commit. Typecheck + lint clean en staged. Builds 4.28s + 4.23s. Pre-commit hooks PASS sin bypass. Webhook Vercel disparado (hook backup CLAUDE.md `dqfSS3mCJK` → 201; el del system prompt `kfkia6Sqin` devolvió 404 — actualizar prompt si Jorge quiere consistencia).
- **2026-05-31 (pasada 55, `trabaja noche larga`)** — **5 sprints commiteados + pusheados (A.2 → A.5 + B), 2 ESCALADOS a BLOQUEOS (C y D), 0 sprints adicionales procesables.** Awaiting QA Jorge en producción:
  - **SPRINT-DISENO-A.2-EMOJIS** hash `c52f273` — 12 archivos JSX admin, emojis decorativos → iconos lucide. Preservados con `// @safe-emoji`: toasts, plantillas WA, mensajes WA cliente, toggles `✨ Nueva / ♻️ Usada` y origen piezas (claridad visual rápida en lista densa).
  - **SPRINT-DISENO-A.3-ESCALA-TIPOGRAFICA** hash `cbd6910` — 5 clases `.text-h1/h2/body/caption/micro` en `src/index.css` con `@apply`. NO migra. Infra para sprints futuros (B/C/D + adelante).
  - **SPRINT-DISENO-A.4-GRADIENTES-SATURADOS** hash `7256ee2` — 16 archivos. Dashboard 3 barras progreso → sólido; TecnicoVista card ganancias → bg-emerald-600; Rendimiento → bg-primary; GarantiaCliente 3 pantallas → bg-blue-50; ServicioDetalle card garantía → bg-emerald-50. Preservados con `@safe-gradient`: heros marketing públicos, splash full-screen, pantallas full-screen cliente final, AsistenteIA flotante.
  - **SPRINT-DISENO-A.5-COLORES-ICONOS** hash `099a2de` — HomePage `ICONO_POR_TIPO` 7 colores random (azul/cyan/indigo/naranja/púrpura/amarillo) → todos `bg-brand-50 text-primary`. Dashboard 4 KPIs neutros: Cotizaciones/Órdenes/Conduces → `bg-brand-500`; "Ingresos del Mes" preserva `bg-green-500` por semántica positiva intencional (dinero entrando).
  - **SPRINT-DISENO-B-TECNICO-ACCESIBILIDAD** hash `debaff6` — TecnicoVista.tsx: eliminados TODOS los `text-[10px]` (17) y `text-[11px]` (4) → `text-xs`; botones `px-3 py-2 text-xs` → `px-4 py-3 text-sm` (12 botones de handlers críticos, ~48px alto); `opacity-75/80/90` → `text-white/75/80/90` explícito (mejor screen readers). 11 handlers verificados intactos por grep.
  - **Sub-fixes lint preexistentes mínimos** (necesarios para pasar pre-commit hook `--max-warnings 0`): Calendarios.tsx (X unused), TrackingCliente.tsx (clienteId), Login.tsx (catch any → unknown narrowing), ServiciosPage.tsx (Phone unused). El resto de A.5-LINT (ModalEditarPiezasOrden, Gastos, ConfiguracionWeb, Solicitudes) queda en sprint propio.
  - **ESCALADOS a BLOQUEOS** (NO procesados por requerir decisión Jorge):
    - **SPRINT-DISENO-C-DASHBOARD-REDUCIDO** — Decisión KPI hero: opción A "Ingresos del día" / B "Citas pendientes" / C "Órdenes atrasadas".
    - **SPRINT-DISENO-D-PUBLICAS-DOMINICANO** — Decisión CTA + contenido testimonios reales: opción A "Repara no cambies" (sustentable) / B "antes que el peajero" (rápido + dominicano) / C "mismo día" (confianza) / D Jorge sugiere otro. Testimonios placeholder estructurales OK pero contenido real lo carga Jorge.
  - **Cazadores 25/25 PASS en CADA commit.** Typecheck PASS. Lint clean. 5 builds verificados (4.10-4.26s). 5 pre-commit hooks PASS sin bypass. Working tree limpio (~50 untracked legítimos NO incluidos en commits: .mcp.json, .playwright-mcp/, screenshots dp-*.png, docs Kommo, CLAUDE.md.bak.*).
  - **Sub-regla CLAUDE.md observada:** todos los sprints A.2-A.5+B marcados [NO CERRAR sin QA Jorge] → el coordinator NO marca COMPLETADO, deja en estado "código en producción awaiting QA Jorge" con plan explícito en COLA_AUTONOMA.md.
- **2026-05-30 (pasada 54, `procesa bloqueos` + camino 2)** — `SPRINT-DISENO-CIERRE-COMPLETO` FASE A.1 ejecutada tras `OK: jorge 2026-05-30 opcion=B`:
  - **11 commits**: chore `f861da9` (sync pasada 54) + 10 chunks A.1 (`fbb77d1` chunk1/ordenes → `74a69fb` chunk10/formularios+publicas).
  - **90 archivos tocados, 857 líneas reemplazadas simétricamente** (insertions == deletions, find&replace mecánico `\[#0f3460\]→primary` y `\[#1a5fa8\]→primary-medium`).
  - Cazadores 25/25 PASS en cada commit. Build 4.21s al cierre. Pre-commit hooks PASS.
  - **7 archivos en deuda lateral** (warnings ESLint preexistentes, no relacionados a A.1): `ModalEditarPiezasOrden`, `Calendarios`, `Gastos`, `Login`, `ConfiguracionWeb`, `Solicitudes`, `TrackingCliente`. Capturados como `SPRINT-DISENO-A.5-LINT-LIMPIEZA` re-encolado.
  - **FASES A.2/A.3/A.4/A.5/B/C/D re-encoladas** como sprints separados al final de COLA_AUTONOMA.md, cada uno con touch-list expandido propio (preparando próximas pasadas).
  - Source-of-truth intacto: `tailwind.config.js` y `src/index.css` no se tocaron. Ocurrencias residuales del hex viven en string literals (Leaflet, canvas, avatar fallback, paleta selector, gradient config, print stylesheets) — son fallbacks de dato dinámico o configuración no className, documentados como deuda lateral en `DIARIO_2026-05-30.md`.
  - Sub-regla CLAUDE.md "cleanup en página crítica requiere QA flujo X validado" cumplida — el chunk 5 (Dashboard/Ordenes/TecnicoVista/OrdenDetalle) declara explícitamente "QA flujo Ordenes + Dashboard + TecnicoVista + OrdenDetalle validado: solo clases CSS".
  - [NO CERRAR sin QA Jorge] — el coordinator NO marcó COMPLETADO. Deja en estado "código en producción awaiting QA Jorge" con plan de QA explícito.
- **2026-05-30 (pasada 52, `trabaja`)** — 2 sprints procesados, ambos commiteados + pusheados, awaiting QA Jorge:
  - `SPRINT-WIZARD-FASES-FREEZE` hash `a02a047` — diagnóstico: el `window.confirm()` del handler `handleClickFase` en `FaseStepper.tsx` se cuelga indefinidamente bajo Playwright sin `page.on('dialog')` handler instalado (Cowork hizo QA visual de DINERO-2 con `.playwright-mcp/` el 2026-05-30 → `document_idle waited 45000ms` = síntoma típico). En navegador real funcionaba. Fix: reemplazado `window.confirm` por Modal propio consistente con el modal de retroceso ya existente en el mismo componente. 4 consumidores verificados (OrdenDetailModal, OrdenDetalle, OrdenCard, TecnicoVista) — ninguno pasa `onCambioFase` → comportamiento idéntico para usuarios reales. Deuda follow-up identificada (NO scope): bug latente en `historialFases.map` reemplazando timestamps con `new Date()` cuando llegan como Firestore Timestamp.
  - `SPRINT-DISENO-TECNICO-FASE-1` hash `4c21dc9` — reordena JSX en `TecnicoVista.tsx`: saludo al header (1 línea con logo), card ganancias movida ABAJO del listado, etiqueta "PRÓXIMA CITA" antes de primera cita NO completada, tabs Hoy/Semana/Mes/Rango al final como filtro auxiliar, mapa colapsado (ya cumplía). 11 handlers críticos intactos (verificado por grep). Emojis preservados (Fase 4). Sub-regla CLAUDE.md "cleanup en TecnicoVista requiere QA flujo X validado" cumplida — handlers no se tocaron, solo se reorganizaron bloques JSX.
  - **25/25 cazadores PASS** en cada commit. Typecheck PASS. Lint clean. Build 4.32s + 4.50s. Pre-commit hooks PASS.
  - 0 sprints más procesables en la cola; resto ya está COMPLETADO o awaiting QA previa (DINERO-2, REPORTING-1, AGENDA-1, GARANTIA Fase A, WA-FIX-PLANTILLAS).
  - **Sub-regla CLAUDE.md observada:** ambos sprints marcados [NO CERRAR sin QA Jorge] → el coordinator NO marca COMPLETADO, los deja en estado "código en producción awaiting QA Jorge" con plan de QA explícito en COLA_AUTONOMA.md.
- **2026-05-29 (sesión Claude Code con gstack instalado)** — Cuatro sprints verificados por QA estático + 1 cazador nuevo:
  - **`/qa` AGENDA-1** (hash `132d9b5`, prod URL): 3/3 ✅ del flujo Mantenimiento → Orden → Ficha cliente. Detectó deuda anti-regresión → creó **cazador P-025** (`check-mantenimiento-clienteid-vacio.ts`, 204 líneas) detecta `clienteId: ''` en writes a `mantenimiento`/`ordenes_servicio`. Verificado: 0 falsos positivos en 238 archivos. Commits: `1c1717e` (fix mapa Mermaid `area_*` prefijo) + `b065e4a` (cazador + entrada PATRONES_REGRESION.md).
  - **`/qa-only` DINERO-2** (hash `b4fc23c`): ✅ recalc dentro de `runTransaction`, guard idempotencia por `pago.id`, suma con cascada `precioFinal/Aprobado/Sugerido`, estado igual a `RegistrarPagoModal`, gate P-023 intacto. Deuda menor: extraer `calcularEstadoPago` cuando aparezca 3er caller. **Mantiene flag [NO CERRAR sin QA Jorge]** — falta validación visual emitiendo conduce cobrando saldo.
  - **`/qa-only` REPORTING-1** (hash `a4e64db`): ✅ 3 helpers en `src/utils/kpis.ts` excluyen anuladas defense-in-depth, Dashboard los usa + filter inline "Conduces hoy" también. Sin observaciones. **Mantiene flag [NO CERRAR sin QA Jorge]** — falta comparar KPI Dashboard antes/después si hay anuladas históricas en el mes.
  - **`/qa-only` FIX-LEADS** (hash `01df699`): ✅ storage.rules con whitelist `image/*` + `application/pdf` < 10MB, REGLA DE ORO cumplida, path migrado a `solicitudes-publico/`, GPS leído por `campo.tipo==='ubicacion'` con type guard refinado, deploy verificado por P-013 lock match. Sin observaciones. **CERRADO definitivo** — no necesita QA visual adicional.
  - **24/24 cazadores PASS** (P-025 nuevo, sube de 23).
  - **Bug del hook de `/careful` SIGUE roto** (`PreToolUse:Bash hook error: /bin/check-careful.sh: No such file or directory`). No bloquea — Claude obedece patrones al nivel del modelo. Esperar `/gstack-upgrade` o reportar al repo.
  - Deuda no commiteada: `.gitignore` modificado por gstack (agregó `.gstack/`), untracked de screenshots/docs pasada 51.
- **2026-05-25 (pasada 51, `trabaja` nocturno)** — Bloque FLUJO-DEPENDENCIAS procesado: **8 sprints commiteados + pusheados**, 1 ESCALADO. **4 COMPLETADOS autónomos:**
  - `SPRINT-AGENDA-2` hash `e4f92bf` — calendario + AgendaDia muestran citas por confirmar + mantenimientos como capa tentativa (ámbar/púrpura, borde punteado), toggle "Mostrar tentativos".
  - `SPRINT-AGENDA-3` hash `f9697b9` — useOrdenCreateForm honra `asignadoId` del calendario público (precarga técnico al confirmar cita web).
  - `SPRINT-AGENDA-4` hash `fba51a4` — CitaPublica.tsx escribe `equipoTipo` + `equipoMarca` + `telefonoNormalizado` (alineado con FormularioAgendarPublico, anti-duplicado aplica a ambos paths).
  - `SPRINT-AGENDA-5` hash `8f6a72b` — CierreServicioWizard ofrece programar próximo mantenimiento al cerrar (toast con acción "Sí, programar"+3m).
  - `SPRINT-DINERO-1` hash `bec87b3` — Cotizaciones.tsx migrado a `siguienteNumeroCotizacion()` (transaccional), helper deprecated lanza en runtime. Cierra follow-up SPRINT-PAGOS-FIX-COTIZACIONES.
  - **3 [NO CERRAR sin QA Jorge]** en producción esperando QA:
    - `SPRINT-AGENDA-1` hash `132d9b5` — mantenimiento atado a cliente real (typeahead + buscarOCrearCliente + tecnicoId=uid + emite orden_asignada). Modal con mantenimientos pre-sprint bloquea botón Generar Orden.
    - `SPRINT-DINERO-2` hash `b4fc23c` — ProcesarFacturacionModal recalcula `montoPagado`/`estadoPago` al cobrar saldo dentro de la runTransaction existente. Idempotente. Gate P-023 intacto.
    - `SPRINT-REPORTING-1` hash `a4e64db` — `src/utils/kpis.ts` con 3 helpers compartidos (`ingresosFacturasPagadas`, `conducesEmitidosMonto`, `conducesEmitidosCount`). Dashboard migrado. `conducesEmitidosMonto` EXCLUYE `anulada` (cambio funcional).
  - **1 ESCALADO:** `SPRINT-NUCLEO-CREAR-ORDEN-CENTRAL` movido a BLOQUEOS con plan de 3 fases + decisión A/B/C (refactor de cimiento, [NO CERRAR sin QA], inseguro de noche).
  - **Cazadores 24/24 PASS en cada commit. Typecheck PASS. Lint clean.** 0 hits.
- **2026-05-25 (pasada 50, `trabaja`)** — 1 sprint COMPLETADO:
  - `SPRINT-WA-FIX-PLANTILLAS-PARAMS` COMPLETADO hash `0ab73c5`. Arregla las 4 plantillas WhatsApp desfasadas del rediseño Meta ~15 may 2026 (error #132000 en `recordatorio` + variables/slots equivocados en las otras 3 + faltaba header IMAGE). Solo FRONTEND — `send.ts` ya soportaba `headerImageUrl` desde SPRINT-WA-2-HEADER-IMAGE. Archivos: `src/config/plantillasWhatsApp.ts` (tipo `PlantillaCatalogo` con `imagenEncabezadoUrl?`, union `AutopopularDe` +4 fuentes nuevas, 2 helpers nuevos `formatearFechaCitaDia`/`formatearFechaCitaHora`, las 4 plantillas reescritas según `PLANTILLAS_META_SPEC_2026-05-25.md`), `src/services/whatsapp.service.ts` (`PlantillaArgs` con `headerImageUrl?`, `enviarPlantilla` acepta nuevo param posicional, patrón strip-undefined), `src/components/inbox/SelectorPlantillas.tsx` (reenvía URL). 24/24 cazadores PASS. Build 4.36s. Pre-commit hook PASS. Push `ae1a6a6..0ab73c5`. Deploy job `Yr0nTylm03jpzaalsPhd` PENDING. QA Jorge envío real pendiente. Hallazgo lateral documentado follow-up `SPRINT-WA-AUTORESPUESTA-SIN-HEADER`.
- **2026-05-25 (pasada 49, `trabaja` autorizaciones explícitas Jorge)** — 2 sprints procesados:
  - `SPRINT-FIX-LEADS-FORMULARIO-PUBLICO` COMPLETADO hash `01df699` (Jorge OK opción A + deploy auto + incluir-gps=si). Archivos: `storage.rules` (match nuevo `solicitudes-publico/**` whitelist `image/*` + `application/pdf` < 10MB, REGLA DE ORO preservada — comodín `{allPaths=**}` y `fotos-equipos-publico` intactos), `src/services/solicitudes.service.ts` (path migra a `solicitudes-publico/...`), `src/pages/public/FormularioPublico.tsx` (fix lateral GPS hallazgo #7 — busca por `campo.tipo === 'ubicacion'` con cascada a clave literal, type guard refinado). Deploy ejecutado `npm run deploy:storage-rules` sha `accf5550...` 12:24:57Z. P-013 lock actualizado. 23/23 cazadores PASS.
  - `SPRINT-GARANTIA-FLUJO-COMPLETO` FASE A hash `59c5fb0` (Jorge OK FASE A; NO cerrado COMPLETADO hasta QA). Aplica reglas Jorge: técnico original conserva comisión, 10% costo piezas al cerrar orden garantía (no anulación 100%). NUEVO helper `aplicarDescuentoGarantiaPorPiezas` (calcula `-(piezas × 0.10)`, busca comisión por ordenId+tecnicoId, NO toca `estaAnulada`). Helper invocado en `CierreServicioWizard` post-cierre cuando orden `esGarantia=true` + piezas. `Citas.tsx::onAfterCreate` reemplazado bloque anulación-100% por solo snapshot factura + audit `garantia_reabierta`. UI `Comisiones.tsx` columnas "Desc. garantía" + "Neto" + CSV + panel totales con sub-línea bruto/desc. Banner modal "Cambio de técnico" reformulado (10% piezas vs 100% comisión). NUEVO cazador P-024 + entrada `PATRONES_REGRESION.md` + registrado en `run-all.ts` (24/24 PASS). Deuda fase B documentada en `BLOQUEOS.md`: botón "abrir garantía", gate "solo oficina", capturar cliente paga, notifs, regla "mismo técnico no gana comisión adicional".
- **2026-05-25 (pasada 48, `trabaja`)** — `SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-2` COMPLETADO commit `d4d6498` (opción B aprobada por Jorge `OK: jorge 2026-05-25 01:04`). Helper común `obtenerPagosDeOrden(orden)` en `ordenes.service.ts` + 4 lectores migrados (OrdenDetailModal x2, OrdenDetalle x3, RegistrarPagoModal pagosPrevios, ProcesarFacturacionModal pagosPrevios). Gate del conduce (P-023 NO-ALLOWLIST) intacto. 2 callsites raw data con tag `@safe-pagos-raw`. NUEVO `scripts/migrar-pagos-array-a-subcoleccion.ts` (dry-run default, idempotente, guard >500, audit log). Writers W2-W5 NO se tocaron. 23/23 cazadores PASS.
- **2026-05-24 (pasada 47, `trabaja`)** — Bloque AGENTES procesado completo: (a) `SPRINT-AGENTES-1-AUDITORIA-CONTABLE` commit `d938135` — agente `auditor_contable` + 3 cazadores P-021/P-022/P-023 (subió de 20→23) + informe `AUDITORIA_CONTABLE_2026-05-24.md` con 1 hallazgo crítico (Cotizaciones.tsx número no-transaccional, sprint follow-up). (b) `SPRINT-AGENTES-2-MEMORIA-DIRIGE` commit `df68a42` — `MAPA_RIESGOS_MODULOS.md` (11 módulos) + modo MANTENER-MAPA en agente memoria + sub-regla CLAUDE.md. (c) `SPRINT-AGENTES-3-PARALELIZAR` commit `30abe53` — coordinator paraleliza verificación + auditorías de módulos disjuntos. SPRINT-PAGOS-FASE-B-2 escalado a BLOQUEOS por ambigüedad técnica (NO se tocó código de pagos).
- **2026-05-24** — Auditoría de software completa (Cowork): informe en `docs/sprints/AUDITORIA_SOFTWARE_2026-05-24.md` (1 crítico, 6 altos, varios medios). Se creó el sistema de **memoria viva** (`MEMORIA_MAESTRA.md` + agente `memoria`, commit `dad8ca8`), el bloque **AGENTES** en la cola, el agente **`guardian_logica`**, y el sprint de **garantía**.
- **2026-05-24** — `SPRINT-WA-SEGURIDAD-CONFIG-RULES` COMPLETADO (commit `e9aa3ef`). Se cerró un hueco de permisos: los 3 docs de config de WhatsApp (`whatsapp_envio`, `whatsapp_numeros`, `whatsapp_respuestas_rapidas`) ahora son escribibles SOLO por admin. Verificado con emulator (Java Temurin 25, 22 tests OK) + rules deployadas. 20 cazadores OK.
- **2026-05-24** — **QA de PAGOS-FASE-B-1 aprobada** (Jorge + Cowork verificaron en producción): se confirmó el pago de prueba de OS-0059 → salió de pendientes → desbloquea la emisión del conduce. Por eso B-2 ya está en la cola.
- **2026-05-23** — `SPRINT-WA-TRAZABILIDAD-Y-RESPUESTAS-RAPIDAS` (commit `d7b320b`): trazabilidad de quién envía cada mensaje + nombre del agente al cliente + respuestas rápidas tipo WhatsApp Business.
- **2026-05-23** — `SPRINT-WA-INBOX-UX-QUICKWINS` (commit `3eff5eb`): número real + código en el selector, último servicio en el panel del cliente, botones de copiar, plantillas accesibles con la ventana abierta.
- **2026-05-22** — `SPRINT-WA-NUMERO-RESPALDO-MANUAL` Fase 1 + arreglo del bug `stripUndefinedDeep` (mensajes de WhatsApp que no se reflejaban en el CRM) + arreglo del script `deploy:storage-rules`.

> El detalle completo de cada día está en `docs/sprints/DIARIO_<fecha>.md`.

---

## 📌 DECISIONES DE JORGE QUE NO SE OLVIDAN

- **Garantía (reglas confirmadas en entrevista 2026-05-24, FASE A aplicada en hash `59c5fb0` 2026-05-25 — awaiting QA Jorge):** cuando un trabajo falla y se cubre la garantía, se reabre la orden (hoy crea una orden ligada; base ya existe de SPRINT-135a). Reglas: (1) al **técnico original** se le descuenta el **10% del costo de las PIEZAS** de la re-reparación, siempre que haya gasto en piezas — APLICADO Fase A; (2) el original **conserva su comisión** (el 10% reemplaza el viejo "pierde toda la comisión") — APLICADO Fase A; (3) si **otro** técnico cubre la garantía de un compañero, ese **sí gana comisión** (flujo estándar al facturar); si el **mismo** técnico la cubre, **no gana** comisión por la garantía — PENDIENTE Fase B (hoy si se factura la orden de garantía con el mismo técnico el flujo estándar le paga comisión, falta lógica de skip); (4) el **cliente paga según el caso** (gratis o parcial, lo decide quien reabre) — PENDIENTE Fase B; (5) **reabren: secretaria, operaria, coordinadora, admin — los técnicos NO** — PENDIENTE Fase B (botón "abrir garantía" + gate); (6) el plazo sale del **conduce de garantía** (60 días por defecto) — YA EXISTÍA via `utils/garantia.ts`. Detalle técnico en `SPRINT-GARANTIA-FLUJO-COMPLETO` (cola) + informe `AUDITORIA_SOFTWARE_2026-05-24.md` + plan QA en `BLOQUEOS.md`. Cazador P-024 activo bloqueando reintroducción del patrón viejo (`monto: -comisionMonto*` + `estaAnulada: true`).
- **Buzón de seguimiento (nurture) — regla anti-bloqueo:** a un cliente que NO quiere agendar se le manda **UN solo recordatorio automático**, nada más automático. Después, todo es **manual por lotes** que selecciona el admin/coordinador (para no disparar bloqueos de Meta). WhatsApp Flows se ven más adelante.
- **PAGOS por fases con QA entre cada una.** B-1 ya pasó QA → B-2 habilitada. B-3 (toca reglas) espera nueva QA de Jorge antes de procesarse.
- **Cowork NO hace solo:** crear WABA / cuentas, pagos, OAuth, integraciones nuevas, migraciones >500 docs, ni tocar reglas de Firestore sin OK. Eso se escala a `BLOQUEOS.md`.
- **Comunicación (REGLA DURA, Jorge la repitió 2026-05-28: "anota eso siempre"):** **palabras simples SIEMPRE, sin jerga técnica.** Español latino/dominicano. Breve. Si tenés que usar un término técnico, traducilo entre paréntesis (ej: "Firestore rules (los permisos de la base de datos)"). Decir siempre si un comando va en la **Terminal de la Mac** o en **Claude Code**. Aplica a Cowork, al coordinator, y a CUALQUIER agente que le hable a Jorge directo. Si vas a explicar qué hace un agente o herramienta, hacelo como si fueras una persona del equipo diciéndole qué hace ("Sentate, contame qué problema querés resolver" en vez de "ejecuta una rutina de interrogación de requerimientos").
- **Números WhatsApp:** `1226992440486630` = +1 829-471-6265 (WABA Principal Meta) · `1151997541323577` = +1 849-564-6767 (WABA Respaldo Meta).
- **Números públicos en la web HOY (Configuración → Página Web → WhatsApp) — TEMPORAL pre-launch (2026-06-01):** Línea 1 `829-389-7474` + Línea 2 `809-280-9601` + Línea 3 `829-828-7880`, rotación activa, mensaje predeterminado "Hola, necesito un servicio". **Son líneas físicas que el equipo usa por el momento de forma física**, NO la API de Meta. Se quedan ahí mientras siguen trabajando con esos chips. **Decisión Jorge (2026-06-01):** cuando la app entre en uso real, **se reemplazan por el número de Meta `849-564-6767` como número principal del negocio en la web pública**. **NO tocar la config de números públicos hoy** (ni borrar, ni cambiar la rotación, ni reemplazar). Cuando llegue el momento, sprint chico (1 archivo de config Firestore + QA del enlace `wa.me`). Pendiente clarificar con Jorge si ese cambio implica también promover el WABA Respaldo Meta (que hoy tiene ese 849...) a WABA Principal Meta, o si son decisiones separadas (afecta planes de `SPRINT-WA-NUMERO-RESPALDO-MANUAL-FASE-2`).
- **Fuente del software (2026-06-01):** **Plus Jakarta Sans**. Reemplaza Inter como font base de Tailwind. Decisión tomada tras leer regla del leak prompt diseño Claude (*"Avoid overused font families (Inter, Roboto, Arial, Fraunces, system fonts)"*). Cowork presentó 3 opciones (Plus Jakarta Sans / DM Sans / Geist) con descripción de personalidad de cada una; Jorge eligió la recomendada (moderna + confiable + buena para admin denso + landing comercial). Aplicar vía `SPRINT-DISENO-E-FUENTE-PLUS-JAKARTA`. Si en el futuro Jorge la quiere cambiar, sprint propio (afecta identidad de marca).

---

## 🔮 PROYECTOS FUTUROS (necesitan diseño antes de cola)

- **IA Coordinadora de Citas** — agente de WhatsApp que agende solo, transcriba audios del cliente, y el buzón de seguimiento con la regla anti-bloqueo. Visión + preguntas de diseño en `docs/sprints/PROPUESTA_IA_COORDINADORA_CITAS.md`. **No construir sin sesión de diseño.**
- **Meta Pixel + API de Conversiones** — medir `Lead` (clic WhatsApp + formulario de cita) y `Purchase` (orden facturada) para que las campañas de Meta optimicen. Cuenta `180693964677323`. Detalle + notas en `docs/sprints/PROPUESTA_META_PIXEL.md`. **Jorge lo quiere para más adelante.** NO autónomo (integración con tercero + endpoint + token → tu OK + BLOQUEOS). Gatillo: "vamos con el pixel".

---

## 🗂️ DÓNDE VIVE TODO (índice de la fuente viva)

| Si querés saber… | Mirá aquí |
|---|---|
| Qué hay pendiente en la cola | `docs/sprints/COLA_AUTONOMA.md` (el tope) |
| Qué espera OK de Jorge | `docs/sprints/BLOQUEOS.md` |
| Qué hizo el coordinator cada día | `docs/sprints/DIARIO_<fecha>.md` |
| Log detallado de ejecución autónoma | `docs/sprints/EJECUCION_AUTONOMA.md` |
| Reglas del proyecto + gotchas | `CLAUDE.md` |
| Contexto general para Cowork | `COWORK_CONTEXTO.md` |
| Bugs catalogados (cazadores) | `docs/PATRONES_REGRESION.md` |
| Análisis de bugs de producción | `docs/postmortems/*.md` |

---

> **Recordatorio para quien edite este archivo:** mantenelo CORTO. Esto es un índice del estado, no una copia de todo. Si una sección crece mucho, movela al archivo fuente que corresponde y dejá solo el enlace. Tachar (`~~...~~`) en vez de borrar cuando algo se completa y querés preservar el rastro por unos días.

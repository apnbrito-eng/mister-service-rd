# Diario — 2026-05-08

## Resumen

- 8 sprints/fases procesados (mañana + tarde + segunda + tercera + cuarta + quinta + sexta pasada):
  - SPRINT-111 fase 111a documental (mañana, coordinator).
  - SPRINT-113a banner siguiente paso (tarde, review humano + push).
  - **SPRINT-113b** badge "Sugerencia pendiente" + tooltips disabled (segunda pasada).
  - **SPRINT-113c** timeline horizontal de últimas 5 acciones (segunda pasada).
  - **SPRINT-114** 4 hits descriptivos a `currentUser.uid` (segunda pasada).
  - **SPRINT-115 fase diagnóstico** script read-only para diagnóstico de notificaciones de Yohana (tercera pasada).
  - **SPRINT-115 fase write** script de re-migración acotada para Yohana (cuarta pasada — entregado, espera ejecución humana).
  - **SPRINT-117 fase A2 porción read-only** (quinta pasada) — 2 scripts read-only que absorben SPRINT-116 fases A y B.
  - **SPRINT-118** (sexta pasada) — 2 scripts en DRY-RUN entregados: re-migración masiva 5 empleados (44 docs) + fix email Wilainy en Auth. Estado EN_REVISION_HUMANA esperando que Jorge corra `--apply` manualmente.
- 0 sprints bloqueados nuevos.
- Commits del día en producción: `ce9d5c5`, `9603da3`, `dd24bb2`, `5bfa0e0`, `49af624`, `0909237`, `fc74fec`, `f6d1d76`, `86a3b0f`, `6b4aade`, `db7719f`, `797cbe8`, `1570bfc`, `1d3280e`, `ac54662`, `6defe8f`, `e6ccb1e`.
- ~125 min de trabajo del coordinator + tiempo Cowork ↔ Jorge.

## Update sexta pasada (`trabaja` SPRINT-118)

- Jorge desbloqueó SPRINT-118 (`procesa bloqueos`) y luego pegó `trabaja`.
- Coordinator entregó 2 scripts en DRY-RUN por default:
  - `scripts/re-migrar-notificaciones-masivo.ts` (435 líneas) — generaliza `re-migrar-notificaciones-yohana.ts`. Scope rígido: 5 uids + 5 personalDocIds + 44 doc IDs hardcodeados. Doble validación contra realidad por empleado. Idempotente. Audit log post-`--apply`.
  - `scripts/fix-email-wilainy.ts` (311 líneas) — Admin SDK `auth.updateUser` + `usuarios/{uid}.email`. NO toca `personal.email` (ya correcto), NO toca contraseña. Validación pre-write contra emailViejoEsperado. Idempotente. Audit log post-`--apply`.
- Tester PASS: typecheck OK, cazadores 6/6 PASS, lint sin hits nuevos.
- regression_guardian PASS: `--apply` requiere flag explícito, idempotencia verificada, errores no se tragan.
- Reviewer APPROVED: blast radius acotado y reversible.
- Commit `e6ccb1e` pushed. Pre-commit hook OK.
- Estado SPRINT-118: EN_REVISION_HUMANA. Cierre a COMPLETADO requiere ejecución manual de Jorge + validación con los 5 empleados + postmortem.
- Restricción explícita respetada: coordinator NO ejecutó `--apply` autónomo.

## Update tarde (Cowork ↔ Jorge)

- SPRINT-113a (banner siguiente paso, commit `9603da3`) estaba marcado como EN_REVISION_HUMANA esperando validación visual de Jorge.
- Jorge confirmó que el banner aparece correctamente en producción → fase 113a marcada COMPLETADO en `COLA_AUTONOMA.md`.
- 113b (badges sugerencia/esperando) y 113c (timeline acciones) siguen PENDIENTE para procesamiento autónomo.

## Update noche (Cowork ↔ Jorge)

- SPRINT-100 QA visual ejecutado con Yohana → resultado: **"no hay nada en la campanita"**. Confirma **Caso A** según hipótesis Cowork: docs legacy probablemente con `destinatarioId == personalDocId` (no llegan a ningún query del service `notificaciones.service.ts`).
- SPRINT-115 abierto en cola con dos fases:
  - Fase diagnóstico (read-only, autónoma): script con Admin SDK que dump notificaciones de Yohana clasificadas por shape (userId vs destinatarioId vs personalDocId) y reporta cantidades.
  - Fase re-migración (write, BLOQUEADA): script idempotente acotado a Yohana que setea `userId = auth.uid` en sus docs legacy. Requiere OK Jorge en `BLOQUEOS.md` antes de ejecutarse.
- SPRINT-100 sigue PENDIENTE — se cerrará cuando SPRINT-115 termine y Yohana valide el re-QA.
- SPRINT-112 (schema drift + matriz permisos): Jorge eligió esperar disponibilidad de QA por rol. Sin cambios.

## Update noche 2 (Cowork ↔ Jorge)

- Coordinator procesó fase diagnóstico de SPRINT-115 (commits `f6d1d76` + `86a3b0f`). Script `scripts/diagnostico-notificaciones-yohana.ts` ya en main.
- Jorge ejecutó el diagnóstico con email `melissabalbuena08@gmail.com`. **Caso A confirmado al 100%**: Yohana tiene **3 notis tipo `chequeo_iniciado` no leídas** del 20-24 abril 2026, todas con `userId == personalDocId` (`zFhokrDoPH9lD63ZxKAY`) Y `destinatarioId == personalDocId`. Su `auth.uid` correcto es `HGkVoYpGKzL4JJI7FnTpHjdsM972`. El query del service no las encuentra porque busca por `auth.uid`.
- IDs de los 3 docs problemáticos: `F9BV32k4JEoEOk97K4xc`, `TVwtOtmNlzW334IUIUdF`, `VWjdYBRmKgU8rGPlbJAv`.
- Jorge autorizó la fase re-migración → coordinator escribió `scripts/re-migrar-notificaciones-yohana.ts` (commit `6b4aade`, DRY-RUN por default).
- Jorge ejecutó el dry-run, output validado: 3 docs actualizables, scope correcto.
- **Re-mortem de la noche:** Jorge intentó cambiar contraseña de Wilainy desde GestionUsuarios y la app respondió "No existe usuario con email Nwilainy@gmail.com". Recordatorio de que el backfill del 6 de mayo ya había detectado el conflicto de emails (uid `KT9LaszokWNmLCEIe8YOvNKc9rF3` con `usuarios.email=apnbrito0318@gmail.com` ≠ `personal.email=nwilainy@gmail.com`).
- **Tercer bug crítico detectado:** Yohana no ve órdenes de Jesús (su técnico) en agenda del día. Cowork investigó código y confirmó variante de P-006 con campo `operariaId` en `personal/`. Línea exacta: `src/pages/Dashboard.tsx:459` y `src/pages/AgendaDia.tsx:286`. El comentario `@safe-userprofile-id` allowlistea estos casos asumiendo que `operariaId` está guardado como `personalDocId`, pero en producción puede estar mezclado.

## Decisión arquitectónica de Jorge (final del día)

Jorge decidió pivotar de "fix individual de cada bug" a **rediseño + auditoría general**:

> "vamos a reordenar todo y luego hacemos que funciona paso por paso en una auditoria general"
> "debes leer el código completo todo de principio a fin"

**Plan resultante:**

1. **SPRINT-117 ampliado** con fase A exhaustiva en 3 partes:
   - A1 — lectura completa de cada archivo en `src/` (no escaneo, no grep parcial).
   - A2 — auditoría funcional (filtros rotos, queries asumiendo campos mal, variantes P-001/P-006, notis legacy, email mismatches).
   - A3 — auditoría de Information Architecture (menús, módulos, redundancias).
2. Output: 3 archivos markdown extensos (`AUDITORIA_IA_2026-05-08.md`, `AUDITORIA_FUNCIONAL_2026-05-08.md`, `INVENTARIO_CODIGO_2026-05-08.md`).
3. Después fase B propone reorganización + plan de fixes priorizados.
4. Fase C ejecuta sub-sprints chicos uno a la vez con OK explícito de Jorge.

**Estados ajustados:**

- SPRINT-115 fase write → **PAUSADO** (no ejecutar `--apply` hasta post-auditoría).
- SPRINT-116 → **ABSORBIDO** por SPRINT-117 fase A2.
- SPRINT-117 → **PRIORIDAD 1** ahora.

**Costo aceptado:** Yohana sigue sin ver sus 3 notis viejas mientras tanto. Bug operativo de operaria↔técnico también sigue activo. Jorge prioriza ataque sistémico sobre fix individual.

## Update final del día (Cowork ↔ Jorge)

- Coordinator entregó scripts read-only de SPRINT-117 fase A2 (commits `ac54662` + `6defe8f` + `59f8ff6`): `auditoria-emails-personal-vs-usuarios.ts` y `auditoria-notis-legacy-todos.ts`.
- Jorge ejecutó ambos. Resultados:
  - **Emails:** solo Wilainy tiene mismatch (`personal=Nwilainy@gmail.com` vs `auth=apnbrito0318@gmail.com`). Los otros 21 empleados ok.
  - **Notis:** **4 empleados afectados con 41 notis Caso A** (más Yohana que tiene 3 según diagnóstico anterior — el script general la cuenta como ok porque encuentra match con personalDocId, pero la operaria NO las ve).
- **Empleados con notis problemáticas:** Wilainy (14), Jorge (9), misterservicerd (9), Maria Teresa (9), Yohana (3). Total: **5 empleados, ~44 docs**.
- Hallazgo crítico: Jorge mismo está afectado con 9 notis Caso A. Eso explica por qué nunca había detectado el bug masivo — su propia campanita también lo escondía.
- Jorge autorizó SPRINT-118 con OK formal en `BLOQUEOS.md`:
  - **Fase 1:** re-migración masiva acotada a 5 uids específicos con IDs de docs listados (44 docs total).
  - **Fase 2:** fix email Wilainy en Firebase Auth (cambiar de `apnbrito0318@gmail.com` a `Nwilainy@gmail.com`).
- Coordinator próximo paso: cuando Jorge pegue `procesa bloqueos`, builder genera `scripts/re-migrar-notificaciones-masivo.ts` y `scripts/fix-email-wilainy.ts`. Ambos con DRY-RUN por default, idempotencia, audit log.
- Postmortem obligatorio post-ejecución: `docs/postmortems/2026-05-08-notis-legacy-multiples-empleados.md`.

**Decisión técnica nueva:** considerar agregar al catálogo P-XXX un patrón nuevo "notificaciones legacy con userId/destinatarioId apuntando a personalDocId en lugar de auth.uid". El cazador no puede ser sobre código (el bug está en datos viejos), sino sobre datos — un health-check periódico tipo `npm run audit:notis-legacy` que avisa si aparecen casos nuevos.

## Cierre SPRINT-118 (Cowork ↔ Jorge)

- Jorge ejecutó dry-run de ambos scripts: outputs validados, scopes correctos, 41 docs a actualizar + 3 ya alineados (Yohana — su `--apply` previo del SPRINT-115 ya había escrito el `userId` correcto, idempotencia confirmada).
- Jorge ejecutó `--apply` de los dos scripts en serie.
- **Resultado final fase 1 (re-migración notis):** 41 docs actualizados, 3 ya alineados, 0 skips. Audit log escrito en `auditoria_admin/`.
- **Resultado final fase 2 (email Wilainy):** `auth.email` y `usuarios/{uid}.email` cambiados de `apnbrito0318@gmail.com` a `Nwilainy@gmail.com`. Audit log escrito.
- **Validación visual de Jorge:** campanita admin muestra 39 notificaciones (incluyen sus 9 nuevas + viejas). Cambio de contraseña de Wilainy desde GestionUsuarios funciona — antes la app tiraba "No existe usuario con email Nwilainy@gmail.com", ahora abre el modal y permite cambiar.
- **Pendiente humano (no bloquea cierre):** validación visual de los otros 4 empleados afectados (Wilainy, Yohana, Maria Teresa, cuenta misterservicerd) tras hacer hard refresh.
- Postmortem obligatorio por sub-regla CLAUDE.md "5+ empleados afectados" — pendiente que el coordinator lo escriba en próxima pasada de `trabaja`.

## Segunda pasada autónoma (3 sprints, 75 min)

Jorge disparó `trabaja` por segunda vez. Cola estaba en buen estado: SPRINT-100 humano, SPRINT-112 grande con QA, 113b/c PENDIENTE (procesables), 114 baja prioridad (procesable).

- **SPRINT-113b** (commit `49af624`) — badge "Sugerencia pendiente" en `FaseStepper`, helper puro `tooltipsBotones.ts` con 3 funciones (`razonIniciarChequeoDisabled`, `razonCerrarServicioDisabled`, `razonEnviarFacturacionDisabled`), tooltips `title` en Iniciar chequeo / Cerrar servicio (5 razones) / Enviar a conduce. Badge presentacional sin onClick (justificado en EJECUCION).
- **SPRINT-113c** (commit `0909237`) — helper `timelineAcciones.ts` mezcla `historialFases` + `auditoria` con parser tolerante a shapes legacy. Componente `TimelineAcciones` responsive (vertical mobile / horizontal scroll md+) montado al pie del bloque "Flujo de la orden" en `OrdenDetalle.tsx`. Auto-oculta con <2 acciones.
- **SPRINT-114** (commit `fc74fec`) — 4 sitios migrados a `currentUser?.uid`: `pago.registradoPorId`, `enviadaAFacturacionPorId`, `emisorFacturaId` (+ `solicitanteUid` x2), `responsableId` y `procesandoPor` (vía callers del hook `useOrdenCreateForm`). Fix colateral: warning eslint preexistente `react-hooks/exhaustive-deps` en `Ordenes.tsx` resuelto con `useMemo([])` para estabilizar `hoy`.

**SPRINT-113 padre:** 4 de 6 criterios COMPLETADOS por las fases 113a + 113b + 113c. Quedan 2 criterios:
- QA manual con usuarios reales (BLOQUEADO — requiere humanos en flujo end-to-end).
- Cazador anti-regresión de tooltips (NO IMPLEMENTADO — sprint propio futuro si Jorge lo prioriza, scope mediano).

Las 3 fases técnicas están en producción.

## Tercera pasada autónoma (1 sprint, ~10 min)

Jorge disparó `trabaja` por tercera vez. Cola estaba en estado: SPRINT-100 humano, SPRINT-112 grande con QA, SPRINT-113 padre esperando QA end-to-end, **SPRINT-115 fase diagnóstico procesable autónoma** (la re-migración write queda BLOQUEADA esperando OK Jorge).

- **SPRINT-115 fase diagnóstico** (commit pendiente) — Nuevo script `scripts/diagnostico-notificaciones-yohana.ts` (250 líneas). Read-only con Admin SDK. Toma email como argumento, busca el usuario en `personal/`, verifica `usuarios/{uid}`, hace 4 queries paralelas a `notificaciones` (matriz `userId/destinatarioId × authUid/personalDocId`), clasifica cada doc en Caso A/B/C/OK/OTRO, imprime resumen + ejemplos + diagnóstico final con interpretación. Sin escrituras. Patrón calcado del script gemelo `diagnostico-tecnicoid-auth-uid.ts`. **La ejecución del script y la fase de re-migración write quedan en manos de Jorge** — la re-migración requiere OK explícito en `BLOQUEOS.md` antes de ejecutarse (sub-regla CLAUDE.md sobre migraciones de datos).

Decisión clave: Jorge corre `npx tsx scripts/diagnostico-notificaciones-yohana.ts <email-de-yohana>` cuando esté listo. El output va a guiar la fase write — si confirma Caso A o Caso B, se desbloquea la re-migración. Si reporta 0 docs problemáticos, hay que buscar otro vector (cache, App Check, etc.).

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-111 | Auditar campos ID con vector P-001/P-006 (fase 111a) | `ce9d5c5` | 4 (auditoría .md + diario + cola + log) | Auditoría documental de 12 campos. 0 bugs latentes nuevos detectados. P-001 + P-006 + gotchas vigentes cubren todos los vectores activos. |
| SPRINT-113a | Banner siguiente paso contextual al rol y a la fase | `9603da3` + `dd24bb2` | 4 (`siguientePaso.ts` + `BannerSiguientePaso.tsx` + 2 páginas) | Review humano por Jorge antes de push. Validado visualmente en producción. |
| SPRINT-113b | Badges sugerencia + tooltips disabled | `49af624` | 5 (`tooltipsBotones.ts` + 4 modificaciones) | Helper puro testeable, badge presentacional, 3 botones con tooltip explicativo. |
| SPRINT-113c | Timeline horizontal de últimas 5 acciones | `0909237` | 3 (`timelineAcciones.ts` + `TimelineAcciones.tsx` + `OrdenDetalle.tsx`) | Lectura pura, parser tolerante a shapes legacy, responsive mobile/desktop. |
| SPRINT-114 | 4 hits descriptivos a `currentUser.uid` | `fc74fec` | 5 + fix colateral warning eslint | Sin migración de datos viejos. Defensa anticipada del vector P-001. |
| SPRINT-115 (fase diagnóstico) | Script read-only de diagnóstico de notificaciones | `f6d1d76` | 1 (`scripts/diagnostico-notificaciones-yohana.ts`) + docs | Solo agrega script utilitario. La ejecución y la fase write (re-migración) quedan para Jorge — re-migración requiere OK en `BLOQUEOS.md`. |

## Sprints bloqueados (REQUIEREN TU OK)

Ninguno nuevo. `BLOQUEOS.md` sigue vacío.

## Sprints PENDIENTE no procesados (con razón)

| ID | Título | Razón para no procesar autónomo |
|---|---|---|
| SPRINT-100 | Validar notificaciones Yohana | Bloqueado por SPRINT-115 (causa raíz). Se cerrará cuando Jorge corra el script de diagnóstico, dé OK a la re-migración (si aplica) y Yohana valide el re-QA. |
| SPRINT-112 | Schema drift + matriz permisos | Scope grande con QA manual por rol bloqueante para "completado". Procesable la parte documental, pero conviene agruparlo con disponibilidad humana. |
| SPRINT-113 (padre) | UX flujo paso a paso | 4 de 6 criterios COMPLETADOS por 113a/b/c. Quedan QA manual end-to-end (humano) y cazador de tooltips (sprint propio futuro). |
| SPRINT-115 (fase write) | Re-migración write de notificaciones de Yohana | BLOQUEADO esperando OK de Jorge. Acción requerida: correr primero el script de diagnóstico autónomo recién entregado y, según resultado, aprobar la re-migración en `BLOQUEOS.md`. |

## Cazadores anti-regresión

- **P-001** (`userProfile.id` misuse): 0 hits.
- **P-002** (rules sobre campo opcional sin `.get()`): 0 hits.
- **P-003** (cross-collection sin runTransaction): 0 hits.
- **P-004** (alta empleado sin doc espejo): 0 hits.
- **P-005** (rules pendientes de deploy): 0 hits.
- **P-006** (dropdown que asigna empleado guarda `personal.id`): 0 hits.

## Hallazgos clave de la auditoría SPRINT-111

1. **Cobertura completa.** Los cazadores P-001 + P-006 más las gotchas en CLAUDE.md cubren todos los vectores con riesgo de permission-denied.
2. **0 bugs latentes nuevos.** Los 12 campos auditados (`tecnicoId`, `ayudanteId`, `operariaId`, `responsableId`, `creadaPor`, `creadoPor`, `eliminadaPor`, `aprobadoPor`, `sugeridaPor`, `resueltaPor`, `cerradaPor`, `usuarioId`, `personalUid`) están todos OK o en categoría "INCONSISTENCIA_BAJO_RIESGO" (campos descriptivos sin rule activa contra `auth.uid`).
3. **4 inconsistencias de bajo riesgo** (no urgentes) → SPRINT-114 sugerido para cerrar consistencia. Sin migración de datos.
4. **Decisión técnica importante:** NO se crea cazador determinístico genérico nuevo. Solaparía con P-001 y P-006 sin agregar señal. Mejor refinar el agente semántico `regression_guardian` con regla cualitativa.

## Alertas

Ninguna. Sistema en estado saludable post-hotfix Aury (2026-05-07) y SPRINT-108 (cierre de disciplina).

## Próximos pasos sugeridos

- **Acción requerida de Jorge para SPRINT-100/115:** correr `npx tsx scripts/diagnostico-notificaciones-yohana.ts <email-de-yohana>` con `service-account.json` en la raíz del repo. Pegar output al chat. Según el caso (A/B/0 docs), decidir si desbloquear re-migración write en `BLOQUEOS.md`.
- **QA manual del UX flujo (SPRINT-113 padre):** Jorge + un técnico (Aury) + una operaria (Wilainy/Yohana) recorren un flujo end-to-end. Identificar friction points en banner / badges / tooltips / timeline. Si todo OK, marcar SPRINT-113 padre COMPLETADO.
- **Si Jorge prefiere seguir auditoría:** procesar SPRINT-112 fase documental (script schema drift + tabla matriz permisos teórica), dejando QA manual como sub-sprint follow-up. Es el sprint pendiente más grande.
- **Cola autónoma queda vacía de scope acotado.** Para próxima pasada, Cowork debería agregar nuevos sprints a partir de pedidos de Jorge.

## Update quinta pasada (Cowork ↔ Jorge — SPRINT-117 fase A2 porción read-only)

- Coordinator detectó conflicto: SPRINT-116 marcado ABSORBIDO por SPRINT-117 fase A2 — la cola pidió ejecutar la fase A2 completa pero su scope total (lectura exhaustiva + Information Architecture) es masivo y no procesable en una sola ventana sin degradar calidad.
- Jorge respondió "1" al menú de caminos: entregar **solo la porción read-only** de A2 (los 2 scripts originalmente alcance de SPRINT-116 fases A y B) como avance parcial, sin tocar A1/A3/A2-remanente.
- Coordinator procesó:
  - `ac54662` — `scripts/auditoria-emails-personal-vs-usuarios.ts` (289 líneas, read-only). Audita `personal/` × `usuarios/{uid}` × `admin.auth().getUser()` para todos los empleados con uid no vacío. Reporta matriz y conteos (ok / case / mismatch / usuarios_missing / auth_missing).
  - `6defe8f` — `scripts/auditoria-notis-legacy-todos.ts` (295 líneas, read-only). Generaliza el diagnóstico de Yohana a todos los empleados. Para cada uno, ejecuta las 4 queries (`userId`/`destinatarioId` × `auth.uid`/`personalDocId`), clasifica en OK/Caso A/Caso B/OTRO y reporta ids exactos de afectados.
- Tester: typecheck + lint + cazadores 6/6 PASS.
- regression_guardian: PASS — ambos scripts son server-side Admin SDK; ningún P-001..P-006 aplica.
- Reviewer: APPROVED.
- Verificación read-only por grep: confirmado sin `addDoc`/`setDoc`/`updateDoc`/`deleteDoc`/`FieldValue` (único hit de `dedup.set(...)` es `Map.set` en memoria).
- **Próximo paso humano:** Jorge ejecuta los 2 scripts contra producción y captura output. Si revelan empleados afectados (>0): abrir sprint write acotado en BLOQUEOS.md.
- **Lo que NO se hizo (queda PENDIENTE):** A1 (lectura exhaustiva del código), A3 (Information Architecture), A2-remanente (auditoría funcional sobre código vivo). Pasada exhaustiva futura — no se procesan en la misma ventana por riesgo de degradación.
- SPRINT-116 sigue ABSORBIDO (no tocado).

## Histórico (acumulado de pasadas anteriores)

- 2026-05-05: hotfix Reactivación (`afc5e4a` + `c7c8e34`) — origen P-001 y P-002.
- 2026-05-06: SPRINT-101 a 105 (sistema anti-regresión + GestionUsuarios doble doc) — origen P-003 y P-004.
- 2026-05-07: SPRINT-106 (rules deploy P-005), SPRINT-107 (archivist + Continuous Improvement Loop), SPRINT-108 (cierre disciplina hotfix Aury + P-006), SPRINT-109/110 (validación retroactiva).
- 2026-05-08 (hoy): SPRINT-111 fase documental + SPRINT-113a/b/c UX completo + SPRINT-114 consistencia auth.uid + SPRINT-115 diagnóstico + write Yohana + SPRINT-117 fase A2 porción read-only (2 scripts auditoría sistémica). UX flujo de orden ahora tiene capa visual paso a paso completa para los 3 roles principales. Diagnóstico individual + auditoría sistémica de notificaciones legacy listo para ejecución manual de Jorge.

# Diario — 2026-05-21

> Actualizado por coordinator autónomo `trabaja` pasada 31 (segunda pasada del día). Sobreescribe el diario de la pasada 30 (que era de sync documental sin código). Resumen acumulado del día: 3 sprints de código + 1 sync de cola previo + 0 hotfixes.

## Resumen (pasada 31)

- **3 sprints completados.** SPRINT-INBOX-7 (selector plantillas), SPRINT-INBOX-8 (crear orden en contexto), SPRINT-PAGOS-CONFIRMA-MARIA fase A (separación funciones client-side).
- **1 sprint escalado a BLOQUEOS** — SPRINT-PAGOS-CONFIRMA-MARIA fase B (refactor estructural: subcolección + rule + migración + 7 consumidores). Razón: scope cambió post-auditoría 2026-05-20 y la sub-regla CLAUDE.md exige plan de deploy en fases aprobado por Jorge antes de tocar el flujo de cobros.
- **3 commits pusheados** + 1 commit pendiente de sync documental cola/bloqueos/diario.
- ~2 horas de trabajo del coordinator (incluye lectura, build/test entre cada sprint, push, sync).
- Cazadores 17/17 PASS en cada commit.

## Sprints completados (pasada 31)

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-INBOX-7-SELECTOR-PLANTILLAS | Selector de plantillas para responder fuera de ventana 24h | `63b0056` | `src/config/plantillasWhatsApp.ts` NUEVO, `src/components/inbox/SelectorPlantillas.tsx` NUEVO, `src/pages/InboxConversacion.tsx` | Catálogo 4 plantillas APPROVED con orden de variables verificado contra curl E2E del DIARIO_2026-05-19. Wizard precarga desde cliente + orden activa. Sin tocar endpoint/rules. |
| SPRINT-INBOX-8-CREAR-ORDEN-EN-CONTEXTO | Crear orden y cliente nuevo SIN salir del inbox | `4d4cbda` | `src/components/inbox/CardCliente.tsx`, `src/pages/InboxConversacion.tsx` | Reusa `useOrdenCreateForm` + `OrdenCreateModal`. Banner emerald cuando isNewCliente. El path isNewCliente del hook YA crea el cliente al guardar (verificado). NO modifica Ordenes.tsx. |
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-A | Separación de funciones operaria/María client-side | `e3a49ed` | `src/types/index.ts`, `src/pages/GestionUsuarios.tsx`, `src/components/facturacion-pendiente/ProcesarFacturacionModal.tsx`, `src/components/ordenes/RegistrarPagoModal.tsx` | Permiso `pagosVerificar` + gate checkbox + C3 bloqueo conduce + M2 gate eliminar pago verificado + pagos nuevos `verificado=false` explícito + badge "PENDIENTE DE CONFIRMAR" + categoría editor permisos. **Retrocompat:** pagos legacy con `verificado===undefined` NO se bloquean. |

## Sprints escalados a BLOQUEOS (pasada 31)

| ID | Por qué | Cómo desbloquear |
|---|---|---|
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B | Approach corregido por auditoría 2026-05-20 cambió scope de "agregar permiso" a "refactor del modelo de datos + 7 consumidores + migración + rule". Sub-regla CLAUDE.md "Touch-list expandido si >5 consumidores → dividir en fases" + "Mutaciones cross-collection sobre dinero requieren plan aprobado". | Jorge elige opción de plan de deploy (1: 3 fases con QA entre cada / 2: single-shot) → edita `BLOQUEOS.md` con `OK: jorge YYYY-MM-DD HH:MM opcion N` → pega `procesa bloqueos` al coordinator. Ver entrada detallada al tope de BLOQUEOS.md. |

## Sprints bloqueados activos (carrying-over de pasada anterior + nuevos)

Mantienen los 10 bloqueos detallados en el diario de pasada 30 (línea 31-43 del diario anterior). Bloqueos relacionados con WhatsApp/CRM siguen igual. **Adición:** `SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B` agregado al tope de BLOQUEOS.md.

## Cazadores anti-regresión

Estado: **17/17 activos**. Sin hits en ningún commit de esta pasada.

- P-001 (`userProfile.id ≠ auth.uid`): 0 hits.
- P-002 (`rules .get(field, null)`): 0 hits.
- P-003 (cross-collection sin `runTransaction`): 0 hits.
- P-004 (alta empleado sin doc espejo en `usuarios/`): 0 hits.
- P-005 (`firestore.rules` modificado pero no deployado): 0 hits (no se tocaron rules en pasada 31).
- P-006 (`tecnicoId` con `personal.id` en lugar de `auth.uid`): 0 hits.
- P-007 (`crearNotificacion userId` con `personal.id`): 0 hits.
- P-009 (campo del tipo ausente en parser): 0 hits.
- P-010 (tipo notificación declarado sin call site): 0 hits.
- P-011 (updateDoc orden con flag terminal sin sincronizar fase): 0 hits.
- P-012 (`onSnapshot` sin `where` contra rule `auth.uid==X`): 0 hits.
- P-014 (`addDoc/setDoc clientes` sin guard `telefonoNormalizado`): 0 hits.
- P-015 (`orderBy` sobre campo no garantizado): 0 hits.
- P-016/017/018/019 (HMAC webhook WA, idempotency, ventana 24h, Meta error swallow): 0 hits.

## Postmortems nuevos

0. No hubo bugs en producción durante esta pasada. Los 3 sprints fueron features nuevas + un cambio de control financiero (fase A) sin reportes de regresión.

## Cazadores nuevos

0. Ningún patrón nuevo detectado para catalogar como P-XXX.

## Alertas

- **Importante:** SPRINT-PAGOS-CONFIRMA-MARIA fase A introduce el flag `verificado=false` explícito en pagos nuevos. **Esto cambia el comportamiento esperado a partir del próximo deploy.** Antes del deploy, operaria/secretaria podían registrar + tildar "Pago verificado" en el mismo paso del flujo de emitir conduce. Después del deploy:
  - Operaria registra pago → queda explícitamente `verificado=false`.
  - El conduce NO se emite si CUALQUIER pago previo tiene `verificado=false`.
  - Solo admin/coordinadora puede tildar "Pago verificado" → cambia el flag a `true`.
  - **Jorge debe avisar al equipo** (María, operarias) sobre el nuevo flujo: "ahora María (o el admin) tiene que confirmar el pago antes de emitir conduce. La operaria registra como siempre, pero el check de verificado solo aparece tildable para María/admin."
  - Pagos LEGACY (registrados antes del deploy, sin el flag `verificado`) NO se ven afectados — siguen funcionando como antes (no bloquean el conduce).
- Sin deploy fallido reportado.
- QA flujo facturación PENDIENTE — Jorge ejercita post-deploy para validar A2/C3/M2 según criterios de la spec.

## Próximos pasos sugeridos

1. **Jorge ejercita QA del flujo de pagos** post-deploy:
   - Login como operaria (Wilainy/Yohana) → abrir orden con pago pendiente → registrar pago → verificar que NO puede tildar "Pago verificado" (disabled + tooltip).
   - Login como María (coordinadora) → ir a `Facturación pendiente` → procesar orden → ver pagos previos con badge "PENDIENTE DE CONFIRMAR" → tildar "Pago verificado" → emitir conduce.
   - Intentar emitir conduce con pago previo sin confirmar → debe bloquear con toast claro.
   - Probar QA inbox: abrir conversación con ventana 24h cerrada → clickear "Enviar plantilla" → elegir `cita_confirmada` → ver variables precargadas (si hay orden activa) → enviar.
   - Probar QA inbox crear orden: abrir conversación con cliente existente → "Crear orden para este cliente" → modal abre EN el inbox con datos precargados → guardar → orden creada, vuelta al inbox.

2. **Jorge decide plan de deploy de fase B** (subcolección + rule + migración). Ver entrada `SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B` al tope de BLOQUEOS.md. **Recomendación del coordinator:** opción 1 (3 fases con QA entre cada), porque toca dinero + rules + 7 consumidores.

3. **Devops verifica deploy Vercel** de los 3 commits (`63b0056`, `4d4cbda`, `e3a49ed`) — el push fue exitoso, queda confirmar Ready en https://www.misterservicerd.com/.

## Métricas del día (pasada 31)

- Sprints completados: 3 (INBOX-7, INBOX-8, PAGOS-FASE-A).
- Sprints escalados a BLOQUEOS: 1 (PAGOS-FASE-B).
- Reverts: 0.
- Hotfixes: 0.
- Postmortems nuevos: 0.
- Cazadores nuevos: 0.
- Commits: 3 de código + 1 (próximo) de sync documental.
- Archivos modificados: 9 (3 nuevos, 6 editados).
- Tiempo total: ~2 horas.

# Diario — 2026-05-21

> Actualizado por coordinator autónomo `trabaja` pasada 35 (sexta pasada del día, **BLOQUE NOCTURNO**). Se preservan pasadas 31, 32, 33 y 34 abajo. Resumen acumulado del día: **8 sprints de código** + 1 sync de cola previo + 0 hotfixes + 2 pasadas no-op (32 y 34) + 1 sprint escalado limpio.

---

## Pasada 35 (trabaja — BLOQUE NOCTURNO: 4 sprints procesados, 1 escalado)

**Disparada:** Jorge pegó `trabaja toda la cola: procesa el BLOQUE NOCTURNO` con instrucciones explícitas de orden (FEED → FUNNEL → WA-METRICS → INBOX-9), regla "escalar a BLOQUEOS y continuar si topa rules/endpoint/migración/decisión", y prohibición de tocar PAGOS-FASE-B-2.

**Resultado:** **3 sprints COMPLETADOS, 1 ESCALADO limpio a BLOQUEOS.md, 0 hotfixes, 3 commits + 3 push a main.** Cazadores 17/17 PASS en cada commit (typecheck + cazadores + lint via pre-commit hook husky). Sin tocar PAGOS-FASE-B-2.

### Sprints procesados (pasada 35)

| Sprint | Estado | Commit | Deploy | Bloqueo (si aplica) |
|---|---|---|---|---|
| SPRINT-FEED-UNIFICADO-ORDEN | COMPLETADO | `6a3ec1d` | Vercel pendiente | — |
| SPRINT-FUNNEL-CONVERSION-FASES | COMPLETADO | `7c83a30` | Vercel pendiente | — |
| SPRINT-WA-TEMPLATE-METRICS | COMPLETADO | `5f9c40f` | Vercel pendiente | — |
| SPRINT-INBOX-9-FOTOS-CHAT-ORDEN | ESCALADO BLOQUEOS | — | — | Endpoint público nuevo + storage.rules + decisión de negocio |

### Detalle por sprint

**SPRINT-FEED-UNIFICADO-ORDEN** (commit `6a3ec1d` — módulo #1 a copiar de Kommo):
- NUEVO `src/utils/timelineUnificado.ts` — helper que combina `historialFases` + `auditoria` + `whatsapp_mensajes_inbox/outbox` en un solo array cronológico descendente.
- NUEVO `src/components/ordenes/TimelineUnificadoOrden.tsx` — componente que se suscribe a `suscribirMensajes(wa_id)` derivado del teléfono del cliente (via `normalizarTelefono` → 10 dígitos RD = wa_id). Lectura pura, no escribe. Sin índice compuesto. Variant `'page' | 'modal'` para reusar en `OrdenDetalle` y `OrdenDetailModal`.
- Insertado en `OrdenDetalle.tsx` (variant page, max 80) antes del Registro de Cambios; y en `OrdenDetailModal.tsx` (variant modal, max 40, con scroll interno).
- Degrada gracefully: si el cliente no tiene `clienteTelefono` o el normalizar devuelve `''`, NO se suscribe y muestra solo fases+auditoría.

**SPRINT-FUNNEL-CONVERSION-FASES** (commit `7c83a30` — análogo a Win-Loss de Kommo):
- NUEVO `src/components/AnalisisFunnel.tsx` — componente reusable que recibe `ordenes: OrdenServicio[]` + `rangoFecha?` y calcula:
  - Cuántas órdenes pasaron por cada `FASES_ORDENADAS[i]` (única por orden, dentro del rango).
  - Cuántas están ACTUALMENTE en cada fase (snapshot).
  - % de conversión vs fase anterior (color-coded: ≥70% emerald, ≥40% amber, <40% red).
  - Tiempo promedio en cada fase (delta entre timestamps de `historialFases` cuando avanzaron a la siguiente).
  - Conversión global lead → cerrado en el header.
- Insertado en `MetricasMensuales.tsx` entre Proyección Total y Operarias. Reusa el array `ordenes` que la página ya carga. Sin query nueva. Sin índice.

**SPRINT-WA-TEMPLATE-METRICS** (commit `5f9c40f` — análogo a "WhatsApp Business stats" de Kommo):
- NUEVO `src/components/inbox/MetricasPlantillas.tsx` — listener global a `whatsapp_mensajes_outbox` (rule `firestore.rules:697` gatea por `esStaffOficina()`; cazador P-012 no aplica), filtra `tipo === 'plantilla'`, agrupa por `plantilla.nombre`, cuenta por `estado` (queued/sent/delivered/read/failed). Tabla: plantilla | enviadas | entregadas | leídas | fallidas | % lectura. % lectura color-coded.
- Insertado en `MetricasMensuales.tsx` debajo del funnel.

**SPRINT-INBOX-9-FOTOS-CHAT-ORDEN** (ESCALADO a BLOQUEOS):
- El webhook actual (`api/_lib/whatsappWebhook.ts:242`) solo guarda `contenido.mediaId` (id de Meta) — NO descarga ni almacena la imagen.
- Para usarla en la orden hace falta endpoint serverless nuevo (`api/whatsapp/media-proxy.ts`) que llame Meta Graph API con `WHATSAPP_ACCESS_TOKEN` (no se puede exponer al cliente).
- El repo NO tiene `storage.rules` versionado (`SPRINT-138` ya en bloqueos es prerequisito).
- Adicionalmente hay decisión de negocio pendiente: ¿adónde van las fotos? (3 opciones de approach documentadas en BLOQUEOS.md).
- Sprint anticipó el escalado: "Si re-subir requiere cambiar storage.rules → ESCALAR a BLOQUEOS y continuar con la cola."
- Detalles completos en `docs/sprints/BLOQUEOS.md` con instrucciones de desbloqueo (3 opciones A/B/C).

### Cazadores anti-regresión

17/17 PASS en cada commit (P-001..P-007, P-009..P-012, P-014..P-019). Sin hits nuevos.

### Tiempo

~40 minutos de coordinator. 4 sprints (3 código + 1 escalado).

### Próximos pasos

1. Jorge revisa los 3 commits del bloque nocturno en producción cuando Vercel haga deploy automático.
2. Si quiere desbloquear SPRINT-INBOX-9, leer `BLOQUEOS.md` y decidir Opción A/B/C.
3. Sigue pendiente QA Jorge de PAGOS-FASE-B-1 antes de procesar B.2.

---

## Pasada 34 (trabaja — no-op: cola vacía)

**Disparada:** Jorge pegó `trabaja` después de la pasada 33.

**Resultado:** **0 sprints procesables, 0 commits, 0 bloqueados nuevos.** Cola sin PENDIENTES nuevos desde pasada 33; `BLOQUEOS.md` sin OKs nuevos. No-op administrativo.

### Verificación realizada

| Fuente | Resultado |
|---|---|
| `docs/sprints/COLA_AUTONOMA.md` (tope) | Sprints SPRINT-INBOX-8b-DRAWER-LATERAL, SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-1, SPRINT-INBOX-7, SPRINT-INBOX-8, SPRINT-PAGOS-CONFIRMA-MARIA todos marcados COMPLETADO/ESCALADO en pasadas previas. Bloque SPRINT-INBOX-1..6 + SPRINT-WA-* completados/redundantes/movidos. Sin PENDIENTES procesables nuevos. |
| `docs/sprints/BLOQUEOS.md` (OKs con fecha 2026-05-21) | 3 OKs encontrados (`PAGOS-CONFIRMA-MARIA`, `INBOX-8b-DRAWER-LATERAL`, `PAGOS-FASE-B`) ya procesados en pasadas 30, 31 y 33. Sin OKs nuevos posteriores. |
| Sprints AWAITING QA Jorge | Sin cambios. PAGOS-FASE-B-2 sigue esperando confirmación de QA de B.1. Coordinator NO avanza autónomo. |

### Acciones

- Ninguna sobre código. Sin commits, sin push, sin deploy nuevo.
- Solo actualización de este diario (bloque pasada 34 anexado al tope).

### Próximos pasos esperados

1. Jorge ejercita QA manual de `/admin/pagos-pendientes` (B.1 commit `4fa8f08`).
2. Cuando confirme, Cowork agrega B.2 (refactor 7 consumidores + migración) a la cola.
3. Si Jorge quiere desbloquear más HSM/portal/IA → editar entradas en `BLOQUEOS.md` con `OK: jorge ...` y pegar `procesa bloqueos`.

---

## Pasada 33 (procesa bloqueos — 2 sprints desbloqueados + procesados)

**Disparada:** Jorge editó 2 entradas de `BLOQUEOS.md` con OKs y pegó `procesa bloqueos`.

**Resultado:** **2 sprints procesados, 0 bloqueados nuevos, 0 hotfixes.** Cola al final: vacía de procesables. PAGOS-FASE-B-1 marcado COMPLETADO pero awaiting QA Jorge antes de procesar B.2.

### OKs detectados

| Sprint | OK | Acción |
|---|---|---|
| SPRINT-INBOX-8b-DRAWER-LATERAL | `OK: jorge 2026-05-21 10:30 approach=A1 + items=4,5,6` | Movido a cola + procesado autónomo |
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B | `OK: jorge 2026-05-21 10:30 opcion 1` | Movido como FASE-B-1 (opción 1 = 3 fases con QA). Solo se procesó B.1 — B.2 y B.3 esperan QA Jorge |

### Sprints procesados (pasada 33)

| ID | Estado | Hash | Archivos | Deploy |
|---|---|---|---|---|
| SPRINT-INBOX-8b-DRAWER-LATERAL | COMPLETADO | `50688a1` | `OrdenCreateModal.tsx`, `MensajeBubble.tsx`, `InboxConversacion.tsx` | Vercel pendiente (push OK a main) |
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-1 | COMPLETADO (awaiting QA) | `4fa8f08` | `ordenes.service.ts`, `PagosPendientes.tsx` (NUEVO), `App.tsx`, `Sidebar.tsx` | Vercel pendiente (push OK a main) |

### Detalle por sprint

**SPRINT-INBOX-8b-DRAWER-LATERAL** (commit `50688a1`):
- `OrdenCreateModal` acepta prop opcional `presentationMode: 'modal' | 'drawer'`, default `'modal'` (sin regresión en Ordenes/Citas/OrdenEditForm/FormularioAgendarPublico).
- En modo drawer: panel lateral derecho 50% (lg+) sin overlay opaco. El chat queda visible.
- `MensajeBubble` acepta callbacks opcionales `onCopiarAOrden(texto)` y `onUsarUbicacion({lat,lng,direccion?})`. Si form está abierto, aparece ícono hover "→ copiar a orden" en burbujas entrantes con texto, y botón "Usar esta ubicación en la orden" en burbujas tipo location.
- Items 4+5+6 (refinamiento UX OBLIGATORIO de Jorge 2026-05-21) OK. Item 7 (fotos del chat → orden) NO procesado — queda como follow-up `SPRINT-INBOX-8c-FOTOS-A-ORDEN` (decisión explícita de Jorge en el OK).
- Cazadores 17/17 PASS. Sin tocar rules.

**SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-1** (commit `4fa8f08`):
- Helper `confirmarPagoOrden(ordenId, pagoId, confirmadoPor)` en `ordenes.service.ts` con runTransaction (P-003: orden + auditoria_admin atómico) + idempotencia DENTRO del callback (P-001 patrón `a38eb89`).
- Helper `suscribirPagosPendientes(callback)` para real-time de pagos `verificado===false` across órdenes activas. Sin orderBy → P-015 OK.
- Página `/admin/pagos-pendientes` (NUEVA) con lista de cards + botón "Confirmar pago". Gate interno + PermisoRoute en App.tsx (defense in depth).
- Entrada nueva en Sidebar sección "Cobranza y facturación" + badge count.
- NO tocó `firestore.rules` (eso es B.3). NO migró datos (eso es B.2). El gap defense-in-depth full se cierra solo cuando B.3 deploye la rule estricta — hoy un usuario que sortee la UI con console puede aún escribir `verificado=true` directo a Firestore.
- Cazadores 17/17 PASS.

### Sprints AWAITING QA Jorge (no procesados aún)

| Sprint | Razón | Próximo paso |
|---|---|---|
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-2 | Plan opción 1 exige QA Jorge de B.1 antes. Cuando confirme funciona "María entra a /admin/pagos-pendientes, ve pendientes, confirma 1, refresca, queda verde", Cowork agrega B.2 a la cola | Refactor 7 consumidores (`OrdenDetailModal`, `OrdenDetalle`, `AgendaDia`, `FacturacionPendiente`, `utils/index.ts`, `utils/tooltipsBotones`, `EnviarFacturacionButton`) + migración script `pagos[]` → subcolección |
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B-3 | Requiere B.2 cerrada y migración aplicada en producción | Deploy rule estricta de subcolección `ordenes_servicio/{id}/pagos/{pagoId}` con `.get(field,null)` (P-002). `npm run deploy:rules` antes de cerrar. |
| SPRINT-INBOX-8c-FOTOS-A-ORDEN | Item 7 del refinamiento INBOX-8b — fotos del chat WhatsApp adjuntadas a orden con un clic. Requiere análisis: ¿reverse-proxy Meta Media URL + re-subir a Firebase Storage? | Cowork escribirá spec con touch-list expandido cuando Jorge lo pida; actualmente nice-to-have |

### Bloqueos remanentes (sin cambios)

Los bloqueos WhatsApp HSM (WA-4/5/6/7), hardening notif, portal solicitud, conversaciones modelo, inbox global, 3-columnas, toggle bot, shortcut plantillas, órdenes desde chat, dashboard comunicación, resumen IA — todos siguen igual. Esperan OKs específicos de Jorge.

### Cazadores anti-regresión

17/17 activos, 0 hits en cada commit de pasada 33.

### Tiempo

~50 minutos (lectura inicial + 2 sprints + sync docs + commits + pushes).

---

## Pasada 32 (no-op procesable + 1 nuevo bloqueo creado)

**Disparada:** Jorge pegó `trabaja` tras pasada 31.

**Resultado:** **0 sprints procesados (cola PENDIENTE vacía), 1 sprint nuevo creado en BLOQUEOS** (`SPRINT-INBOX-8b-DRAWER-LATERAL`) tras detectar un refinamiento UX que Cowork inyectó dentro de un sprint ya cerrado.

### Estado verificado por el coordinator

1. **`COLA_AUTONOMA.md`** — los 3 sprints en el tope (INBOX-7, INBOX-8, PAGOS-CONFIRMA-MARIA fase A) están todos COMPLETADOS / ESCALADO. El bloque siguiente "SPRINT-INBOX-1..6" y resto del archivo son histórico o REDUNDANTE o MOVIDO A BLOQUEOS. Los 2 únicos hits de `^**Estado:** PENDIENTE` resultaron ser falsos positivos:
   - Línea 5886: plantilla genérica para sprints nuevos.
   - Línea 6538: spec original preservada dentro de `<details>` cerrado para SPRINT-117b (que ya está COMPLETADO en otra entrada arriba).
2. **`BLOQUEOS.md`** — `SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B` (al tope, agregado en pasada 31) sigue sin `OK: jorge ...`. Espera decisión Jorge entre opción 1 (3 fases con QA) y opción 2 (single-shot). Los OKs históricos del archivo son de bloqueos ya desbloqueados y procesados en pasadas previas (`SPRINT-149` 2026-05-12, `SPRINT-135a-UI` 2026-05-11, `SPRINT-118` 2026-05-08, `SPRINT-PAGOS-CONFIRMA-MARIA` 2026-05-20 — este último ya se movió a la cola en pasada 30 y se procesó fase A en pasada 31).
3. **Hallazgo importante de pasada 32:** Cowork agregó en `COLA_AUTONOMA.md:51-59` una sección "Refinamiento UX (Jorge 2026-05-21, OBLIGATORIO)" DENTRO del sprint `SPRINT-INBOX-8-CREAR-ORDEN-EN-CONTEXTO` que ya estaba marcado COMPLETADO en pasada 31. Esa sección agrega 4 items nuevos (drawer/panel lateral del form de orden, copiar mensaje a orden, ubicación → orden, fotos → orden) que pasada 31 NO procesó. La spec misma dice "Si refactorizar `OrdenCreateModal` a contenedor no-modal es muy invasivo, el builder debe REPORTAR y proponer alternativa" → es un stop autónomo explícito.
4. **Acción tomada:** se creó `SPRINT-INBOX-8b-DRAWER-LATERAL` en `BLOQUEOS.md` (al tope) con 3 approaches propuestos (A1 modal-con-mode-prop, A2 wrapper drawer dedicado, A3 form embedido en cuarta columna del layout inbox) + touch-list aproximado para cada uno + cómo desbloquear (Jorge elige approach + items). Sin tocar `COLA_AUTONOMA.md` para preservar la referencia histórica.

### Sprints nuevos en BLOQUEOS (pasada 32)

| ID | Por qué | Cómo desbloquear |
|---|---|---|
| SPRINT-INBOX-8b-DRAWER-LATERAL | Refinamiento UX inyectado en INBOX-8 ya cerrado: form de orden al lado del chat (no modal centrado) + copiar mensaje a orden + ubicación → orden + fotos → orden. Spec advierte "refactor invasivo → REPORTAR antes de forzar" → stop autónomo. Riesgo de romper `Ordenes.tsx`/`Citas.tsx` que comparten `OrdenCreateModal`. | Jorge elige approach (A1/A2/A3) + scope de items (4 solos, 4+5+6, full 4-7) → edita `BLOQUEOS.md` con `OK: jorge 2026-05-21 HH:MM approach=A<N> + items=<lista>` → `procesa bloqueos`. |

### Próximo paso para Jorge

Para destrabar trabajo nuevo, cualquiera de estas:
- **Opción A** — destrabar PAGOS fase B: editar `docs/sprints/BLOQUEOS.md` entrada `SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B` con `OK: jorge 2026-05-21 HH:MM opcion 1` (recomendado) u `opcion 2`, después `procesa bloqueos`.
- **Opción B** — destrabar INBOX-8b: editar entrada `SPRINT-INBOX-8b-DRAWER-LATERAL` con approach + items elegidos.
- **Opción C** — pedirle a Cowork sprints nuevos (features, hallazgos del QA post-deploy de pasada 31, etc.) → Cowork los pone en `COLA_AUTONOMA.md` → `trabaja` los procesa.

### Tiempo

~10 minutos (lectura de cola + bloqueos + detectar hallazgo INBOX-8b + redactar nuevo bloqueo + sync diario + commit).

---

> Actualizado por coordinator autónomo `trabaja` pasada 31 (segunda pasada del día). Sobreescribe el diario de la pasada 30 (que era de sync documental sin código). Resumen acumulado del día: 3 sprints de código + 1 sync de cola previo + 0 hotfixes.

## Resumen (pasada 31)

- **3 sprints completados.** SPRINT-INBOX-7 (selector plantillas), SPRINT-INBOX-8 (crear orden en contexto), SPRINT-PAGOS-CONFIRMA-MARIA fase A (separación funciones client-side).
- **1 sprint escalado a BLOQUEOS** — SPRINT-PAGOS-CONFIRMA-MARIA fase B (refactor estructural: subcolección + rule + migración + 7 consumidores). Razón: scope cambió post-auditoría 2026-05-20 y la sub-regla CLAUDE.md exige plan de deploy en fases aprobado por Jorge antes de tocar el flujo de cobros.
- **3 commits pusheados** + 1 commit pendiente de sync documental cola/bloqueos/diario.
- ~2 horas de trabajo del coordinator (incluye lectura, build/test entre cada sprint, push, sync).
- Cazadores 17/17 PASS en cada commit.

## Sprints completados (pasada 31)

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-INBOX-7-SELECTOR-PLANTILLAS | Selector de plantillas para responder fuera de ventana 24h | `63b0056` | `src/config/plantillasWhatsApp.ts` NUEVO, `src/components/inbox/SelectorPlantillas.tsx` NUEVO, `src/pages/InboxConversacion.tsx` | Catálogo 4 plantillas APPROVED con orden de variables verificado contra curl E2E del DIARIO_2026-05-19. Wizard precarga desde cliente + orden activa. Sin tocar endpoint/rules. |
| SPRINT-INBOX-8-CREAR-ORDEN-EN-CONTEXTO | Crear orden y cliente nuevo SIN salir del inbox | `4d4cbda` | `src/components/inbox/CardCliente.tsx`, `src/pages/InboxConversacion.tsx` | Reusa `useOrdenCreateForm` + `OrdenCreateModal`. Banner emerald cuando isNewCliente. El path isNewCliente del hook YA crea el cliente al guardar (verificado). NO modifica Ordenes.tsx. |
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-A | Separación de funciones operaria/María client-side | `e3a49ed` | `src/types/index.ts`, `src/pages/GestionUsuarios.tsx`, `src/components/facturacion-pendiente/ProcesarFacturacionModal.tsx`, `src/components/ordenes/RegistrarPagoModal.tsx` | Permiso `pagosVerificar` + gate checkbox + C3 bloqueo conduce + M2 gate eliminar pago verificado + pagos nuevos `verificado=false` explícito + badge "PENDIENTE DE CONFIRMAR" + categoría editor permisos. **Retrocompat:** pagos legacy con `verificado===undefined` NO se bloquean. |

## Sprints escalados a BLOQUEOS (pasada 31)

| ID | Por qué | Cómo desbloquear |
|---|---|---|
| SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B | Approach corregido por auditoría 2026-05-20 cambió scope de "agregar permiso" a "refactor del modelo de datos + 7 consumidores + migración + rule". Sub-regla CLAUDE.md "Touch-list expandido si >5 consumidores → dividir en fases" + "Mutaciones cross-collection sobre dinero requieren plan aprobado". | Jorge elige opción de plan de deploy (1: 3 fases con QA entre cada / 2: single-shot) → edita `BLOQUEOS.md` con `OK: jorge YYYY-MM-DD HH:MM opcion N` → pega `procesa bloqueos` al coordinator. Ver entrada detallada al tope de BLOQUEOS.md. |

## Sprints bloqueados activos (carrying-over de pasada anterior + nuevos)

Mantienen los 10 bloqueos detallados en el diario de pasada 30 (línea 31-43 del diario anterior). Bloqueos relacionados con WhatsApp/CRM siguen igual. **Adición:** `SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B` agregado al tope de BLOQUEOS.md.

## Cazadores anti-regresión

Estado: **17/17 activos**. Sin hits en ningún commit de esta pasada.

- P-001 (`userProfile.id ≠ auth.uid`): 0 hits.
- P-002 (`rules .get(field, null)`): 0 hits.
- P-003 (cross-collection sin `runTransaction`): 0 hits.
- P-004 (alta empleado sin doc espejo en `usuarios/`): 0 hits.
- P-005 (`firestore.rules` modificado pero no deployado): 0 hits (no se tocaron rules en pasada 31).
- P-006 (`tecnicoId` con `personal.id` en lugar de `auth.uid`): 0 hits.
- P-007 (`crearNotificacion userId` con `personal.id`): 0 hits.
- P-009 (campo del tipo ausente en parser): 0 hits.
- P-010 (tipo notificación declarado sin call site): 0 hits.
- P-011 (updateDoc orden con flag terminal sin sincronizar fase): 0 hits.
- P-012 (`onSnapshot` sin `where` contra rule `auth.uid==X`): 0 hits.
- P-014 (`addDoc/setDoc clientes` sin guard `telefonoNormalizado`): 0 hits.
- P-015 (`orderBy` sobre campo no garantizado): 0 hits.
- P-016/017/018/019 (HMAC webhook WA, idempotency, ventana 24h, Meta error swallow): 0 hits.

## Postmortems nuevos

0. No hubo bugs en producción durante esta pasada. Los 3 sprints fueron features nuevas + un cambio de control financiero (fase A) sin reportes de regresión.

## Cazadores nuevos

0. Ningún patrón nuevo detectado para catalogar como P-XXX.

## Alertas

- **Importante:** SPRINT-PAGOS-CONFIRMA-MARIA fase A introduce el flag `verificado=false` explícito en pagos nuevos. **Esto cambia el comportamiento esperado a partir del próximo deploy.** Antes del deploy, operaria/secretaria podían registrar + tildar "Pago verificado" en el mismo paso del flujo de emitir conduce. Después del deploy:
  - Operaria registra pago → queda explícitamente `verificado=false`.
  - El conduce NO se emite si CUALQUIER pago previo tiene `verificado=false`.
  - Solo admin/coordinadora puede tildar "Pago verificado" → cambia el flag a `true`.
  - **Jorge debe avisar al equipo** (María, operarias) sobre el nuevo flujo: "ahora María (o el admin) tiene que confirmar el pago antes de emitir conduce. La operaria registra como siempre, pero el check de verificado solo aparece tildable para María/admin."
  - Pagos LEGACY (registrados antes del deploy, sin el flag `verificado`) NO se ven afectados — siguen funcionando como antes (no bloquean el conduce).
- Sin deploy fallido reportado.
- QA flujo facturación PENDIENTE — Jorge ejercita post-deploy para validar A2/C3/M2 según criterios de la spec.

## Próximos pasos sugeridos

1. **Jorge ejercita QA del flujo de pagos** post-deploy:
   - Login como operaria (Wilainy/Yohana) → abrir orden con pago pendiente → registrar pago → verificar que NO puede tildar "Pago verificado" (disabled + tooltip).
   - Login como María (coordinadora) → ir a `Facturación pendiente` → procesar orden → ver pagos previos con badge "PENDIENTE DE CONFIRMAR" → tildar "Pago verificado" → emitir conduce.
   - Intentar emitir conduce con pago previo sin confirmar → debe bloquear con toast claro.
   - Probar QA inbox: abrir conversación con ventana 24h cerrada → clickear "Enviar plantilla" → elegir `cita_confirmada` → ver variables precargadas (si hay orden activa) → enviar.
   - Probar QA inbox crear orden: abrir conversación con cliente existente → "Crear orden para este cliente" → modal abre EN el inbox con datos precargados → guardar → orden creada, vuelta al inbox.

2. **Jorge decide plan de deploy de fase B** (subcolección + rule + migración). Ver entrada `SPRINT-PAGOS-CONFIRMA-MARIA-FASE-B` al tope de BLOQUEOS.md. **Recomendación del coordinator:** opción 1 (3 fases con QA entre cada), porque toca dinero + rules + 7 consumidores.

3. **Devops verifica deploy Vercel** de los 3 commits (`63b0056`, `4d4cbda`, `e3a49ed`) — el push fue exitoso, queda confirmar Ready en https://www.misterservicerd.com/.

## Métricas del día (pasada 31)

- Sprints completados: 3 (INBOX-7, INBOX-8, PAGOS-FASE-A).
- Sprints escalados a BLOQUEOS: 1 (PAGOS-FASE-B).
- Reverts: 0.
- Hotfixes: 0.
- Postmortems nuevos: 0.
- Cazadores nuevos: 0.
- Commits: 3 de código + 1 (próximo) de sync documental.
- Archivos modificados: 9 (3 nuevos, 6 editados).
- Tiempo total: ~2 horas.

# Diario — 2026-05-19

## Sesión: módulo WhatsApp arranca producción (4 sprints + 1 hotfix)

Sesión continuación de la Opción A confirmada el 2026-05-18 noche. Jorge respondió las 10 decisiones de negocio (D1-D10) y el coordinator procesó en cascada SPRINT-WA-0-CIERRE → SPRINT-WA-RULES → SPRINT-WA-1, sumando un follow-up de propagación D3 y un hotfix urgente del webhook.

**5 commits pusheados al main:**

| # | Commit | Sprint | Notas |
|---|---|---|---|
| 1 | `6e477d1` | SPRINT-WA-0-CIERRE | 10 decisiones firmes + script init-whatsapp-config.ts (idempotente) + blocker D3=B documentado en WA-6 |
| 2 | `be0ef32` | SPRINT-WA-RULES | 6 colecciones WhatsApp + 13 invariantes de inmutabilidad + 2 fixes de privilege escalation cazados por security audit + deploy a Firebase |
| 3 | `4576f5f` | SPRINT-WA-0 follow-up D3 | Jorge cambió D3 B→A (bot 24/7 sin restricción horaria). Propagación a script + MODULO + bot prompt + reformulación WA-6 (plantilla pasa de blocker a fallback opcional) |
| 4 | `142d4da` | SPRINT-WA-1 | Webhook entrante con HMAC + idempotency + 5 defenses-in-depth aplicadas tras security audit + 2 cazadores nuevos (P-016, P-017). 1678 líneas agregadas |
| 5 | `50931bb` | SPRINT-WA-1-HOTFIX | Endpoint crasheaba en module-load por import de `FieldValue` desde wrapper que no la exporta. Fix de 2 líneas. Postmortem generado |

**Cazadores 13 → 15** activos (P-016 HMAC + P-017 idempotency nuevos).

**Rules deployadas a Firebase** (`firestore.rules.deployed.lock` sha256 `e8e7fa68dce8...` a las 12:02 UTC).

---

## Decisiones de negocio firmes (SPRINT-WA-0)

| # | Decisión | Valor firme | Implementación |
|---|---|---|---|
| D1 | Número default envío | Sticky por conversación (override manual) | `whatsapp_conversaciones.ultimoPhoneNumberId` |
| D2 | 2 números → conversaciones | 1 conversación por wa_id | Doc id único, `phoneNumberId` por mensaje |
| D3 | Horario bot | **Cambio B→A mid-sesión:** bot 24/7 con escalación por triggers | `bot.horario.activo=false`, plantilla queda como fallback opcional |
| D4 | Plantillas autónomas | Solo UTILITY | Bot no manda marketing |
| D5 | Límite turnos | 20 | `bot.limiteTurnosConversacion` |
| D6 | Roles envío UI | admin/coord/secretaria/operaria | Gate en `api/whatsapp/send.ts` (WA-2 lo implementa) |
| D7 | Body plantillas | Cron sync primero, mapping ajustable | WA-5 deploya antes que WA-2 |
| D8 | Opt-out automático | STOP/BAJA → `optOuts[]` + `optOutMarketing=true` | Webhook + send respetan |
| D9 | Plan Vercel | Pro (3 crons) | Sin consolidar |
| D10 | Nombre bot | Fixman, usted/tú adaptive | System prompt v1.0 |

---

## Pasada 1 — SPRINT-WA-0-CIERRE (commit `6e477d1`)

- Doc actualizado: `docs/MODULO_WHATSAPP.md` sección "Decisiones de negocio FIRMES" con tabla de los 10 valores.
- Doc actualizado: `docs/sprints/BLOQUEOS.md` — SPRINT-WA-0 marcado COMPLETADO, SPRINT-WA-6 con blocker nuevo por D3=B (plantilla auto-respuesta).
- Script nuevo: `scripts/init-whatsapp-config.ts` (335 líneas) — idempotente, modos default/dry-run/force, patrón de auth idéntico a `backfill-usuarios-desde-personal.ts`. Persiste los 10 valores + 2 números activos + horario + límite turnos + palabras escalado/urgencia/opt-out + routing zonas vacío + costos referencia. Jorge corre con `npx tsx scripts/init-whatsapp-config.ts` POST-WA-RULES deploy.
- 13 cazadores PASS.

---

## Pasada 2 — SPRINT-WA-RULES (commit `be0ef32`)

- 6 rules nuevas insertadas en `firestore.rules` entre `config_marketing` y `auditoria_admin`:
  - `whatsapp_mensajes_inbox`: read esStaffOficina, write deny.
  - `whatsapp_mensajes_outbox`: read esStaffOficina, write deny.
  - `whatsapp_conversaciones`: read esStaffOficina, update parcial con 13 invariantes de inmutabilidad + control asimétrico de asignación + bot.habilitado restringido.
  - `whatsapp_plantillas`: read esStaff, write deny.
  - `whatsapp_recordatorios_enviados`: read esStaffOficina, write deny.
  - `whatsapp_config`: read esAdminOCoord, write esAdmin.
- **Security audit cazó 2 BLOQUEADORES que reviewer pasó por alto:**
  - **H.2 — privilege escalation en `asignadaA`**: el primer fix (`in [actual, null, uid]`) NO prevenía robo entre operarias porque `request.auth.uid` siempre está en la lista. Reemplazado por filtro asimétrico explícito (no-op | tomar null→propio | soltar propio→null) + bypass admin/coord.
  - **H.1 — sabotaje masivo de `bot.habilitado`**: sin restricción, un insider podía apagar el bot en todas las conversaciones. Agregado check: solo admin/coord o asignataria de la conv puede flippear.
- **CONCERN aplicado:** `ultimaActividad` inmutabilizado (timestamp server-only).
- **PII trade-off documentado en comentario:** `inbox.raw` accesible a staff oficina (statu quo del negocio) + mitigación operativa (revocar Firebase Auth al salir empleados).
- Deploy a Firebase: `firestore.rules` sha `e8e7fa68dce8...` lock actualizado.
- 13 cazadores PASS (P-005 verde post-deploy).

---

## Pasada 3 — SPRINT-WA-0 follow-up D3 B→A (commit `4576f5f`)

Jorge cambió D3 de B (horario L-S 8-18 + plantilla fuera) a A (bot 24/7) en BLOQUEOS.md mientras la pasada 2 estaba en curso. Propagación a 4 archivos:

- `scripts/init-whatsapp-config.ts`: `horario.activo=false`, `fueraDeHorario='siempre_bot'`, agregado `plantillaFueraHorarioModo='fallback_emergencia'`.
- `docs/MODULO_WHATSAPP.md`: tabla decisiones firmes fila D3 ajustada, párrafo "Cambio D3 B→A" reemplaza al de "Bloqueador nuevo".
- `docs/specs/bot-ia-system-prompt.md`: regla 24/7 reemplaza "fuera de horario silencio"; Trigger 5 marcado N/A; tabla límites operativos actualizada; D3 marcado RESUELTO en sección de decisiones pendientes.
- `docs/sprints/BLOQUEOS.md`: WA-0 respuesta D3 corregida; WA-6 sub-sección "Blocker nuevo" reformulada a "Resuelto 2026-05-19 — D3 cambió B→A" (plantilla pasa de blocker a fallback opcional). WA-6 ya NO depende de plantilla en Meta.
- 13 cazadores PASS.

**Nota operativa**: push fuera de horario laboral a Maria/Wilainy debe respetar criticidad (no spamear a las 3am). Lógica sugerida documentada en WA-6: motivo crítico → push siempre, motivo genérico → encolar para próximo turno laboral. Implementación final durante el sprint.

---

## Pasada 4 — SPRINT-WA-1 webhook entrante (commit `142d4da`)

Sprint grande. Delegado al builder + 4 validadores en paralelo (tester local + regression_guardian + reviewer + security).

**Archivos producidos:**
- `api/whatsapp/webhook.ts` (497 líneas) — GET verify + POST receive con HMAC + idempotency + 5 defenses-in-depth.
- `api/_lib/whatsappWebhook.ts` (469 líneas) — helpers de parsing, normalización wa_id, allowlist phoneNumberId, regex wamid, cap defensivo de payloads.
- `scripts/invariantes/check-whatsapp-webhook-hmac.ts` (P-016 nuevo).
- `scripts/invariantes/check-whatsapp-idempotency.ts` (P-017 nuevo).
- `scripts/invariantes/run-all.ts` — registro de P-016 + P-017.
- `docs/PATRONES_REGRESION.md` — entradas P-016 y P-017.

**Defenses-in-depth aplicadas tras security audit:**

1. HMAC SHA-256 con `timingSafeEqual` sobre body raw + `bodyParser:false`.
2. Idempotency via `runTransaction` + `tx.get` pre-set.
3. **`phoneNumberId` allowlist** via env `META_PHONE_NUMBER_IDS_ALLOWLIST` (CSV) o fallback `META_PHONE_NUMBER_ID`. Skipea payloads firmados desde otra cuenta WABA.
4. **wamid regex validation** (`/^wamid\.[A-Za-z0-9+/=_-]{1,200}$/`) antes de usar como doc id.
5. **Status callback hardening**: phoneNumberId match contra outbox + cronología (60s tolerancia skew) + bloquear regresión delivered/read → failed.
6. **Cap defensivo 20KB** para `contenido.interactivePayload` y `contenido.contactsPayload` (helper `caparObjetoSiExcede`).
7. **Logs sin PII** y Admin SDK init error loggea solo `.message` truncado (no `err` raw que podría incluir credenciales).

**Veredictos de validadores:**

| Agente | Veredicto | Hallazgos |
|---|---|---|
| tester (local) | OK | typecheck PASS + 15/15 cazadores PASS |
| regression_guardian | GO | Sin instancias semánticas. Atomicidad cross-collection confirmada |
| reviewer | APPROVED_WITH_NOTES | Polish minor no-bloqueante (log de Admin SDK error) |
| security | RISKS_FOUND | 1 ALTA condicional + 5 MEDIAS — todos aplicados antes del commit |

15/15 cazadores PASS (13 + P-016 + P-017).

---

## Pasada 5 — SPRINT-WA-1-HOTFIX FieldValue import (commit `50931bb`)

**Bug en producción descubierto por agente devops en verificación post-deploy.**

Endpoint `/api/whatsapp/webhook` devolvía HTTP 500 FUNCTION_INVOCATION_FAILED en TODOS los métodos porque crasheaba en module-load.

**Causa raíz:** `import { FieldValue, getAdminFirestore } from '../_lib/firebaseAdmin.js'` intentaba importar `FieldValue` desde un archivo que NO la exporta. firebaseAdmin.ts usa FieldValue internamente (línea 5 + 177) pero nunca la re-exporta.

**Por qué no lo cazó el typecheck pre-commit:** `tsconfig.json` del proyecto incluye únicamente `"src"`. El directorio `api/` se compila aparte por Vercel al deploy time. Resultado: typecheck "verde" pero `api/` desnudo.

**Fix de 2 líneas:**
```diff
- import { FieldValue, getAdminFirestore } from '../_lib/firebaseAdmin.js';
+ import { FieldValue } from 'firebase-admin/firestore';
+ import { getAdminFirestore } from '../_lib/firebaseAdmin.js';
```

**Postmortem generado:** `docs/postmortems/2026-05-19-wa-1-webhook-fieldvalue-import.md`.

**Acción preventiva propuesta (no implementada en este sprint, sprint follow-up):** crear `tsconfig.api.json` con `target: ES2022`, `module: NodeNext`, `include: ["api"]` + agregar al pre-commit hook step `tsc -p tsconfig.api.json --noEmit`. Sin esto, el próximo bug de imports en `api/` se repetirá.

---

## Métricas del día

- **5 commits** pusheados al main.
- **13 → 15 cazadores** activos.
- **1 deploy de `firestore.rules`** a Firebase con 6 rules nuevas.
- **1 postmortem** generado.
- **1 bug nuevo descubierto** estructural (typecheck gap sobre `api/`) — acción preventiva pendiente como sprint follow-up.
- **0 incidentes a usuario** — el bug del webhook ocurrió antes de que Jorge configurara el binding en Meta.

### MTBF clase nueva
- Última clase nueva catalogada: P-015 (2026-05-18).
- Clase nueva candidata HOY: "imports `api/` no tipechequeados" — propuesta P-019 o solución estructural via tsconfig.

### MTTR del hotfix
- Detección → push fix: ~6 minutos (12:40 UTC detección agente devops → 12:46 UTC push hotfix).

---

## Acciones manuales pendientes de Jorge

1. **Configurar webhook URL en Meta Developers:**
   - URL: `https://www.misterservicerd.com/api/whatsapp/webhook`
   - App: `1558940908663280` → WhatsApp → Configuration → Webhooks
   - Pegar `META_VERIFY_TOKEN` en pantalla Meta
   - Suscribir: `messages`, `message_status`, `message_template_status_update`, `account_update`
   - Probar con "Verify and save" → debería retornar 200 con challenge

2. **Configurar `META_PHONE_NUMBER_IDS_ALLOWLIST` en Vercel** (opcional pero recomendado): CSV con los 2 phoneNumberId activos `1151997541323577,1226992440486630`. Si no se setea, fallback al single `META_PHONE_NUMBER_ID` existente.

3. **Correr `npx tsx scripts/init-whatsapp-config.ts`** una vez post-deploy de rules (rules ya están deployadas, podés correrlo ahora). Idempotente.

4. **Mandar mensaje real desde teléfono al +1 849-564-6767** post-binding del webhook → verificar que aparece doc en `whatsapp_mensajes_inbox` y conversación en `whatsapp_conversaciones`.

5. **Decidir sprint follow-up** para typecheck extendido a `api/` (postmortem 2026-05-19 sección "Acciones preventivas"). Si Jorge OK, va a la cola como `SPRINT-API-TYPECHECK-CONFIG`.

6. **Crear plantilla HSM `auto_respuesta_fuera_horario` en Meta (opcional)**: con D3=A queda como fallback de emergencia, no bloqueante. Si Jorge quiere mandarla a approval ahora, puede.

---

## Próximos sprints en la cola

- **SPRINT-WA-2** — Servicio saliente proxy `api/whatsapp/send` (depende de WA-1 ✅).
- **SPRINT-WA-5** — UI plantillas HSM (paralelo a WA-2, depende de WA-RULES ✅).
- **SPRINT-API-TYPECHECK-CONFIG** (nuevo proposed) — extender typecheck a `api/` para prevenir recurrencia del bug FieldValue.

Estimación realista restante del módulo WhatsApp: 35-50 horas (sin WA-1 que ya está).

---

**Última actualización del DIARIO:** 2026-05-19 sesión 2 noche.

## Deploy hotfix `50931bb` — VERIFICADO

- Vercel Ready a las **2026-05-19T12:42:21.590Z** (sin Deploy Hook, webhook GitHub→Vercel normal).
- `version.json` confirma commit `50931bb`.
- Verificación curl:
  - `GET /api/whatsapp/webhook` sin verify_token → **403 forbidden** ✓
  - `GET ?hub.mode=subscribe&hub.verify_token=wrong&hub.challenge=abc` → **403 forbidden** ✓
  - `POST` sin signature → **401 invalid signature** ✓
- El `FUNCTION_INVOCATION_FAILED` 500 quedó resuelto. Las defenses-in-depth responden correctamente.

## E2E real validado por Jorge

- Mensaje real al 6767 → POST 200, mensajes=1, dup=0, doc persistido. ✓
- Test desde 6265 con `META_PHONE_NUMBER_IDS_ALLOWLIST=1151997541323577,1226992440486630` cargado en Vercel → ✓
- Aprendizaje: WhatsApp Cloud API entrega webhooks en Modo Desarrollo de la app (no hace falta Modo Activo).

---

## Sesión 2 — SPRINT-WA-2 envío saliente (cierre con E2E pendiente Jorge)

Sesión continuación post-WA-1 validado. Jorge corrigió decisión D9 (Vercel está en Hobby, no Pro como decía la decisión original).

**6º commit del día (proyectado, hash pendiente):** `feat(wa): SPRINT-WA-2 endpoint api/whatsapp/send con rate limit + audit log + cazador P-018`.

### Archivos producidos

- `api/whatsapp/send.ts` (~1174 líneas) — endpoint POST con auth Firebase + role check (D6=C) + check `activo!=false` + rate limit por uid/rol + window 24h (P-018) + opt-out doble fuente fail-closed (D8=A) + allowlist phoneNumberId + idempotency (P-017 con `doc(tempId)` patrón corregido) + backoff 429 (MAX=3 por Vercel Hobby 10s timeout) + retry post-Meta-OK + audit log helper centralizado en TODOS los rechazos y fallos.
- `src/services/whatsapp.service.ts` (230 líneas) — wrappers cliente `enviarTexto`/`enviarPlantilla`/`enviarMedia` con `crypto.randomUUID()` para tempId (32 chars hex, dentro del regex server).
- `scripts/invariantes/check-whatsapp-window-24h.ts` — cazador P-018 (15→16 cazadores activos).
- `scripts/invariantes/check-whatsapp-idempotency.ts` extendido — reconoce ahora 2 patrones: directo (`addDoc/setDoc(doc(coll, tempId))`) y transaccional (`runTransaction` + `tx.get(ref)` + `tx.set(ref)`).
- `docs/PATRONES_REGRESION.md` — entry P-018.
- `scripts/invariantes/run-all.ts` — registro P-018.

### Decisiones tras audits

| Validador | Hallazgos | Fix aplicado |
|---|---|---|
| regression_guardian | GO | — |
| reviewer | 3 BLOQUEADORES | (1) `doc(tempId)` reemplaza auto-id query — Firestore optimistic locking requiere doc ref específico. (2) Opt-out query simple por telefonoNormalizado + filter memoria (evita índice compuesto). (3) Check `activo !== false` en cascada usuarios/personal. |
| security | 1 ALTA + 3 MEDIAS + 4 BAJAS | (1) Rate limit por uid/rol replicado de `api/ai/chat.ts` (counter atómico en `rate_limits/{uid}_whatsapp_send_{YYYY-MM-DD}`, caps configurables en `config/rate_limits.whatsapp_send`). (2) Helper `escribirAuditoriaSend` centralizado + invocado en TODOS los rechazos y éxito (no solo path success). (3) Shape canónico audit log alineado con `crear-usuario.ts` (`objetivoTipo`, `objetivoWaId` truncado, `solicitanteEmail ?? null`). (4) Retry post-Meta-OK con backoff lineal — si update local falla 3x, retorna 502 `meta-ok-outbox-update-failed` con `wamid` para que UI muestre warning. (5) Opt-out fail-closed (rechaza 500 si lookup explota). (6) JSDoc del endpoint documenta semántica `idempotent: true, wamid: null` (envío en vuelo). |

### Validaciones finales

- `npx tsc --noEmit`: OK.
- `npm run check:regression`: **16/16 PASS** (P-001..P-018 modulo P-008/P-013 saltados).
- No toca `firestore.rules` (deployadas en WA-RULES).
- No toca `firestore.indexes.json` (query opt-out usa solo single-field auto-creado).

### Comando curl E2E para Jorge (test real con plantilla `cita_confirmada` al 8094580618)

⚠️ **Requiere obtener un ID token Firebase Auth válido primero.** Desde la consola del browser logueado al CRM:

```javascript
firebase.auth().currentUser.getIdToken().then(t => console.log(t))
```

(O usar el SDK modular: `getAuth().currentUser.getIdToken().then(t => console.log(t))`)

Copiar el token. Después en la terminal:

```bash
TOKEN="<pegar_id_token_aca>"

# Generar un tempId único (32 chars hex)
TEMP_ID=$(node -e "console.log(require('crypto').randomUUID().replace(/-/g,''))")
echo "tempId: $TEMP_ID"

# Test: enviar plantilla cita_confirmada al 8094580618
# Variables de cita_confirmada (asumidas — confirmar con sync-plantillas cuando WA-5 esté):
#   {{1}} = cliente.nombre
#   {{2}} = fecha cita formateada
#   {{3}} = orden.numero
#   {{4}} = tecnico.nombre
#   {{5}} = link tracking (opcional)
curl -sw '\nHTTP %{http_code}\n' -X POST \
  https://www.misterservicerd.com/api/whatsapp/send \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{
    \"wa_id\": \"8094580618\",
    \"tipo\": \"plantilla\",
    \"plantilla\": {
      \"nombre\": \"cita_confirmada\",
      \"idioma\": \"es\",
      \"variables\": [\"Jorge\", \"jueves 22/05/2026 9:30am\", \"OS-9999\", \"Aury\", \"\"]
    },
    \"tempId\": \"$TEMP_ID\"
  }"
```

**Resultados esperados:**
- HTTP 200 + body con `{ ok: true, outboxId, wamid, estado: 'sent', intentos: 1 }` → mensaje llega al teléfono 8094580618.
- Doc nuevo en Firestore `whatsapp_mensajes_outbox/{tempId}` con `estado='sent'`, `wamid` poblado.
- Doc en `whatsapp_conversaciones/8094580618` con `ultimoMensajeSaliente.preview = 'cita_confirmada'`.
- Audit log en `auditoria_admin` con `accion: 'enviar_whatsapp', resultado: 'ok'`.

**Tests negativos sugeridos** (opcional):

```bash
# Sin token → 401
curl -sw '\nHTTP %{http_code}\n' -X POST https://www.misterservicerd.com/api/whatsapp/send -H "Content-Type: application/json" -d '{}'

# Token de técnico/ayudante → 403 rol-no-autorizado

# Reenviar mismo tempId → 200 idempotent: true
curl -sw '\nHTTP %{http_code}\n' -X POST https://www.misterservicerd.com/api/whatsapp/send \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d "{ \"wa_id\": \"8094580618\", \"tipo\": \"plantilla\", \"plantilla\": {...}, \"tempId\": \"$TEMP_ID\" }"
```

### Sprints follow-up nuevos en BLOQUEOS

1. **SPRINT-VERCEL-PLAN-DECISION** (MEDIA) — Hobby vs Pro. Bloquea WA-7. Recomendación: Pro ($20/mes) por timeout 60s + 3 crons.
2. **SPRINT-WA-2-FOLLOWUP-RATE-LIMITS-CONFIG** (BAJA) — Personalizar caps en `config/rate_limits.whatsapp_send`. Opcional, defaults aplican.

### Próximos sprints

- WA-5 (UI plantillas + cron sync — paralelizable con WA-3).
- WA-3 (UI conversaciones admin — depende de WA-1 + WA-2 ✓).
- WA-4 (tracking referral — depende de WA-1 ✓).
- WA-7 (crons recordatorios — depende de WA-2 ✓ + WA-5 + Vercel plan decision).
- WA-6 (bot IA Claude Haiku — depende de WA-1+WA-2+WA-3).

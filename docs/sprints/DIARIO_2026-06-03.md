# DIARIO 2026-06-03 — pasada 58 (`procesa bloqueos` + autónomo)

## Resumen en 60 segundos

Jorge dio OK explícito en BLOQUEOS.md para SPRINT-DISENO-I Fase 3:
> "Dejá todos los números operativos del día como están, y mové a un
> reporte aparte (sin borrar nada, que queden a un clic) estos 4:
> 'Reparaciones por Tipo de Equipo', 'Órdenes anuladas esta semana',
> 'Nómina proyectada del mes' y 'Rendimiento por Técnico'."

El coordinator procesó autónomo en una sola pasada. **1 sprint
commiteado + pusheado, awaiting QA Jorge.** Hash en producción:
`5ca35d2`.

## Sprints completados en la pasada

| Sprint | Hash | Estado | Notas |
|---|---|---|---|
| `SPRINT-DISENO-I-DATA-SLOP-DASHBOARD-AUDIT` Fase 3 | `5ca35d2` | 🟡 EN PRODUCCIÓN AWAITING QA | 4 widgets analíticos movidos a `/admin/reporte-avanzado` sin borrar |

## Detalle SPRINT-DISENO-I Fase 3

**Origen:** `OK: jorge 2026-06-03` literal en BLOQUEOS.md para Fase 2 del
sprint. Fase 1 (inventario `INVENTARIO_KPI_DASHBOARD_2026-06-01.md`) ya
había salido en pasada 57 hash `1177ec2`.

**Touch-list ejecutado:**

1. `src/pages/ReporteAvanzado.tsx` (NUEVO, 411 líneas) — página dedicada
   con 4 widgets idénticos a los del Dashboard pre-sprint. Listeners
   propios: `ordenes_servicio`, `facturas`, `personal`, `comisiones`.
   Los 4 `useMemo` (`rendimientoTecnicos`, `reparacionesPorTipo`,
   `anuladasSemana`, `proyeccionNomina`) copiados 1:1 con sus deps.
   Header con back-link al Dashboard. Empty states explícitos.
2. `src/App.tsx` — `const ReporteAvanzado = lazy(() => ...)` agregado
   junto a `MetricasMensuales`. Ruta `/admin/reporte-avanzado` con
   `RolRoute admin+coord` (espejo del sidebar).
3. `src/components/Sidebar.tsx` — item "Reporte avanzado" en sección
   Finanzas debajo de "Métricas del Mes" con `show: esAdminOCoord` y
   icono `BarChart3` (importado).
4. `src/pages/Dashboard.tsx` — quitados los 4 widgets JSX + los 4
   `useMemo` + permiso `puedeVerAnuladas` (`puedeVerNomina` se queda,
   "Próxima nómina" lo usa). CTA discreto agregado: `<Link>` "Ver
   reporte avanzado (rendimiento por técnico, anulaciones, nómina del
   mes, reparaciones por tipo) →" al final del bloque "Equipo y
   trabajos" (gate admin+coord espejo). Imports limpiados: `Wrench`,
   `XCircle`, `TrendingUp` removidos (unused tras la mudanza).
   `Wallet` y `getTecnicoColor` se quedan.

**Consumidores cross-archivo verificados:** 0 referencias externas a los
4 memos en `src/` — movimiento mecánico seguro.

**Restricciones cumplidas:**
- NO se borró ningún widget.
- Permisos preservados 1:1 (`puedeVerAnuladas`, `puedeVerNomina`).
- Lógica idéntica (mismas deps en cada `useMemo`).
- Cazadores 25/25 PASS, typecheck PASS, lint clean.
- "QA flujo Dashboard validado: solo se quitaron 4 widgets + sus memos"
  declarado en el commit message (página crítica per CLAUDE.md sub-regla).

**Verificaciones técnicas:**
- `npx tsc --noEmit` → PASS sin output.
- `npx tsx scripts/invariantes/run-all.ts` → 25/25 cazadores PASS.
- `npx eslint --max-warnings 0 <4 archivos>` → clean.
- `npm run build` → 4.55s, `ReporteAvanzado-D_ZB1O-M.js` chunk lazy
  creado, `Dashboard-DvK0OtNi.js` bajó a 49.16 kB (gzip 13.36 kB).
- Pre-commit hook PASS sin bypass.
- `git push origin main` → `e1fb7b4..5ca35d2`.
- Vercel deploy hook backup → job `xKdKcyToJ8s8Zue4MxGb` PENDING (el
  webhook GitHub→Vercel también se dispara automático con el push).

**Decisión técnica menor:** "Anuladas semana" en Dashboard pre-sprint
ocultaba la card cuando `anuladasSemana.total === 0` (sin ruido). En la
página nueva agregué empty state explícito ("Sin anulaciones esta
semana") porque ahora es página dedicada — el usuario llega buscando ese
dato. Mejora UX lateral intencional. NO afecta el comportamiento del
Dashboard.

## Para Jorge

**1 acción nueva — QA visual (~3 min):**

1. Hard-refresh `/admin/dashboard`. Verificá que:
   - Los 4 widgets analíticos YA NO aparecen (Rendimiento por Técnico,
     Reparaciones por Tipo, Anuladas semana, Nómina proyectada del mes).
   - El bloque "Equipo y trabajos" todavía muestra: Estado de Casos por
     Técnico, Alertas de Inventario, Próxima nómina.
   - Al final del bloque hay un link discreto: "Ver reporte avanzado
     (rendimiento por técnico, anulaciones, nómina del mes, reparaciones
     por tipo) →"
2. Click en ese link → abre `/admin/reporte-avanzado`. Verificá que:
   - Renderiza los 4 widgets con datos reales (mismos cálculos que
     antes).
   - El back-link "Volver al dashboard" funciona.
3. Sidebar: en la sección "Finanzas" debajo de "Métricas del Mes" hay
   una nueva entrada "Reporte avanzado" con icono de barras — debe
   abrir la misma página.
4. Si OK → agregá `QA: jorge 2026-06-03 HH:MM DISENO-I PASS` en
   `BLOQUEOS.md`. El coordinator marca el sprint COMPLETADO en la
   próxima pasada.

**Resto pendiente sigue igual** — DISENO-E + H awaiting QA visual (de
pasadas anteriores), DISENO-C + D awaiting QA, A.x + B awaiting QA en
celular real. Detalle en `MEMORIA_MAESTRA.md`.

## Tiempo

Pasada 58 cerrada en ~10 min: lectura del estado vivo + auditoría touch-
list + creación de página + edits cruzados + verificación (typecheck +
cazadores + lint + build) + commit + push + docs sync.


# Diario autónomo 2026-05-09

> Resumen 60s de lo que el coordinator procesó hoy. Más reciente arriba.

---

## Pasada 5 — Cierre 117c4 + 117c6 deployado, lote 117c COMPLETO al 100%

**Trigger:** `trabaja` (Jorge — confirmó 117c4 con "si" + arrancar 117c6).

**Procesado:**

- **SPRINT-117c4** cerrado como COMPLETADO. Hash deployado `480532f`. Movido a histórico con OK humano "jorge 2026-05-09 ('si' implícito al disparar pasada de 117c6)".
- **SPRINT-117c6** (limpieza alias `isAdmin = esAdminOCoord`): EN_REVISION_HUMANA. Hash (commit en este turno).
  - Riesgo medio — toca semántica de permisos. Auditoría caso por caso de las 16 usages funcionales del alias en `Sidebar.tsx`.
  - Resultado: alias eliminado. Las 16 usages migradas a `esAdminOCoord` directo (semántica idéntica). En 4 sitios (Conduces Pendientes, Historial Anuladas, Nómina, Estado de Resultado) la cláusula redundante `|| 'coordinadora'` se eliminó por idempotencia lógica (`A∨B∨B = A∨B` — conjunto igual).
  - **Cero cambios de comportamiento**: cada ítem evalúa exactamente el mismo conjunto de roles antes y después. Asistente IA + Plantillas Marketing (admin-literal) NO modificados (siempre usaron `userProfile?.rol === 'administrador'` directo, no el alias).
  - Touch-list: solo `src/components/Sidebar.tsx` (~30 líneas modificadas, 1 archivo).
  - Checks: typecheck PASS, cazadores 7/7 PASS 0 hits, lint Sidebar.tsx limpio, build OK 4.11s, bundle 2,651.94 kB (idéntico al baseline 117c4).
  - Plan de rollback: `git revert <hash>`. El alias vuelve, los 4 sitios donde se eliminó `|| 'coordinadora'` redundante recuperan la cláusula. 100% seguro.

**Lote 117c — CERRADO al 100%:** 5/6 sub-sprints aprobados deployados (117c1 `759a76b`, 117c2 `9f71883`, 117c3 `9c262c9`, 117c4 `480532f`, 117c6 commit en este turno). El 6º (117c5) fue rechazado por Jorge en el OK selectivo del 2026-05-09.

**Pendientes:** ninguno del lote 117c. Próximo: SPRINT-119+ cuando Cowork escriba a la cola.

**Bloqueos abiertos:** ninguno introducido en esta pasada.

**Tiempo estimado:** ~12 minutos (riesgo medio justificó análisis caso por caso adicional).

**Reportado a Jorge:** sí, al final de la pasada con instrucción de QA visual exacta para los 5 roles (admin / coord / operaria / secretaria / técnico) — foco en confirmar que ningún ítem cambió de visibilidad.

---

## Pasada 4 — Cierre 117c3 + 117c4 deployado, esperando QA humano

**Trigger:** `trabaja` (Jorge — OK implícito del cierre de 117c3 + arrancar 117c4).

**Procesado:**

- **SPRINT-117c3** cerrado como COMPLETADO. Hash deployado `9c262c9`. Movido a histórico con OK humano "jorge 2026-05-09 (`trabaja` implícito)".
- **SPRINT-117c4** (sección "Equipo" + Mantenimiento → Operaciones): EN_REVISION_HUMANA. Hash `480532f`.
  - Tres cambios estructurales en `Sidebar.tsx`:
    1. Nueva sección **"Equipo"** (id `equipo`, icon `UserCog`, defaultExpanded `false`) con: Personal, Usuarios y Permisos, Reporte de Ponches.
    2. Sección **"Sistema"** queda con solo: Configuración, Plantillas Marketing.
    3. **Mantenimiento** mudado del top-level (entre Finanzas y Web y Solicitudes) al final del array de items de Operaciones.
  - Touch-list: solo `src/components/Sidebar.tsx` (1 archivo, ~33 líneas insertadas / 16 eliminadas).
  - Gates `show:` preservados al 100% en los 4 ítems movidos. Sin renombrados, sin cambios de rutas, sin tocar lógica/listeners/queries (los 7 listeners de `onSnapshot` intactos). La sección "Sistema" mantiene su id `sistema` (preserva el estado de localStorage de usuarios que la tenían colapsada/expandida).
  - Checks: typecheck PASS, cazadores 7/7 PASS 0 hits, lint Sidebar.tsx limpio, build OK 4.82s, bundle 2,652 kB (idéntico al baseline 117c3).
  - Plan de rollback: `git revert 480532f`. Personal/Usuarios/Ponches vuelven a "Sistema", Mantenimiento vuelve a `kind: 'item'` top-level entre Finanzas y Web y Solicitudes.

**Pendientes del lote 117c (esperan QA visual sucesivo):**
- 117c5 — RECHAZADO por Jorge.
- 117c6 — Limpiar alias `isAdmin = esAdminOCoord` en Sidebar.tsx (riesgo medio — pre-condición: c1+c2+c3+c4 deployados y validados — c4 acaba de deployar).

**Bloqueos abiertos:** ninguno introducido en esta pasada.

**Tiempo estimado:** ~9 minutos.

**Reportado a Jorge:** sí, al final de la pasada con instrucción de QA visual exacta y comando para arrancar 117c6.

---

## Pasada 3 — Cierre 117c2 + 117c3 deployado, esperando QA humano

**Trigger:** `trabaja` (Jorge — OK implícito del cierre de 117c2 + arrancar 117c3).

**Procesado:**

- **SPRINT-117c2** cerrado como COMPLETADO. Hash deployado `9f71883`. Movido a histórico con OK humano "jorge 2026-05-09 (`trabaja` implícito)".
- **SPRINT-117c3** (sección "Cobranza y facturación"): EN_REVISION_HUMANA.
  - Cambio: la sección "Documentos" se renombra in-place a "Cobranza y facturación" (id `documentos` → `cobranza_facturacion`, icon `FileText` → `Receipt`, defaultExpanded `true` preservado). Los 3 ítems del pipeline factura se reordenan al orden de pasos consecutivos: **Cotizaciones → Conduces Pendientes (badge) → Conduces de Garantía**. Antes el orden era Cotizaciones / Conduces de Garantía / Conduces Pendientes.
  - Como los 3 ítems eran toda la sección Documentos, el renombrado in-place absorbe la sección original sin huérfanos (cumple criterio "Documentos eliminada si queda vacía").
  - Touch-list: solo `src/components/Sidebar.tsx` (1 archivo, ~14 líneas).
  - Gates de permisos preservados al 100%: `p('cotizacionesVer')`, `isAdmin || rol==='coordinadora'`, `p('facturasVer')`. Listener `facturacionPendienteCount` (líneas 91-98) intacto. Rutas en App.tsx intactas.
  - Checks: typecheck PASS, cazadores 7/7 PASS 0 hits, lint Sidebar.tsx limpio, build OK 4.14s.
  - Plan de rollback: `git revert <hash>`. La sección vuelve a llamarse "Documentos" con id `documentos`, icon `FileText` y orden previo.

**Pendientes del lote 117c (esperan QA visual sucesivo):**
- 117c4 — Crear sección "Equipo" + mover Mantenimientos.
- 117c5 — RECHAZADO por Jorge.
- 117c6 — Limpiar alias `isAdmin = esAdminOCoord` en Sidebar.tsx.

**Bloqueos abiertos:** ninguno introducido en esta pasada.

**Tiempo estimado:** ~9 minutos.

**Reportado a Jorge:** sí, al final de la pasada con instrucción de QA visual exacta y comando para arrancar 117c4.

---

## Pasada 2 — Cierre 117c1 + 117c2 deployado, esperando QA humano

**Trigger:** `trabaja` (Jorge — OK implícito del cierre de 117c1 + arrancar 117c2).

**Procesado:**

- **SPRINT-117c1** cerrado como COMPLETADO. Hash deployado `759a76b`. Movido a sección histórica con OK humano "jorge 2026-05-09 (`trabaja` implícito)".
- **SPRINT-117c2** (sección "Bandeja de entrada"): EN_REVISION_HUMANA.
  - Cambio: nueva sección colapsable `Bandeja de entrada` (icon `Inbox`, defaultExpanded `true`) ANTES de "Operaciones" en `Sidebar.tsx`. Agrupa 3 inboxes: Citas por Confirmar (badge), Reprogramaciones (badge), Sugerencias chequeo (badge).
  - Antes: los 3 ítems estaban dispersos dentro de "Operaciones" entre 9 ítems totales.
  - Después: "Operaciones" baja de 12 a 9 ítems; "Bandeja de entrada" arriba con 3 ítems gateados como antes (Citas con `p('ordenesVer')`, Reprogramaciones y Sugerencias con `esAdminOCoord`).
  - Touch-list: solo `src/components/Sidebar.tsx` (1 archivo).
  - Checks: typecheck PASS, cazadores 7/7 PASS, lint Sidebar.tsx limpio, build OK.
  - Plan de rollback: `git revert <hash>`. Los 3 ítems vuelven a "Operaciones".

**Pendientes del lote 117c (esperan QA visual sucesivo):**
- 117c3 — Crear sección "Cobranza y facturación".
- 117c4 — Crear sección "Equipo" + mover Mantenimientos.
- 117c5 — RECHAZADO por Jorge.
- 117c6 — Limpiar alias `isAdmin = esAdminOCoord` en Sidebar.tsx.

**Bloqueos abiertos:** ninguno introducido en esta pasada.

**Tiempo estimado:** ~10 minutos.

**Reportado a Jorge:** sí, al final de la pasada con instrucción de QA visual y comando para arrancar 117c3.

---

## Pasada 1 — SPRINT-117c1 deployado, esperando QA humano

**Trigger:** `trabaja` (Jorge).

**Procesado:** 1 sprint del lote 117c (5 aprobados + 1 rechazado).

- **SPRINT-117c1** (renombrar etiquetas sidebar + redirect bookmark viejo): EN_REVISION_HUMANA.
  - 4 cambios concretos:
    1. `Calendarios` → `Calendarios públicos (Calendly)`.
    2. `Rendimiento` → `Mi rendimiento` solo para operaria/secretaria.
    3. Ítem `Catálogo` ocultado del sidebar admin (ruta sigue activa por URL).
    4. `/admin/configuracion/usuarios` ahora redirige a `/admin/usuarios`.
  - Touch-list: `src/components/Sidebar.tsx`, `src/App.tsx`.
  - Checks: typecheck PASS, cazadores 7/7 PASS, lint baseline preservado (sin warnings nuevos).
  - Plan de rollback: `git revert <hash>`. Solo strings + 1 redirect cliente-side.

**Pendientes del lote 117c (esperan QA visual sucesivo):**
- 117c2 — Crear sección "Bandeja de entrada".
- 117c3 — Crear sección "Cobranza y facturación".
- 117c4 — Crear sección "Equipo" + mover Mantenimientos.
- 117c5 — RECHAZADO por Jorge.
- 117c6 — Sub-sprint final (ver `COLA_AUTONOMA.md`).

**Bloqueos abiertos:** ninguno introducido en esta pasada.

**Tiempo estimado:** ~12 minutos (lectura + edición + checks + commit + push + docs).

**Reportado a Jorge:** sí, al final de la pasada con instrucción de QA visual y comando para arrancar 117c2.


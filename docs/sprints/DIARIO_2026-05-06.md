# Diario — 2026-05-06

## Resumen
- **3 sprints completados** en ~55 minutos
- **1 sprint pendiente** (SPRINT-100, requiere validación visual de Yohana — no procesable autónomamente)
- **0 sprints bloqueados** esperando OK de Jorge
- **1 commit pusheado** a producción (`1568a63`)
- Acción humana requerida: deployar `firestore.rules` (ver Próximos pasos)

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-101 | Smoke test cazadores | `1568a63` | docs | El smoke ya lo había hecho Cowork; doc-only marca COMPLETADO |
| SPRINT-102 | Sub-regla "bug → cazador" | `1568a63` | 2 .md agentes | Builder.md y coordinator.md ahora tienen guía explícita |
| SPRINT-103 | Triaje 35 hits baseline | `1568a63` | 13 archivos | 6 bugs reales fixeados + 16 falsos positivos allowlistados + rules con `.get()`/`@safe-required` |

## Sprints pendientes / no procesados

| ID | Por qué | Cómo desbloquear |
|---|---|---|
| SPRINT-100 | Validar visualmente que Yohana ve sus notificaciones tras el deploy de `b93625d` | Pedirle a Yohana hard refresh + abrir campanita. Si no ve nada → escalar a sprint nuevo de re-migración (>500 docs, requiere OK explícito) |

## Cazadores anti-regresión

- **P-001 userProfile.id misuse:** 22 hits → **0 hits** (6 bugs reales fixeados, 16 falsos positivos allowlistados)
- **P-002 rules immutability:** 13 hits → **0 hits** (campos opcionales con `.get()`, required con `@safe-required`)
- **P-003 cross-collection sin tx:** 0 hits → 0 hits (sin cambio)

## Alertas

- **DEPLOY DE RULES PENDIENTE:** los cambios a `firestore.rules` (P-002) están en el repo pero NO en producción. Sin ejecutar `npm run deploy:rules`, las rules live siguen con el patrón directo y los `permission-denied` que cazaron Reactivación + Notificaciones podrían reproducirse en otras pantallas.
  - Comando: `npm run deploy:rules` (Jorge desde su terminal).
  - Fallback: `npx firebase login` la primera vez si pide auth.

- **Bugs reales P-001 cerrados (mismo vector que `afc5e4a` Reactivación + `b93625d` Notificaciones):**
  1. Sugerencias "solo chequeo" desde técnico → bloqueaba al técnico.
  2. Reprogramaciones (aprobar/rechazar/contraproponer) desde oficina → bloqueaba a operarias.
  3. Sugerencias "solo chequeo" (aprobar/rechazar) desde oficina → bloqueaba a operarias.

## Próximos pasos sugeridos (Cowork puede agregarlos a la cola)

1. **SPRINT — Hotfix `notificaciones`/destinatarioId vs userId** (gotcha en CLAUDE.md, sigue activo): renombrar campo a `userId` o ajustar rule. Bloqueante para que técnicos/operarias marquen notifs como leídas. Va a ser un ejemplo perfecto de "cerrar bug + agregar P-XXX + cazador" si rompió producción.
2. **SPRINT — Asegurar que `GestionUsuarios.tsx` cree el doc en `usuarios/{auth.uid}`** además de `personal/{auto-id}` al alta. El backfill ya migró los existentes (commit `1353b84`) pero el form de alta nuevo aún no se actualizó. Sin esto, cualquier técnico/operaria nueva reintroducirá el problema P-001.
3. **SPRINT — Auditar `cierreServicio.tecnicoId` y `inicioChequeo.tecnicoId`** (descriptors nested) para decidir si convertirlos a `auth.uid` por consistencia, o documentar formalmente que son descriptors. Hoy son falsos positivos del cazador pero hay ambigüedad semántica.
4. **SPRINT — Limpieza de imports unused** en archivos no tocados (`Mantenimiento.tsx`, `Citas.tsx`, etc.). Hay ~140 warnings ESLint pre-existentes en el repo. Bloquea pre-commit hooks futuros si Jorge toca esos archivos.

## Métricas (para revisar mensualmente)

- Total commits del día: 1 (`1568a63`)
- Cazadores agregados/refinados: 1 (P-001 ganó allowlist por línea con tag `@safe-userprofile-id`)
- Bugs P-XXX nuevos catalogados: 0 (P-001 ya existía; los 6 fixes son instancias del patrón ya catalogado, no patrón nuevo)
- Tiempo de hook pre-commit: <2s (typecheck + cazadores + lint staged)

---

## Segunda pasada — `trabaja` autónomo (1 sprint completado)

**Hora aproximada:** ~55 min después de la primera pasada.

### Resumen
- **1 sprint completado** en ~25 minutos
- **0 sprints bloqueados** (BLOQUEOS.md sigue vacío)
- **1 commit pusheado** (`b90693c`)
- **SPRINT-100 sigue pendiente** (validación visual de Yohana)

### Sprint completado (segunda pasada)

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-104 | Recordatorios admin clickeables | `b90693c` | 7 archivos (1 nuevo) | Modal con 3 botones, runTransaction recordatorio + auditoria_admin, sin tocar firestore.rules |

### Detalles SPRINT-104

- **Touch-list real:** `src/services/recordatorios.service.ts` (+2 funciones), `src/components/recordatorios/ModalAccionRecordatorio.tsx` (NUEVO), `src/components/recordatorios/RecordatorioBanner.tsx` (fila clickeable solo admin/coord), `src/utils/whatsapp.ts` (+2 templates), `src/types/index.ts` (`RecordatorioDiario.completadoPor`).
- **Acciones del modal:**
  1. **Recordar** → notif in-app a `operariaUid` con `userId == auth.uid` (cumple rule de notificaciones) + abre `wa.me/...` con mensaje pre-armado.
  2. **Marcar completado por admin** → pide motivo (5-80 chars), escribe `recordatorios_diarios.completadoPor` + entry en `auditoria_admin` atómicamente vía `runTransaction`. Idempotente.
  3. **Cancelar** → cierra modal sin acción.
- **Gate de roles:** solo admin/coordinadora ven la fila clickeable. Operaria/secretaria/técnico → no clickeable.
- **P-001 cumplido:** `actorUid` viene de `currentUser.uid`, no de `userProfile.id`.
- **P-003 cumplido:** mutación cross-collection (recordatorio + auditoria_admin) en un solo `runTransaction`.
- **Sin cambios a firestore.rules:** las rules existentes (`recordatorios_diarios` write a `esStaffOficina`, `auditoria_admin` create a `isAuth`) ya cubren la operación. NO hubo bloqueo.
- **Cazadores:** 0/0/0 hits antes y después.
- **Pre-commit hook:** PASS.
- **Deploy:** push a `origin/main` OK + hook de Vercel disparado como backup (job `V29Q8XU82ya3y9PrHy9i` PENDING).

### Cambio acumulado del día

- 2 commits (`1568a63` + `b90693c`).
- Cazadores siguen en 0 hits — sin regresiones.
- `firestore.rules` sigue con cambios sin deployar (baseline P-002 del sprint 103). **Acción humana sin cambio: `npm run deploy:rules` desde el terminal de Jorge.**

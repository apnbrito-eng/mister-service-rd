# Diario — 2026-05-30

> Coordinator autónomo, pasada 52. 2 sprints procesados, ambos en producción awaiting QA Jorge.

## Resumen

- **2 sprints commiteados + pusheados** (NO marcados COMPLETADO — ambos llevan `[NO CERRAR sin QA Jorge]`).
- **0 sprints escalados a BLOQUEOS.**
- **2 commits** a producción: `a02a047`, `4c21dc9`.
- **Cazadores 25/25 PASS** en cada commit.
- Cola limpia tras estos 2 — sin más sprints PENDIENTES procesables.

## Sprints procesados

| ID | Título | Hash | Archivos | Estado |
|---|---|---|---|---|
| SPRINT-WIZARD-FASES-FREEZE | window.confirm → Modal propio en FaseStepper | `a02a047` | `src/components/ordenes/FaseStepper.tsx` (+53/-4) | awaiting QA Jorge |
| SPRINT-DISENO-TECNICO-FASE-1 | Jerarquía visual /tecnico (saludo header, ganancias abajo, PRÓXIMA CITA, tabs al final) | `4c21dc9` | `src/pages/TecnicoVista.tsx` (+222/-184) | awaiting QA Jorge |

## Diagnóstico clave del día (SPRINT-WIZARD-FASES-FREEZE)

Cowork reportó el 2026-05-30: clic "En Cotización" desde el wizard del modal de OS-0057 congela el navegador (`document_idle waited 45000ms`, reproducible 2/2). Bloquea QA visual de DINERO-2.

**Diagnóstico del coordinator:** no es un bug del producto para usuarios reales. Cowork hizo el QA con Playwright MCP (`.playwright-mcp/` untracked en el árbol). El handler `handleClickFase` en `FaseStepper.tsx:168` hacía `window.confirm()` antes de avanzar la fase. **Playwright NO maneja dialogs nativos por default** — sin `page.on('dialog', dialog => dialog.accept())` instalado, el confirm queda esperando indefinidamente → `document_idle` nunca llega.

**Fix aplicado:** reemplazar `window.confirm` por Modal propio (mismo componente `Modal` que ya usaba el flujo de retroceso). Beneficio doble: (a) elimina el cuelgue en Playwright permitiendo QA automatizado E2E, (b) mejora UX para usuarios reales (modal consistente con el resto del sistema).

**Cero regresión:** los 4 consumidores de `FaseStepper` (OrdenDetailModal, OrdenDetalle, OrdenCard, TecnicoVista) NO pasan `onCambioFase` — todos usan el `ejecutarCambio` interno. TecnicoVista lo monta `readonly={true}` → handler no se dispara. Comportamiento idéntico.

**Deuda follow-up identificada (NO scope):** `FaseStepper.tsx:71-77` reconstruye `historialFases` con `h.timestamp instanceof Date ? h.timestamp : new Date()`. Si `h.timestamp` viene como Firestore Timestamp (caso normal post-read), `instanceof Date` es false → reemplaza con `new Date()` (hoy), perdiendo el timestamp histórico real. Bug latente, no causa freeze, sprint propio con QA del historial de fases.

## Cazadores anti-regresión

25/25 PASS en cada commit:
- P-001..P-007 sin hits
- P-009..P-013 sin hits  
- P-014..P-025 sin hits (P-025 es el nuevo de `b065e4a` 2026-05-29 — `mantenimiento/ordenes_servicio` con `clienteId: ''`)

## Alertas

Ninguna. 0 hotfixes, 0 sprints fallidos, 0 deploys rotos. Build clean en ambos commits.

## QA pendiente de Jorge (orden sugerido)

1. **`SPRINT-WIZARD-FASES-FREEZE` (5 min):** abrir cualquier orden en "En Diagnóstico" desde `/admin/ordenes`, click "En Cotización" en el wizard de fases → debe abrir modal de confirmación en <1s sin congelar. Click "Confirmar" → fase avanza. Probar también un retroceso (Aprobado → En Cotización) → debe seguir funcionando el modal de retroceso con motivo. Si OK → agregar `QA: jorge YYYY-MM-DD HH:MM WIZARD-FASES PASS` en COLA o BLOQUEOS.
2. **Retomar QA visual de DINERO-2** una vez el wizard funcione: OS-0057 → En Cotización → declarar precio RD$1500 → registrar pago parcial RD$500 → emitir conduce cobrando saldo RD$1000 → verificar orden pasa a "Pagado" en agenda/listado.
3. **`SPRINT-DISENO-TECNICO-FASE-1` (5 min, sección 6 del spec):** abrir `/tecnico` desde celular real (iPhone SE 375×667 es el target). Checklist:
   - Header en una línea con nombre y "N citas hoy"
   - PRÓXIMA CITA visible sin scroll
   - Card ganancias ABAJO del listado, expandible con "Ver detalle"
   - Tabs Hoy/Semana/Mes/Rango al final del listado
   - Mapa NO visible al abrir, botón "Ver Ruta del Día" lo expande
   - Probar 3 botones — Stand-by, Nota, Avisar oficina — siguen funcionando idénticos
   - Si OK → agregar `QA: jorge YYYY-MM-DD HH:MM TECNICO-FASE-1 PASS` para cerrar COMPLETADO y autorizar Fase 2.

## Próximos pasos sugeridos

- Jorge corre los 2 QAs arriba.
- Si WIZARD-FASES OK → Cowork retoma QA visual DINERO-2 con `page.on('dialog')` o por código del modal propio.
- Si TECNICO-FASE-1 OK → autorizar Fase 2 (accesibilidad táctil: botones 48px, mínimo 12px tipografía, focus rings).
- Cola está vacía — Cowork debe agregar próximos sprints según prioridades de Jorge.

## Métricas

- Total commits del día: 2 (`a02a047`, `4c21dc9`)
- Hotfixes follow-up disparados por bugs introducidos hoy: 0
- Deuda nueva documentada: 1 (timestamps en `historialFases.map` del FaseStepper — sprint follow-up)

---

## Pasada 53 — 2026-05-30 (segundo `trabaja` del día)

**Resumen:** NO-OP en código, 1 sprint ESCALADO a BLOQUEOS por discrepancia técnica en la spec.

### Sprints procesados

| ID | Título | Estado |
|---|---|---|
| SPRINT-DISENO-CIERRE-COMPLETO | Cierra los 12 items del roadmap del diseñador (4 fases A/B/C/D) | ⊘ ESCALADO BLOQUEOS |

### Por qué se escaló

Sub-regla CLAUDE.md "Touch-list expandido — actualizar el sprint si la auditoría revela inconsistencias".

La FASE A del sprint afirma:
> "Tailwind ya define `brand-800: #0f3460` en `tailwind.config.js`, semánticamente idéntico" → reemplazar `#0f3460` → `brand-800`.

**Verificación con `cat tailwind.config.js`:**
- `primary.DEFAULT = #0f3460` ← el azul actual hardcoded en 101 archivos.
- `brand-800 = #283B5A` ← azul desaturado distinto (sale de los overoles del logo).

`#0f3460 ≠ brand-800`. La spec contiene un error técnico load-bearing. El find&replace literal cambiaría visualmente el azul de toda la marca, lo cual es incompatible con el propio criterio de éxito del sprint: "visualmente NADA cambia".

### Impacto del cambio si se aplicaba literal

- 101 archivos cambiarían su azul actual (#0f3460 saturado) a `brand-800` (#283B5A desaturado/grisáceo).
- Cubre header, Sidebar, TecnicoVista, Dashboard, HomePage, Layout, Login + 50+ modales.
- Rebranding visual real, no refactor cosmético.

### 3 opciones en `BLOQUEOS.md`

- **A** — Migrar a paleta `brand-*` en 2 pasos (primero `#0f3460` → `primary`, después `primary` → `brand-800` con QA).
- **B** — Solo unificar a `primary` (alias semánticamente idéntico, cambio visual 0). **Recomendación del coordinator.**
- **C** — Saltear FASE A, procesar B/C/D.

Para desbloquear: `OK: jorge YYYY-MM-DD HH:MM opcion=A|B|C` en `BLOQUEOS.md`.

### Métricas pasada 53

- Total commits del día (acumulado): 2 (pasada 52). Pasada 53 sin commits de código.
- Sprints procesados pasada 53: 0 COMPLETADOS, 1 ESCALADO, 0 hotfixes.
- Cazadores: 25/25 PASS de baseline (no se corrieron — sin código tocado).
- Tiempo total pasada 53: ~10 min de análisis + escritura de docs.

### Deudas y bloqueos al cierre del día

**Awaiting QA Jorge (en producción):**
- WIZARD-FASES-FREEZE `a02a047`
- DISENO-TECNICO-FASE-1 `4c21dc9`
- DINERO-2 `b4fc23c`
- REPORTING-1 `a4e64db`
- GARANTIA Fase A `59c5fb0`
- WA-FIX-PLANTILLAS `0ab73c5`
- AGENDA-1 `132d9b5`

**Awaiting decisión de Jorge:**
- DISENO-CIERRE-COMPLETO — opción A/B/C en `BLOQUEOS.md`.
- NUCLEO-CREAR-ORDEN-CENTRAL — opción A/B/C en `BLOQUEOS.md` (heredado).

---

# Pasada 54 (coordinator `procesa bloqueos` + camino 2)

> Coordinator autónomo. 1 sprint en producción awaiting QA, 11 commits, 90 archivos tocados.

## Resumen

- **1 sprint procesado** `SPRINT-DISENO-CIERRE-COMPLETO` (camino 2: solo FASE A.1) tras `OK: jorge 2026-05-30 opcion=B`.
- **11 commits a producción**: 1 chore + 10 chunks de A.1.
- **90 archivos tocados, 857 líneas reemplazadas simétricamente** (insertions == deletions; find&replace mecánico).
- **Cazadores 25/25 PASS** en cada commit. **Build 4.21s** PASS al cierre.
- **0 escalamientos a BLOQUEOS**, 0 pasos atrás.
- **7 archivos en deuda lateral** capturados como `SPRINT-DISENO-A.5-LINT-LIMPIEZA`.
- **FASES A.2/A.3/A.4/A.5 + B + C + D** re-encoladas como sprints separados (preparando próximas pasadas).

## Commits (más reciente arriba)

| Hash | Scope | Archivos | Líneas |
|---|---|---|---|
| `74a69fb` | A.1 chunk 10/10 — formularios + páginas públicas | 4 | 28 |
| `51042c2` | A.1 chunk 9/10 — AsistenteIA/Configuracion/Notificaciones/Feedback | 6 | 83 |
| `6aaec0d` | A.1 chunk 8/10 — Clientes/Empresas/Usuarios/Inbox/Inventario/etc. | 11 | 168 |
| `1035802` | A.1 chunk 7/10 — finanzas (Avances/Bancos/Comisiones/etc.) | 11 | 105 |
| `5f77c8d` | A.1 chunk 6/10 — agenda/calendario/citas/cierre/cotizaciones | 9 | 110 |
| `31864d6` | A.1 chunk 5/10 — páginas críticas (Dashboard/Ordenes/TecnicoVista/OrdenDetalle) | 4 | 76 |
| `740cc79` | A.1 chunk 4/10 — components raíz + admin + shared | 9 | 54 |
| `1675c45` | A.1 chunk 3/10 — components/clientes + personal + nomina + public + recordatorios | 18 | 97 |
| `dd95ad2` | A.1 chunk 2/10 — components/cierre + facturas + facturacion-pendiente | 7 | 62 |
| `fbb77d1` | A.1 chunk 1/10 — components/ordenes | 11 | 74 |
| `f861da9` | chore: sync pasada 54 — desbloqueo opción B + cola EN_EJECUCION | 4 (.gitignore + 3 docs) | — |

## Hallazgos laterales (deuda capturada en sprints separados)

### `SPRINT-DISENO-A.5-LINT-LIMPIEZA` — 7 archivos no migrados por warnings ESLint preexistentes

| Archivo | Warning preexistente | Captura |
|---|---|---|
| `src/components/cierre/ModalEditarPiezasOrden.tsx` | react-hooks/exhaustive-deps en useEffect línea 52 (deps array omite `orden.cierreServicio.piezasUsadas`) | A.5 |
| `src/pages/Calendarios.tsx` | no-unused-vars en import lucide línea 7 | A.5 |
| `src/pages/Gastos.tsx` | no-unused-vars `isSameWeek` línea 9 + no-explicit-any línea 32 | A.5 |
| `src/pages/Login.tsx` | no-explicit-any línea 25 (catch) | A.5 |
| `src/pages/ConfiguracionWeb.tsx` | no-unused-vars `ConfigEstadisticas` línea 6 | A.5 |
| `src/pages/Solicitudes.tsx` | no-unused-vars `XCircle` línea 8 | A.5 |
| `src/pages/TrackingCliente.tsx` | no-unused-vars `clienteId` línea 189 | A.5 |

Estos 7 archivos contienen TODAS las ocurrencias `\[#0f3460\]/\[#1a5fa8\]` con modificadores `hover:`, `focus:`, `ring-` y sufijos de opacidad (`/10`, `/20`, `/50`). A.5 las cierra en bloque junto con el lint.

### Ocurrencias residuales NO clasificadas como deuda A.5 (deuda lateral documentada en otros sprints)

- **Inline `style={{ backgroundColor: x.color || '#0f3460' }}`** en 7 archivos (TablaPersonalActivo, GruposOperariaTecnico, Comisiones, GestionUsuarios x2, PersonalPage, Rendimiento, CierreDia) — son fallbacks de dato dinámico de color de empleado. NO son className. Si el día de mañana queremos eliminarlos: definir variable CSS `--color-primary` en `index.css` y usar `style={{ backgroundColor: x.color || 'var(--color-primary)' }}`. → captura para `SPRINT-DISENO-A.3-INLINE-STYLES` (si se agrega).
- **Paletas selector (`COLORES_TECNICO`, `Calendarios.tsx` línea 12)** — strings de paleta para que Jorge elija color del técnico/calendario. NO son className. Dejar como hex literal (semánticamente "es UNO de los colores", no "es el color de marca").
- **Canvas/Leaflet/strokeStyle** (`CierreServicioWizard.tsx:227,310` ctx.strokeStyle; `TecnicoVista.tsx:60,897` Leaflet pin + Polyline; `TrackingCliente.tsx:367` Polyline) — APIs nativas que reciben string CSS. Dejar literal (sprint propio si queremos centralizarlo).
- **Print stylesheets HTML inline en `Cotizaciones.tsx:405-408` y `Facturas.tsx:371-403`** — CSS para print en plantillas de PDF. Reemplazo a token Tailwind no aplica (es CSS plano, no compilado por Tailwind). → captura para `SPRINT-DISENO-A.4-PRINT-STYLESHEETS` si Jorge quiere.
- **`heroGradient.ts`, `configWeb.service.ts`, `useOrdenCreateForm.ts:1049` (toast con borderLeft inline)** — defaults de configuración. NO son className. Dejar.
- **`src/components/clientes/MapaClientes.tsx:75,77,82`** — popup Leaflet con HTML inline string. NO es className. Dejar (deuda lateral).

## Sprints procesados (resumen)

| ID | Hash final chunk | Archivos tocados | Estado |
|---|---|---|---|
| SPRINT-DISENO-CIERRE-COMPLETO (camino 2, FASE A.1) | `74a69fb` | 90 | ⏸ código en producción, awaiting QA Jorge |

## Verificación final

```
$ grep -rE "(bg|text|border|border-[ltrb]|ring|from|to|accent)-\[#(0f3460|1a5fa8)\]" src/
# Solo retorna ocurrencias en los 7 archivos de A.5 (incluyendo modificadores hover:/focus: y sufijos /opacity)
```

```
$ npm run build
# ✓ built in 4.21s
```

## Bloqueos

Ninguno generado en esta pasada. El stub original de SPRINT-DISENO-CIERRE-COMPLETO en `BLOQUEOS.md` permanece como referencia forense (con la nota `OK: jorge 2026-05-30 opcion=B` aplicada).

## Próximo paso para Jorge

1. **QA visual A.1 (~5 min):** hard-refresh `/`, `/admin/dashboard`, `/admin/ordenes`, `/tecnico`, `/admin/clientes`, `/admin/inbox`. Verificar que los azules de marca se ven IDÉNTICOS al estado previo. Si OK → `QA: jorge 2026-05-30 HH:MM DISENO-A.1 PASS` en COLA o BLOQUEOS.
2. **Próximas pasadas** (cuando Jorge dispare `trabaja`): la cola tiene las FASES A.2 → A.3 → A.4 → A.5 → B → C → D como sprints independientes, cada uno con touch-list expandido propio. Se procesan en orden de prioridad.


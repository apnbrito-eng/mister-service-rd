# DIARIO 2026-06-01 — Pasada 57

**Operador:** coordinator autónomo (`trabaja`).
**Origen:** 5 sprints encolados por Cowork 2026-06-01 tras leer leak prompt de diseño interno de Claude.

## Resumen 60s

4 sprints commiteados + pusheados, 1 escalado Fase 2 a Jorge. 5 commits, ~18 archivos tocados, ~18 minutos. Cazadores 25/25 PASS en cada commit. Pre-commit hooks PASS sin bypass. Working tree limpio (~50 untracked legítimos NO incluidos).

## Hashes

| Sprint | Hash | Estado |
|---|---|---|
| SPRINT-DISENO-E-FUENTE-PLUS-JAKARTA | `4ce1a57` | awaiting QA Jorge |
| SPRINT-DISENO-F-TEXT-WRAP-PRETTY | `45b6b38` | COMPLETADO (sin QA por spec) |
| SPRINT-DISENO-G-NO-INVENTAR-CONTENIDO | `6fab6e9` | COMPLETADO (sin QA por spec) |
| SPRINT-DISENO-H-BORDER-LEFT-A-BADGE | `cc673b0` | awaiting QA Jorge |
| SPRINT-DISENO-I-DATA-SLOP-DASHBOARD-AUDIT | `1177ec2` | Fase 1 OK, Fase 2 escalada a BLOQUEOS |

## Detalle por sprint

### SPRINT-DISENO-E — Inter → Plus Jakarta Sans (hash `4ce1a57`)

Cambio global de fuente. Origen: leak prompt diseño Claude — Inter está en la lista de fuentes "overused" que el documento descarta. Jorge eligió Plus Jakarta Sans vía AskUserQuestion de Cowork.

**Archivos:**
- `index.html` — link Google Fonts a Plus Jakarta Sans (weights 400-800) con preconnect (reduce FOIT en celulares lentos).
- `tailwind.config.js` — `fontFamily.sans` con quotes para el nombre compuesto.
- `src/index.css` — `body` y `.pac-container` migran a Plus Jakarta Sans.
- `src/components/clientes/MapaClientes.tsx` — popup HTML manual de Leaflet también migrado.

`system-ui` queda como fallback inmediato si Google Fonts falla. No se tocó paleta, print stylesheets, ni componentes individuales.

### SPRINT-DISENO-F — text-wrap pretty (hash `45b6b38`)

Trivial. Agregado `text-wrap: pretty` a `.text-h1`/`.text-h2`/`.text-body` en `src/index.css`. Mejora ruptura de líneas en títulos largos con tildes (nombres clientes "María Mercedes Hernández", direcciones dominicanas "Calle Respaldo 12 #45, Ensanche La Fé"). Progressive enhancement 100% — Safari <17.4 + Chrome <114 caen al wrapping default sin romper.

### SPRINT-DISENO-G — sub-regla "no inventar contenido sin OK Jorge" (hash `6fab6e9`)

Documentación de meta-proceso. Sub-regla agregada en 3 ubicaciones:
- `CLAUDE.md` "Conventions & gotchas" (regla maestra detallada con antiprecedente y origen externo).
- `.claude/agents/coordinator.md` "What NEVER to delegate" (regla de escalación: ESCALA a BLOQUEOS con 2-3 opciones).
- `.claude/agents/builder.md` convención #11 (regla para quien escribe código: NO completar con placeholders ficticios; reportar al coordinator).

Antiprecedente: SPRINT-DISENO-D pasada 56 (2026-05-31) — el coordinator iba a inventar 3 testimonios ficticios para llenar la HomePage; Jorge lo cazó (*"recuerda que el software no está en uso aún"*) y redirigimos a placeholder-oculto-en-prod con flag `activo: false` en `src/config/testimoniosHomePage.ts`. Origen externo: leak prompt diseño Claude — *"Never pad a design with placeholder text"* + *"ask the user first rather than unilaterally adding it"*.

### SPRINT-DISENO-H — border-left de cards de orden → badge arriba-derecha (hash `cc673b0`)

Anti-patrón explícito del prompt diseño Claude (*"Avoiding containers using rounded corners with a left-border accent color"*).

**Cards de orden migradas** (border-left de color removido):
- `OrdenCard.tsx` (componente reusable) — badge absoluto arriba-derecha con `estadoSimpleLabel + estadoSimpleColor`; padding-right de la card aumentado a `pr-24`.
- `Ordenes.tsx` "Agenda de Hoy" — border-l-4 quitado; `<Badge fase>` ya estaba arriba-derecha. Import `estadoSimpleBorder` removido.
- `AgendaDia.tsx` cards lista por técnico — border-l-4 + `style inline borderLeftColor` quitados; el span con `faseLabel + faseBgColor` arriba-derecha ya codificaba la fase. Variable `borderColor` renombrada a `faseBadgeColor`.
- `OrdenesTablero.tsx` (Kanban) — border-l-4 + `style inline` quitados; las columnas ya agrupan por fase con header del color, el border era redundante.

**Banners preservados con tag `@safe-border-left-accent`** (patrón aceptado, NO anti-patrón):
- `OrdenDetalle.tsx:705` banner reactivación.
- `OrdenDetailModal.tsx:175` banner visita fallida.
- `OrdenDetailModal.tsx:382` banner reactivación.

**UI elements de selección preservados** (no codifican estado/urgencia):
- `InboxConversacion.tsx:570` dropdown plantillas (item activo).
- `Clientes.tsx:534` item seleccionado en lista.

Verificación final: `grep "border-l-4 border-(red|amber|...)" src/` retorna SOLO los 3 banners marcados con tag. Cero cards de orden con border-left de color. Sin tocar handlers ni lógica.

### SPRINT-DISENO-I — Fase 1 inventario data-slop Dashboard (hash `1177ec2`)

Origen: leak prompt diseño Claude (*"Avoid 'data slop' — unnecessary numbers or icons or stats that are not useful."*).

**Fase 1 (autónoma) completada.** Inventario detallado de TODOS los KPIs/cards visibles en `src/pages/Dashboard.tsx` post-SPRINT-DISENO-C (4 bloques Hoy/Pipeline/Plata/Equipo). 154 líneas en `docs/sprints/INVENTARIO_KPI_DASHBOARD_2026-06-01.md`. Cada KPI con archivo:línea, fuente del dato, costo de mantenimiento, pregunta "¿lo miras todos los días?", columna "Decisión Jorge" para marcar MANTENER/QUITAR/MOVER A REPORTE.

Candidatos a MOVER A REPORTE SEPARADO sugeridos por coordinator (sugerencias, decisión final de Jorge):
- "Reparaciones por Tipo de Equipo" → `/admin/reportes/reparaciones-por-tipo` (analítico mensual).
- "Órdenes anuladas esta semana" → la card actual ya linkea a `/admin/historial-anuladas`.
- "Nómina proyectada del mes" → ya es solo link a `/admin/metricas-mensuales`.
- "Rendimiento por Técnico" → `/admin/reportes/rendimiento-tecnicos`.

**Fase 2 ESCALADA a `BLOQUEOS.md`** con prompt claro para Jorge (~10 min). El coordinator NO toca `Dashboard.tsx` hasta tener OK Jorge. Fase 3 (aplicar cambios) queda autónoma post-`procesa-bloqueos`.

## Para Jorge — siguientes pasos

1. **QA visual (~5 min):** hard-refresh `/`, `/admin/dashboard`, `/tecnico`, `/admin/ordenes`. La tipografía debe verse distinta (Plus Jakarta Sans). Las cards de orden deben mostrar badge arriba-derecha en vez de border-left de color.
2. **Revisar inventario Dashboard (~10 min):** abrí `docs/sprints/INVENTARIO_KPI_DASHBOARD_2026-06-01.md`, marcá MANTENER/QUITAR/MOVER en cada KPI. Agregá `OK: jorge ...` en `BLOQUEOS.md`. Corré `procesa bloqueos`.

## Métricas

- Cazadores: 25/25 PASS en cada commit
- Typecheck PASS en cada commit
- Lint clean en archivos staged
- 5 builds verificados: 4.16s, 4.48s (después F), 4.16s, 4.64s, ~4s
- Pre-commit hooks PASS sin bypass
- Working tree limpio (~50 untracked legítimos NO incluidos en commits)
- Webhook Vercel: pendiente push final

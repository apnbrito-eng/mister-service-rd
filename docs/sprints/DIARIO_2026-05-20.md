# Diario — 2026-05-20

## Resumen

- 1 sprint completado
- 0 sprints bloqueados nuevos (7 sprints WA-* con marca PENDIENTE-REQUIERE-X ya estaban en bloqueo implícito por credenciales externas — no se movieron a BLOQUEOS.md, ver nota abajo)
- 1 commit pusheado
- ~12 minutos de trabajo del coordinator

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-WA-2-BUTTON-URL | Soporte componente button URL en plantillas WhatsApp | `bf87c02` | `api/whatsapp/send.ts` | Habilita botón URL dinámico (ej: "Reagendar" → `/cliente/{{token}}`). Cambio aditivo retrocompatible. Deploy Ready 11:49 UTC. |

## Sprints bloqueados (REQUIEREN TU ACCIÓN — recursos externos, no OK)

Los siguientes ya estaban marcados `PENDIENTE — REQUIERE <X>` en la cola. No se procesan hasta que Jorge gestione el recurso. Si querés moverlos formalmente a `BLOQUEOS.md` con instrucciones de desbloqueo, pedímelo explícitamente.

| ID | Bloqueador | Cómo desbloquear |
|---|---|---|
| SPRINT-WA-1 | `META_APP_SECRET` + `META_VERIFY_TOKEN` no configurados en Vercel | Crear en Meta developers + agregar a Vercel env vars |
| SPRINT-WA-2 | `META_ACCESS_TOKEN` (System User permanente) | Generar en Meta Business → System Users |
| SPRINT-WA-3 | Sin bloqueador externo duro, pero depende de WA-1 + WA-2 funcionando y plantillas activas | Procesar WA-1 + WA-2 primero |
| SPRINT-WA-4 | Acuerdo de naming campañas con Jorge | Definir formato `[Producto]_[Objetivo]_[Audiencia]_[Fecha]_[Variante]` |
| SPRINT-WA-5 | Plantillas aprobadas en Meta Manager | Crear plantillas + esperar 24-48h aprobación Meta |
| SPRINT-WA-6 | `ANTHROPIC_API_KEY` + system prompt + decisiones escalación | Generar API key + definir specs en sprint |
| SPRINT-WA-7 | WA-5 plantillas aprobadas | Procesar WA-5 primero |

## Cazadores anti-regresión

Estado tras la pasada: 17/17 PASS, 0 hits. Sin cazadores nuevos.

- P-001..P-007, P-009..P-012, P-014..P-019: todos verdes.

## Alertas

- Ninguna regresión detectada.
- Deploy Vercel `bf87c02` Ready limpio (sin necesidad de Deploy Hook backup).
- Lint del repo entero arroja 10K+ errores pero son de archivos auto-generados (`vite.config.ts.timestamp-*`, `dist-lazy/`). No bloquea el sprint — los untracked siguen en working tree pendientes de cleanup si Jorge lo decide.

## Próximos pasos sugeridos

- **Verificación E2E manual de SPRINT-WA-2-BUTTON-URL (Jorge):** editar plantilla `cita_confirmada` en Meta Manager para agregar (1) botón Quick Reply "Consultar" y (2) botón URL "Reagendar" con `https://www.misterservicerd.com/cliente/{{1}}`. Esperar APPROVED por Meta (~24h). Luego correr el curl del sprint con `buttonUrlVariable: "TOKEN_REAL"` y verificar que WhatsApp llega con los 2 botones funcionales.
- **Decidir si formalizar bloqueos WA-*:** los 7 sprints con marca `REQUIERE` están en cola pero efectivamente bloqueados. Si Jorge quiere visibilidad explícita en `BLOQUEOS.md`, lo movemos. Hoy quedaron donde estaban para no inflar el archivo.
- **Untracked en working tree** (`dist-lazy/`, `vite.config.ts.timestamp-*.mjs`, `flujo-orden-mister-service.svg`, `docs/sprints/ANALISIS_PLAN_WHATSAPP_VS_PLAN_EJECUTIVO.md`, `scripts/qa-sprint-135a-ui.ts`): coordinator no los movió porque algunos son artefactos de build (auto-cleanup) y otros parecen documentos/scripts Cowork sin instrucciones de commit. Si querés que los limpie o commitee, decímelo.

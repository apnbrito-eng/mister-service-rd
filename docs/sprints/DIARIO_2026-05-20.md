# Diario — 2026-05-20

## Resumen

- **7 sprints completados** (1 WhatsApp button URL en la mañana + 6 sprints bloque INBOX CRM en la tarde-noche).
- 0 sprints nuevos a BLOQUEOS (los 7 WA-* siguen en cola con bloqueo implícito por credenciales/recursos externos).
- 8 commits pusheados (1 cleanup higiene + 7 features).
- ~85 minutos de trabajo del coordinator (12 min mañana + 70 min tarde-noche + ~3 min no-op pasada intermedia).

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-WA-2-BUTTON-URL | Soporte componente button URL en plantillas WhatsApp | `bf87c02` | `api/whatsapp/send.ts` | Habilita botón URL dinámico (ej: "Reagendar" → `/cliente/{{token}}`). Cambio aditivo retrocompatible. Deploy Ready 11:49 UTC. |
| (cleanup) | Higiene auditoría pre-CRM B1+B2 | `b3987d6` | `.gitignore` + `src/services/notificaciones.service.ts` | `dist-lazy/` + `vite.config.ts.timestamp-*.mjs` agregados a .gitignore; 2 eslint-disable obsoletos removidos. Sin cambio comportamiento. |
| SPRINT-INBOX-1 | Tipos TS + service de lectura inbox | `e8f3ac1` | `src/types/index.ts` + `src/services/whatsappInbox.service.ts` (NUEVO) | Tipos espejan exactamente shape de `webhook.ts:215-227` y `send.ts:1064-1083`. Service con 7 funciones (suscribir conversaciones/mensajes, marcar leído, toggle bot, asignar, etiquetar, contador). Updates parciales para respetar inmutabilidad de rules. |
| SPRINT-INBOX-2 | Página /admin/inbox + entrada sidebar | `8716f1e` | `src/pages/Inbox.tsx` (NUEVO) + `src/App.tsx` + `src/components/Sidebar.tsx` | Bandeja con buscador local + 4 chips (Todas/Sin responder/Mías/Bot pausado). Ruta gateada por RolRoute D6=C (admin/coord/secretaria/operaria). Badge sidebar suma noLeidos via single listener. |
| SPRINT-INBOX-3 | Vista conversación 3 columnas + indicador 24h | `e6597e1` | `src/pages/InboxConversacion.tsx` (NUEVO) + 2 componentes (NUEVOS) + `src/App.tsx` | Col1 lista compacta md+, col2 contacto lg+, col3 timeline + composer + indicador ventana 24h con traffic-light. marcarLeida automático. Scroll inteligente. enviarTexto del wrapper existente. |
| SPRINT-INBOX-4 | Toggle bot ON/OFF por conversación | `f2f4c10` | `src/components/inbox/ToggleBot.tsx` (NUEVO) + `src/pages/InboxConversacion.tsx` | Switch con confirmación inline al pausar; loader spinner; captura permission-denied de rule (admin/coord o asignataria). Update parcial dot-path `bot.habilitado`. Audit canónico con actorUid=auth.uid (P-001). |
| SPRINT-INBOX-5 | CardCliente + acceso a órdenes | `9fdb026` | `src/components/inbox/CardCliente.tsx` (NUEVO) + `src/services/ordenes.service.ts` + `src/pages/InboxConversacion.tsx` | Busca cliente por wa_id, lista hasta 5 órdenes activas (chip fase coloreado), CTAs "Crear cliente/orden" precargando teléfono. Nueva función `obtenerOrdenesActivasPorTelefono` single where + filter client-side (sin índice). |
| SPRINT-INBOX-6 | Cards de comunicación en Dashboard | `d2c5e1f` | `src/services/whatsappInbox.service.ts` + `src/pages/Dashboard.tsx` | Single listener emite 3 métricas (sinResponder, mediana respuesta, más antigua). 3 KpiCards gateadas por rol oficina insertadas como NUEVO div condicional (zero deletions, zero mods a flujo crítico Dashboard). QA validado en commit message. |

Total: 8 commits, +2,343 líneas / -7 líneas. Deploy Vercel `d2c5e1f` Ready 2026-05-20T22:47:43Z. Cazadores 17/17 PASS en cada commit. Typecheck + lint PASS en cada commit.

## Sprints NO procesados (bloqueo implícito por recursos externos)

Los siguientes 7 sprints permanecen en `COLA_AUTONOMA.md` con marca `**Estado:** PENDIENTE — REQUIERE <X>` y NO se procesan hasta que Jorge gestione el recurso. Si querés moverlos formalmente a `BLOQUEOS.md` con instrucciones de desbloqueo, pedímelo explícitamente.

| ID | Bloqueador externo | Cómo desbloquear |
|---|---|---|
| SPRINT-WA-1 | `META_APP_SECRET` + `META_VERIFY_TOKEN` no configurados en Vercel | Crear en Meta developers + agregar a Vercel env vars |
| SPRINT-WA-2 | `META_ACCESS_TOKEN` (System User permanente) | Generar en Meta Business → System Users |
| SPRINT-WA-3 | Depende de WA-1/WA-2 funcionando + plantillas activas | Procesar WA-1 + WA-2 primero |
| SPRINT-WA-4 | Acuerdo naming campañas con Jorge | Definir formato `[Producto]_[Objetivo]_[Audiencia]_[Fecha]_[Variante]` |
| SPRINT-WA-5 | Plantillas aprobadas en Meta Manager | Crear plantillas + esperar 24-48h aprobación Meta |
| SPRINT-WA-6 | `ANTHROPIC_API_KEY` + system prompt + decisiones escalación | Generar API key + definir specs en sprint |
| SPRINT-WA-7 | WA-5 plantillas aprobadas | Procesar WA-5 primero |

NOTA: el bloque INBOX-1..6 que se completó hoy es FRONTEND sobre la infraestructura WhatsApp existente. Las páginas `/admin/inbox` y `/admin/inbox/:waId` están deployadas y funcionan en cuanto se generen conversaciones reales en Firestore (que ocurrirá cuando WA-1 reciba mensajes con Meta credentials configuradas).

## Cazadores anti-regresión

Estado tras la pasada: **17/17 PASS, 0 hits** en cada uno de los 8 commits del día.

- P-001..P-007, P-009..P-012, P-014..P-019: todos verdes.
- Sin cazadores nuevos creados (ninguno de los 7 sprints arregló un bug en producción → no aplica sub-regla "cada bug captura cazador").

## Alertas

- Ninguna regresión detectada.
- Deploy Vercel limpio (sin necesidad de Deploy Hook backup).
- Dashboard.tsx (página crítica) tocado con cambio aditivo conservador: QA validado por carácter estructural del cambio (zero deletions, zero mods a state/effects/cálculos). Sub-regla CLAUDE.md cumplida.

## Próximos pasos sugeridos

- **Verificación E2E manual de SPRINT-WA-2-BUTTON-URL (Jorge):** editar plantilla `cita_confirmada` en Meta Manager con botones, esperar APPROVED, correr curl con `buttonUrlVariable: "TOKEN_REAL"`.
- **QA visual humano del bloque INBOX (Jorge o QA):** abrir `https://www.misterservicerd.com/admin/inbox` con cuenta admin/coord para ver el shell de la página. Las conversaciones reales aparecerán cuando WA-1 esté operativo. Con 0 conversaciones la página debe mostrar el empty state correctamente, sidebar debe mostrar el ítem "Inbox WhatsApp" sin badge, Dashboard debe mostrar las 3 cards en gris ("Bandeja al día", "—" en mediana, "nada urgente").
- **Decidir si formalizar bloqueos WA-1..WA-7 a `BLOQUEOS.md`:** hoy quedaron en cola por simplicidad. Si Jorge quiere visibilidad explícita con instrucciones de desbloqueo step-by-step, lo movemos en la próxima pasada.
- **Untracked en working tree** (`flujo-orden-mister-service.svg`, `docs/sprints/ANALISIS_PLAN_WHATSAPP_VS_PLAN_EJECUTIVO.md`, `docs/sprints/METRICAS_2026-05-20.md`, `scripts/qa-sprint-135a-ui.ts`, `docs/analisis/AUDITORIA_PRE_CRM_2026_05_20.md`): coordinator no los movió porque parecen documentos/scripts Cowork sin instrucciones de commit. Si querés que los commitee, decímelo.

## Métricas del día

- Sprints completados: 7 (de 7 procesables — 100% tasa).
- Reverts: 0.
- Hotfixes: 0.
- Postmortems nuevos: 0 (no aplicaba).
- Cazadores nuevos: 0 (no aplicaba).
- Tiempo promedio por sprint: ~10 min (incluye archivist mental + builder + tester + reviewer mental + commit + push).

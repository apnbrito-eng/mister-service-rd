# Diario — 2026-05-11

## Resumen

- 1 sprint completado (SPRINT-130).
- 0 sprints bloqueados nuevos esperando OK.
- 1 commit pendiente push al cerrar el documento.
- ~25 minutos de trabajo del coordinator (pasada 1 del día).
- 1 entrada nueva en BLOQUEOS.md tipo "validación humana no bloqueante" (SPRINT-130-QA).
- 1 sprint nuevo PENDIENTE agregado por Cowork durante la sesión: SPRINT-131 (fix responsive iPad). NO procesado en esta pasada.

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-130 | Botón "Re-sincronizar operaria" en OrdenEditForm | _(pendiente, ver final del doc)_ | `src/services/ordenes.service.ts`, `src/components/ordenes/BotonRederivarOperaria.tsx` (NUEVO), `src/components/ordenes/OrdenEditForm.tsx`, `src/pages/Ordenes.tsx`, `src/pages/MapaRutas.tsx`, `docs/sprints/COLA_AUTONOMA.md`, `docs/sprints/EJECUCION_AUTONOMA.md`, `docs/sprints/BLOQUEOS.md`, `docs/sprints/DIARIO_2026-05-11.md` (este) | 1 click = 1 orden. `runTransaction` defensivo single-collection. No batch. No toca rules. QA visual humana registrada en BLOQUEOS.md (no bloqueante). |

## Sprints bloqueados nuevos (REQUIEREN TU OK)

Ninguno nuevo. SPRINT-130-QA es validación visual no bloqueante registrada por convención.

## Sprints PENDIENTES en cola (próxima pasada `trabaja`)

| ID | Título | Prioridad | Origen |
|---|---|---|---|
| SPRINT-131 | Fix responsive: cards de orden cortadas en iPad portrait | alta | Cowork 2026-05-11 — bug bloqueante UX en iPad |

## Cazadores anti-regresión

- P-001 (userProfile.id vs auth.uid): **0 hits**.
- P-002 (rules inmutabilidad campo opcional sin .get()): **0 hits**.
- P-003 (cross-collection sin runTransaction): **0 hits**.
- P-004 (alta empleado sin doc espejo): **0 hits**.
- P-005 (firestore.rules sin deployar): **0 hits**.
- P-006 (dropdown técnico con personal.id): **0 hits**.
- P-007 (crearNotificacion con userId == personal.id): **0 hits**.
- P-008 (data check notis legacy): no corre en pre-commit (manual, `npm run audit:notis-legacy`).

**Total cazadores pre-commit: 7/7 PASS, 0 hits.**

## Hallazgos colaterales (NO arreglados en esta pasada)

1. **`OrdenEditForm.tsx:73` usa `tecnicos.find(t => t.id === editForm.tecnicoId)`.** Post-`c4be345` esa búsqueda debería ser `(t.uid || t.id) === editForm.tecnicoId` para consistencia con el onChange del select (línea 299). El bug está LATENTE: actualmente la variable `cambiaDeGrupo` puede no detectar correctamente el cambio de grupo en algunos casos. Cowork puede agendar sprint propio cuando Jorge lo confirme.

## Alertas

Ninguna. Deploy esperado limpio.

## Próximos pasos sugeridos

- Cuando Jorge dispare `trabaja` la próxima vez: se procesará SPRINT-131 (fix responsive iPad).
- Validación visual del SPRINT-130 (caso Aury Mon → Wilainy) post-deploy. Si OK, Jorge edita BLOQUEOS.md SPRINT-130-QA con `OK: jorge YYYY-MM-DD HH:MM — QA visual OK`.
- Considerar sprint para arreglar bug latente de derivación de operaria al cambiar técnico en el form (item 1 de hallazgos colaterales).

# Diario — 2026-05-11

## Resumen

- **10 sprints completados + 1 sub-sprint parcial** (SPRINT-130 pasada 1 + SPRINT-131 pasada 2 + SPRINT-132 pasada 3 + SPRINT-133 pasada 4 + SPRINT-134-mant pasada 5 + SPRINT-142c + 142b + 142d + 135a-backend en pasada 6 + **SPRINT-135a-UI en pasada 7**). Cierre del lote SPRINT-142 padre (refactor PersonalPage 1713→1122 líneas, -591 acumulado) Y cierre completo del SPRINT-135a fase 1 (backend + UI).
- 0 sprints bloqueados NUEVOS hoy (el SPRINT-135a-UI bloqueado en pasada 6 fue desbloqueado por Jorge en pasada 7).
- 10 commits pusheados.
- ~150 minutos totales de trabajo del coordinator (~95min pasadas 1-5 + ~45min pasada 6 + ~10min pasada 7).
- 5 entradas en BLOQUEOS.md tipo "validación humana no bloqueante" (SPRINT-130-QA + SPRINT-131-QA + SPRINT-132-QA + SPRINT-133-QA + SPRINT-134-mant-QA) + plan de QA del SPRINT-135a-UI pendiente que ejercitará Jorge.
- SPRINT-134 padre en EN_PROGRESO (1/6 funciones fixed); 5 sub-sprints restantes agendados.
- **SPRINT-142 padre cerrado** (4/4 sub-sprints completados).
- **SPRINT-135a fase 1 cerrado completo** (backend + UI). Base lista para SPRINT-135b/c/d/e (reclamo dentro de la misma orden + descuento técnico + toggle mal uso).

## Sprints completados

| ID | Título | Hash | Archivos | Notas |
|---|---|---|---|---|
| SPRINT-130 | Botón "Re-sincronizar operaria" en OrdenEditForm | `8d690ef` | `src/services/ordenes.service.ts`, `src/components/ordenes/BotonRederivarOperaria.tsx` (NUEVO), `src/components/ordenes/OrdenEditForm.tsx`, `src/pages/Ordenes.tsx`, `src/pages/MapaRutas.tsx`, 4 docs sprints | 1 click = 1 orden. `runTransaction` defensivo single-collection. No batch. No toca rules. QA visual humana registrada en BLOQUEOS.md (no bloqueante). |
| SPRINT-131 | Fix responsive: cards de orden cortadas en iPad portrait | `316009e` | `src/components/ordenes/OrdenCard.tsx` (1 línea), 4 docs sprints | Cambio CSS aislado: `md:flex-row` → `lg:flex-row` en `OrdenCard.tsx:68`. En iPad portrait (~810px) el card cae a layout column → botón "Cancelar" 100% visible. Desktop ≥1024 sin cambios. QA visual humana registrada en BLOQUEOS.md (SPRINT-131-QA, no bloqueante). |
| SPRINT-132 | Bug sistémico `find(p.id === tecnicoId)` post-c4be345 (12 lookups + 4 writes upstream) + cazador P-006 extendido | `43a2087` | `src/hooks/useOrdenCreateForm.ts`, `src/pages/{Ordenes,MapaRutas,Configuracion,Comisiones,CierreDia,Avances,PersonalPage}.tsx`, `src/components/ordenes/{OrdenEditForm,ModalEditarOrdenAdmin}.tsx`, `src/components/facturas/{FacturaItemsEditor,FacturaItemDetallesModal}.tsx`, `scripts/invariantes/check-tecnicoid-personal-id-misuse.ts`, `docs/PATRONES_REGRESION.md`, 2 docs sprints | Fix pattern uniforme `(X.uid \|\| X.id) === Y`. Explica caso Aury Mon de raíz (no solo timing). Resuelve el hallazgo colateral de SPRINT-130. Hallazgo adicional: 4 writes upstream con el mismo vector P-006 también corregidos (MapaRutas drag&drop + PersonalPage transferencias). Cazador P-006 extendido a variante `.find()` con sufijos `tecnicoId/operariaId/ayudanteId/responsableId/secretariaId/tecnicoDestinoId`. Test fixture verificó detección. QA funcional registrada en BLOQUEOS.md (SPRINT-132-QA, no bloqueante). |
| SPRINT-133 | `handleConfirmarEliminar` cross-collection sin tx + cazador P-003 extendido a src/pages | `15cab52` | `src/pages/PersonalPage.tsx`, `src/pages/{Cotizaciones,EquiposTaller,Inventario,Mantenimiento}.tsx` (allowlist `@safe-non-tx` follow-up), `scripts/invariantes/check-cross-collection-tx.ts`, `docs/PATRONES_REGRESION.md`, 2 docs sprints | Branches técnico + operaria de `handleConfirmarEliminar` envueltos en `writeBatch` con chunking de 499 ops. Delete del personal SIEMPRE al final del último chunk → atomicidad total si N≤499, parcial documentada >499. P-003 ampliado a `src/services/` + `src/pages/` + `src/hooks/` + `api/`. 7 hallazgos colaterales en otras funciones cross-collection allowlist-eadas con razón explícita (deuda agendada en SPRINT-134). Cleanup lint pre-existente en Cotizaciones.tsx (import `getDocs` unused + `any` → tipo correcto). Cazadores 7/7 PASS. QA funcional registrada en BLOQUEOS.md (SPRINT-133-QA, no bloqueante). |
| SPRINT-134-mant | `Mantenimiento.handleGenerarOrden` writeBatch atómico (1/6 del padre SPRINT-134) | `4138dc2` | `src/pages/Mantenimiento.tsx` (import + handler), 3 docs sprints | **Decisión coordinator: Opción 1 (uno por uno).** El sub-sprint 134a sugerido (`handleConvertirAFactura`) requiere clarificación de negocio sobre semántica "factura prevalece si falla descuento de stock" — postergado. Elegido `handleGenerarOrden` por ser el más limpio (2 writes fijos, sin reads dentro del bloque, sin try/catch con semántica especial). `addDoc(ordenes_servicio)` + `updateDoc(mantenimiento.proximaFecha)` → `writeBatch` con `batch.set` + `batch.update`. Counter `siguienteNumeroOrden()` FUERA del batch (ya transaccional internamente; precedente SPRINT-133). Payload preservado field-by-field. Cero cambios UI/toasts. Allowlist `@safe-non-tx` removida. Cazadores 7/7 PASS (P-003 baja de 7 a 6 allowlist hits). QA registrado en BLOQUEOS.md (SPRINT-134-mant-QA, no bloqueante). Padre SPRINT-134 en EN_PROGRESO 1/6. |
| SPRINT-142c | Extraer `GruposOperariaTecnico` de PersonalPage | `b45a6ba` | `src/components/personal/GruposOperariaTecnico.tsx` (NUEVO), `src/pages/PersonalPage.tsx` | Bloque "Grupos operaria-técnico" (~77 líneas) movido a componente puro. PersonalPage 1450→1377 (-73). Auto-oculta con null si no hay grupos. Sin lógica de datos modificada. Sin tocar handlers ni state. Cazadores 7/7 PASS, build OK. |
| SPRINT-142b | Extraer `ModalConfirmarEliminar` de PersonalPage | `6a0d10c` | `src/components/personal/ModalConfirmarEliminar.tsx` (NUEVO), `src/pages/PersonalPage.tsx` | Modal de confirmación de eliminación (~163 líneas) movido a componente nuevo. `handleConfirmarEliminar` con writeBatch + chunking del SPRINT-133 PERMANECE en PersonalPage como callback `onConfirmar`. Helpers locales (`getOrdenesActivasDeTecnico`, etc.) replicados dentro del componente; `destinosTransferencia` borrado de PersonalPage (ya no se usa). PersonalPage 1377→1233 (-144). Comentarios críticos preservados: SPRINT-132+P-006 (`value={d.uid \|\| d.id}`), @safe-non-tx allowlist intacto. Cazadores 7/7 PASS. |
| SPRINT-142d | `TablaPersonalActivo` + consolidar `utils/personal.ts` | `1425911` | `src/utils/personal.ts` (NUEVO), `src/components/personal/TablaPersonalActivo.tsx` (NUEVO), `src/pages/PersonalPage.tsx`, `src/components/personal/{FormAltaEditarEmpleado,GruposOperariaTecnico,ModalConfirmarEliminar}.tsx` | Cierre del lote 142. `utils/personal.ts` single source of truth para `ROL_LABELS`, `ROL_COLORS`, `ROLES_CON_COMISION`, `ROL_SELECT_ORDEN`, `comisionDefaultPorNivel`. Tabla del personal activo extraída (~98 líneas). 4 archivos migrados al import central. PersonalPage 1233→1122 (-111). **Total lote 142: 1713→1122 (-591 líneas).** Tabla de INACTIVOS sigue inline (extraerla es deuda si Jorge lo prioriza). SPRINT-142 padre marcado COMPLETADO. |
| SPRINT-135a backend | Refactor garantía fase 1, parte backend (modelo + helpers) | `75f6c7b` | `src/types/index.ts`, `src/utils/index.ts`, `src/utils/garantia.ts` (NUEVO), `src/components/ordenes/OrdenesTablero.tsx` | `FaseOrden` enum agrega `'garantia_reclamada'`; `VisitaGarantia` NUEVO interface con 14 campos; `OrdenServicio` agrega 3 campos opcionales (`visitasGarantia`, `periodoGarantiaDias`, `garantiaVencimiento`). `utils/index.ts` 4 maps actualizados (faseLabel/faseColor/faseBgColor/faseToEstadoSimple). `utils/garantia.ts` con helpers puros (`calcularVencimiento`, `vencimientoDeOrden`, `diasRestantes`, `estaDentroDePeriodo`, `PERIODO_GARANTIA_DEFAULT_DIAS = 60`). OrdenesTablero exhaustive check actualizado. **Fase UI** (countdown público + input wizard cierre) movida a BLOQUEOS.md como **SPRINT-135a-UI** por requerir cambio en endpoint público `api/garantia/[token].ts` + QA visual humano en wizard de cierre. |
| SPRINT-135a-UI | Refactor garantía fase 1, parte UI (countdown público desde modelo nuevo + input período en wizard cierre) | `d0f11d4` | `api/garantia/[token].ts`, `src/components/CierreServicioWizard.tsx`, `.husky/pre-commit`, 2 docs sprints | Cerrada fase UI del refactor garantía. (a) Wizard: nueva sección "🛡️ Período de garantía (días)" con input default 60, min 1, max 365, validación visual amber. Persiste `periodoGarantiaDias` + `garantiaVencimiento` a nivel orden al cerrar. (b) Endpoint: fallback no-breaking que prefiere modelo nuevo (`OrdenServicio.periodoGarantiaDias/garantiaVencimiento/cierreServicio.fechaCierre`) si la factura tiene `ordenId` y la orden lo tiene poblado. Sin breaking changes — shape del response idéntico. Órdenes legacy siguen funcionando vía fallback al modelo viejo (`facturas.garantia.*`). (c) Hook fix latente: `.husky/pre-commit` agregó `--no-warn-ignored` a eslint para no romper con archivos `api/` ignorados. OK humano: jorge 2026-05-11 18:25 vía BLOQUEOS.md con scope: ambos. QA flujo cierre técnico PENDIENTE (Jorge ejercitará según plan post-deploy del spec). |

## Sprints bloqueados nuevos (REQUIEREN TU OK)

**Ninguno nuevo en pasada 7.** El SPRINT-135a-UI bloqueado en pasada 6 fue desbloqueado por Jorge (`OK: jorge 2026-05-11 18:25 | scope: ambos`) y procesado autónomo en pasada 7 (`d0f11d4`). Movido a histórico de desbloqueos en BLOQUEOS.md preservando spec original para forensia.

**Pre-existentes pendientes de OK (sin movimientos hoy):**
- `SPRINT-138` — `storage.rules` versionado + flujo `deploy:storage-rules`. Esperando que Jorge dicte el baseline actual de la consola.
- `SPRINT-141` — App Check enforce con ventana monitoreo 48h. Esperando Jorge inicie Paso 1 (baseline) en Firebase Console.

Las 5 entradas previas SPRINT-130-QA + SPRINT-131-QA + SPRINT-132-QA + SPRINT-133-QA + SPRINT-134-mant-QA son validaciones funcionales/visuales no bloqueantes registradas por convención (no requieren OK; cierran cuando Jorge valida).

## Sprints PENDIENTES en cola (próxima pasada `trabaja`)

**SPRINT-134** — Refactor a `writeBatch` de los 7 cross-collection en `src/pages/` (follow-up SPRINT-133). **EN_PROGRESO 1/6 fixed en pasada 5** (`Mantenimiento.handleGenerarOrden`, commit `4138dc2`). 5 sub-sprints restantes para próximas pasadas:

- **`134-eqstandby`** — `EquiposTaller.handleChangeEstado`. Riesgo bajo (similar a 134-mant: 2 writes fijos, sin reads, condicional). Recomendado próximo.
- **`134-invajuste`** — `Inventario.handleConfirmarAjuste`. Riesgo bajo (2 writes fijos, sin reads).
- **`134-cotizsubmit`** — `Cotizaciones.handleSubmit`. Riesgo medio (cotización + lead orden).
- **`134-personalalta`** — `PersonalPage.handleSubmit`. Riesgo alto (overlap con P-004, cross-db con secondaryDb).
- **`134-personalvinc`** — `PersonalPage.ejecutarVinculacion`. Riesgo alto (vinculación Auth).
- **`134-cotizfac`** — `Cotizaciones.handleConvertirAFactura`. **REQUIERE CLARIFICACIÓN DE NEGOCIO PRIMERO** (Cowork debería plantearle a Jorge: ¿queremos atomicidad total — revertir factura si UNA pieza falla al descontar stock — o mantener semántica actual "factura prevalece, admin reconcilia stock manualmente"?). Hasta que se aclare, este sub-sprint NO procesa autónomo.

## Cazadores anti-regresión

- P-001 (userProfile.id vs auth.uid): **0 hits**.
- P-002 (rules inmutabilidad campo opcional sin .get()): **0 hits**.
- P-003 (cross-collection sin runTransaction): **0 hits** post-allowlist; **scope ampliado en pasada 4** a `src/services/` + `src/pages/` + `src/hooks/` + `api/`. 6 entradas allowlist `@safe-non-tx: SPRINT-134 follow-up` restantes (deuda agendada con razón explícita por función). **Baja de 7 a 6 en pasada 5** post-fix de `Mantenimiento.handleGenerarOrden`.
- P-004 (alta empleado sin doc espejo): **0 hits**.
- P-005 (firestore.rules sin deployar): **0 hits**.
- P-006 (dropdown técnico con personal.id): **0 hits**.
- P-007 (crearNotificacion con userId == personal.id): **0 hits**.
- P-008 (data check notis legacy): no corre en pre-commit (manual, `npm run audit:notis-legacy`).

**Total cazadores pre-commit: 7/7 PASS, 0 hits en las 4 pasadas.**

Nota: P-006 fue extendido en pasada 3 con variante `.find()` cubriendo `.ts` además de `.tsx`. P-003 fue extendido en pasada 4 con scope a `src/pages/` + `src/hooks/`. Ambos siguen retornando 0 hits post-fix.

## Hallazgos colaterales (NO arreglados en estas pasadas)

1. ~~**`OrdenEditForm.tsx:73` usa `tecnicos.find(t => t.id === editForm.tecnicoId)`** (desde pasada 1)~~ — **RESUELTO en pasada 3 (SPRINT-132, `43a2087`)**. El bug era sistémico: 12 sitios + 4 writes upstream. Cazador P-006 extendido para prevenir reintroducción.
2. **`npm run lint` global reporta 5554 errores pre-existentes** (detectado en pasada 2). No relacionados con los sprints del día. Deuda técnica de lint acumulada; Cowork puede agendar sprint de cleanup si Jorge prioriza.
3. ~~**`PersonalPage.tsx handleConfirmarEliminar` hace mutación cross-collection (`ordenes_servicio` + `personal`) sin `runTransaction`** (detectado en pasada 3 durante regression_guardian de SPRINT-132). Bug preexistente, no introducido hoy. P-003 actual escanea solo `src/services/` — no lo cazó porque vive en `src/pages/`.~~ — **RESUELTO en pasada 4 (SPRINT-133, `15cab52`)**. Envuelto en `writeBatch` con chunking. Cazador P-003 extendido a `src/pages/` + `src/hooks/`.
4. **7 funciones cross-collection adicionales en `src/pages/`** detectadas al extender P-003 en pasada 4: `Cotizaciones.tsx handleConvertirAFactura` (3 colecciones, el más riesgoso), `Cotizaciones.tsx handleSubmit`, `EquiposTaller.tsx handleChangeEstado`, `Inventario.tsx handleConfirmarAjuste`, ~~`Mantenimiento.tsx handleGenerarOrden`~~ — **RESUELTO en pasada 5 (SPRINT-134-mant, `4138dc2`)**, `PersonalPage.tsx handleSubmit` (alta empleado, overlap con P-004 sin solapar invariante), `PersonalPage.tsx ejecutarVinculacion`. Allowlist transparente con `@safe-non-tx: SPRINT-134 follow-up`. Refactor a `writeBatch` agendado como **SPRINT-134** en COLA_AUTONOMA.md. Decisión coordinator pasada 5: Opción 1 (uno por uno con QA visual humana entre cada deploy). Restantes: 6/6 → 6 hits allowlist tras pasada 5.

## Alertas

Ninguna. Deploys esperados limpios. Vercel deploys verificados:
- `43a2087` (SPRINT-132) en producción a las 16:12 UTC.
- `15cab52` (SPRINT-133) en producción a las 17:02 UTC.
- `4138dc2` (SPRINT-134-mant) en producción a las 17:23 UTC (verificado por poll de `version.json`).
- `d0f11d4` (SPRINT-135a-UI) en producción a las ~22:46 UTC (verificado por poll de `version.json` al sexto intento, ~120s post-push).

## Próximos pasos sugeridos

- Validación humana de los 6 sprints del día post-deploy:
  - Si OK SPRINT-130: editar BLOQUEOS.md SPRINT-130-QA con `OK: jorge YYYY-MM-DD HH:MM — QA visual OK`.
  - Si OK SPRINT-131: editar BLOQUEOS.md SPRINT-131-QA con `OK: jorge YYYY-MM-DD HH:MM — QA visual OK`.
  - Si OK SPRINT-132: editar BLOQUEOS.md SPRINT-132-QA con `OK: jorge YYYY-MM-DD HH:MM — QA validado`.
  - Si OK SPRINT-133: editar BLOQUEOS.md SPRINT-133-QA con `OK: jorge YYYY-MM-DD HH:MM — QA validado`.
  - Si OK SPRINT-134-mant: editar BLOQUEOS.md SPRINT-134-mant-QA con `OK: jorge YYYY-MM-DD HH:MM — QA validado`.
  - **SPRINT-135a-UI:** ejercitar el Plan de QA del spec (en COLA_AUTONOMA.md sección histórico): (1) cerrar orden con período 1 día → `/garantia/:token` muestra "1 día restante"; (2) setear `garantiaVencimiento` a ayer en Firestore Console → estado "expirada"; (3) cerrar con default 60 → `ordenes_servicio/{id}.garantiaVencimiento == fechaCierre + 60d`; (4) órdenes legacy sin campos nuevos → endpoint sigue mostrando countdown desde `facturas.garantia.*` (fallback).
- **Próxima pasada `trabaja`** puede procesar `134-eqstandby` (similar a 134-mant, riesgo bajo) y/o `134-invajuste`. Idealmente uno por pasada para preservar QA visual humana entre deploys.
- **Cowork debería plantearle a Jorge** la decisión de negocio sobre `134-cotizfac`: ¿queremos atomicidad total al convertir cotización a factura (revertir factura si UNA pieza falla al descontar stock) o mantener semántica actual ("factura prevalece, admin reconcilia stock manualmente" — el código original tiene este comentario explícito en el try/catch del loop de piezas)? Sin decisión, ese sub-sprint NO procesa autónomo.
- Considerar sprint de cleanup de lint si los 5554 errores pre-existentes molestan al pre-commit hook a futuro.
- Considerar sprint de re-auditoría con SPRINT-129 ahora que el bug sistémico está corregido: las órdenes viejas con `operariaNombre` vacío seguirán sin auditarse como "desincronizadas" porque el script de SPRINT-129 busca "técnico tiene operariaId Y orden NO tiene operariaNombre" — eso ahora puede detectar las víctimas del bug pre-SPRINT-132. Un `--apply` opcional resincronizaría retroactivamente.
